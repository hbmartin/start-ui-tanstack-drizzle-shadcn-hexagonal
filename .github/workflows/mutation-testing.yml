name: Mutation Testing

on:
  push:
    tags:
      - 'v*'
  schedule:
    - cron: '17 6 * * 1'
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  critical:
    name: Stryker (${{ matrix.scope }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        scope:
          [profile, auth, book, genre, kernel, runtime-config, shared, user]
    env:
      SKIP_ENV_VALIDATION: true
      VITE_BASE_URL: http://localhost:3000
      VITE_S3_BUCKET_PUBLIC_URL: http://localhost:9000/default
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          node-version: 24

      - name: Run scoped mutation tests
        run: node scripts/run-mutation.mjs --scope=${{ matrix.scope }}

      - name: Upload mutation report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: mutation-report-${{ matrix.scope }}
          path: |
            reports/mutation/${{ matrix.scope }}/
            reports/stryker-incremental/${{ matrix.scope }}.json
          if-no-files-found: ignore
          retention-days: 30
