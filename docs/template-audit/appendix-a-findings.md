# Appendix A — All verified findings

315 findings that survived adversarial verification, sorted by severity. `Apps` records which downstream app already solved the problem — an entry of `both` or two apps listed independently is the strongest signal that the practice belongs in the template.

Full evidence (file:line references, commit SHAs, quoted snippets) for each finding lives in the per-track appendices B–H; this table is the index.

**Severity mix:** 22 critical · 99 high · 148 medium · 46 low


## Critical

| # | Finding | Kind | Area | Apps | Effort | Conf. | Recommendation |
|---|---|---|---|---|---|---|---|
| 1 | Template docs assert `src/start.ts` does not exist, while it does and is the sole CSRF registration point — a false security claim both apps inherited and independently corrected | doc-drift | Agentification | hume, iris | trivial | high | Replace docs/strict-modular-monolith.md:110-121 with APP2's text (/home/user/iris-insights-crm/docs/strict-modular-monolith.md:110-118), which is already correct and already names the browser-mutation guard. Add a '## Request Middleware' section to AGENTS.md listing the seven middlewares in order and stating that defining src/start.ts replaces framework defaults. Add a guard in tests/architecture/ (the pattern already exists — tests/architecture/dependency-cruiser.unit.spec.ts:23-31 reads .dependency-cruiser.cjs as a string and asserts on its content) that (a) reads src/start.ts and asserts `csrfMiddleware` appears inside the requestMiddleware array, and (b) greps docs/**/*.md for the string 'does not define `src/start.ts`' and fails. |
| 2 | Template's `security:risk-register` gate is expired — a fresh clone fails `pnpm check` and the CI `audit` job on day one | defect | CI/CD, supply chain, and release engineering | — | small | high | (1) Refresh `docs/security-risk-register.md` by adopting the app overrides (`launch-editor: ^2.14.1`, `js-yaml@4: ^4.3.0`, `protobufjs@7: ^7.6.5`, `esbuild@0.28: ^0.28.1`) and deleting the resolved rows rather than bumping dates. (2) Split the gate: fail PRs only on newly added rows with a missing/invalid review date, and move the expiry check to a scheduled workflow (`on: schedule`, `permissions: {issues: write}`) that opens a tracking issue instead of turning every PR red. (3) Document in README/AGENTS that the register must be re-seeded on fork. |
| 3 | `pnpm env:client` validates nothing — the build-time client-env gate is a no-op, and it took APP2's production down on every request | defect | CI/CD, supply chain, and release engineering | iris | trivial | high | Add `scripts/validate-client-config.ts` (APP2's 5-line file) and repoint `"env:client"` at it. Add a regression test asserting `pnpm env:client` exits non-zero when a required `VITE_*` is unset. Separately consider APP2's larger change — moving server-only URL config off the `VITE_` prefix (`VITE_BASE_URL` → `APP_URL`, visible in APP2's e2e-tests.yml diff at lines 37 and 178) so it stops being inlined into the browser bundle — and note the same commit documented that `import.meta.env.PROD` is a build-time constant identical across preview and production deploys of one artifact, which the template's README-documented Vercel-preview `VITE_BASE_URL` derivation (README.md:266) inherits. |
| 4 | docs/strict-modular-monolith.md tells readers the template has no src/start.ts and therefore inherits default CSRF — the file exists, owns the middleware chain, and a template unit test already pins it | doc-drift | Documentation, onboarding, and doc-drift | iris | trivial | high | Replace docs/strict-modular-monolith.md:110-119 with iris's corrected text verbatim (it names the real middleware order, the `handlerType === 'serverFn'` scope, `referer: true`, `secFetchSite: 'same-origin'`, and the `browserMutationGuardMiddleware` companion) and add the cross-reference to `tests/unit/start.unit.spec.ts` that already proves it. Do NOT add a new test — add a docs-lint rule instead: assert that no doc sentence of the form "does not define <path>" / "does not exist" names a path that exists on disk. |
| 5 | Template's own `pnpm check` / `pnpm verify` fails on a fresh clone: every risk-register review date expired 2026-07-23 | defect | Documentation, onboarding, and doc-drift | both | trivial | high | Refresh the register now, and make expiry a template maintenance obligation rather than a landmine every fork trips on day one: add a scheduled weekly GitHub Actions job running `pnpm security:risk-register` that opens an issue on failure. Add an `Owner` column (neither app's register has one). Document in README/AGENTS.md that re-reviewing the inherited register is a fork's first task — both apps figured this out unaided. |
| 6 | docs/security practices.md is a verbatim import from an unrelated codebase; every checkable number and filename in it is wrong, and it propagated byte-identically to iris | doc-drift | Documentation, onboarding, and doc-drift | neither | small | high | Delete the file. If a security-practices summary is wanted, rewrite from scratch, rename to `docs/security-practices.md` (no space), and generate the scanner-count rows from a script that counts `- id:` in .semgrep.yml and `name:` in .dependency-cruiser.cjs and fails `pnpm check` on mismatch. That two production forks shipped this file unedited is the clearest evidence forks trust template docs without reading them. |
| 7 | No versioning, CHANGELOG, tags, or upstream-merge mechanism — a template fix cannot reach either fork, and no fork records which template revision it came from | missing-practice | Documentation, onboarding, and doc-drift | neither | medium | high | Two-part mechanism. (1) Publish releases: semver-tag `main`, keep a CHANGELOG.md with a `### Breaking for forks` section per release, and stamp the baseline into a `template.json` (`{ "template": "hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal", "version": "4.1.0", "sha": "…" }`) committed at each fork's root. (2) Make forks mergeable: fork WITH history (not a `first commit` squash), `git remote add template https://github.com/hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal`, and document `docs/pulling-template-updates.md` with the expected conflict zones (.dependency-cruiser.cjs, sheriff.config.ts, .semgrep.yml, package.json scripts, docs/architecture/*). Add a reverse channel — `docs/upstreaming.md` — telling app authors which of their findings are template-shaped; iris's rate-limiting discovery is exactly the case that was lost. |
| 8 | `pnpm env:client` validates nothing — the template's client env check is a structural no-op that took an app down in production | defect | app-inventions | iris | trivial | high | Add `scripts/validate-client-config.ts` (`import { getEnvClient } from '@/platform/env/client'; getEnvClient();`) and repoint `package.json:41`. Add a unit/integration test asserting the script exits non-zero when a required `VITE_*` is unset. Separately, make module-scope consumers lazy — `src/modules/auth/presentation/better-auth-client.ts:28` constructs at module scope today, so one bad value takes down every route rather than one. |
| 9 | Canonical app-URL / deployment-signal resolution: both apps independently invented `app-url.ts`; the template has none | missing-practice | app-inventions | both | medium | high | Add `src/modules/kernel/infrastructure/config/app-url.ts` modelled on APP2's, exporting `getAppUrlConfig(): { origin, host, hostname, isHttps }` and the `AppUrlSource` union. Introduce a server-only `APP_URL`, drop `VITE_BASE_URL` from `clientSchema()`, change `security-headers` to take `isHttps: boolean` (APP2 already proved this is the only thing it read the URL for), and have `better-auth-client.ts` derive `baseURL` from `window.location.origin`. Put the Vercel-specific readers behind a small platform-signal table so Fly/Render/Railway can be added without touching precedence. Ship `app-url.unit.spec.ts` with it. |
| 10 | PGlite globalSetup teardown discards Vitest's exit code — `pnpm test` and `pnpm verify` exit 0 with failing tests | defect | app-inventions | iris | trivial | high | Apply APP2's change verbatim to `tests/server/pglite-global-setup.ts`, keeping the comment. Add a CI smoke assertion that runs the full suite against a deliberately-failing fixture behind a flag and asserts non-zero exit, so a future teardown that swallows the code is caught. |
| 11 | `pnpm env:client` validates nothing: it imports a module that only re-exports a lazy Proxy, and it runs inside both `pnpm dev` and `pnpm build` | defect | config-divergence | iris | trivial | high | Copy APP2's `scripts/validate-client-config.ts` and repoint `package.json:41` at it. Add a unit test under `tests/unit/platform/env/` that spawns the entry module with a required `VITE_*` unset and asserts a non-zero exit, so the no-op cannot silently return again. Separately adopt APP2's structural fix: client factories that read env must be lazily constructed, not built at module scope. |
| 12 | Template's CSP nonce rewrite runs inside request middleware and replaces the SSR Response, breaking TanStack Start's stream/dispose contract — APP1 fixed it properly, then deleted CSP entirely 40 minutes later | defect | hume-archaeology | hume | medium | high | Port 907a42f into the template verbatim: (1) add `replaceCspNoncePlaceholderInSsrResponse` to src/platform/http/csp-nonce-server.ts using `normalizeSsrResponse` and a `serverSsrCleanup: 'stream'` disposer that cancels the rewritten body then delegates to the original dispose; (2) replace src/server.ts's `handler.fetch` with `createStartHandler(defineHandlerCallback(...))` wrapping `defaultStreamHandler`; (3) strip `replaceCspNoncePlaceholderInHtmlResponse` out of `securityHeadersMiddleware` at src/start.ts:158-160 so that middleware only mutates headers and never replaces the Response. Add the `expect(result.response).toBe(originalResponse)` regression test, plus an e2e/integration test that actually streams an SSR page with a nonce — unit mocks did not catch this. |
| 13 | `pnpm check` fails on a fresh clone of the template today — every risk-register advisory expired 2026-07-23 | defect | iris-archaeology | iris | small | high | Two changes. (1) Clear the advisories: port Iris's pnpm-workspace.yaml override block (brace-expansion@1/2/5, esbuild@0.28, fast-uri, js-yaml@4, launch-editor, postcss, protobufjs@7, shell-quote, socket.io-parser, svgo, undici, body-parser) and bump vitest/@vitest/browser/@vitest/browser-playwright from 4.1.9 to ≥4.1.10. (2) Make the time bomb non-fatal for forks: check-risk-register.mjs should hard-fail only for entries that are BOTH expired AND still reported by `pnpm audit`, or the template should ship the register with zero accepted entries. A starter whose advertised merge gate is guaranteed to fail N days after release is the worst first-run experience in the repo. |
| 14 | `runtimeEnv()` spreads frozen build-time `import.meta.env` AFTER runtime `process.env`, so baked build values override the platform's runtime values | defect | iris-archaeology | iris | small | high | Swap the spread order in BOTH copies and port Iris's `buildTimeEnv()`/`processEnv()` split with its docblock — the comment is what stops the next person re-inverting it. Add `getEnvSourceDrift()` (~8 lines); it turns 'I changed it in the dashboard and nothing happened' into a one-line diagnostic. Add an architecture/unit test asserting `process.env` wins for a key present in both. |
| 15 | The template has no server-side canonical-origin resolution; `VITE_BASE_URL` is a required client env var, and BOTH apps independently invented a replacement | missing-practice | iris-archaeology | both | large | high | Port Iris's src/modules/kernel/infrastructure/config/app-url.ts plus the `getDeploymentEnvironment` / `isLocalRuntimeEnvironment` additions to env-schema.ts, and delete `VITE_BASE_URL` from the client schema. Keep the three design points: (1) the canonical origin is SERVER-ONLY — all real consumers (cookie scoping, Better Auth allowedHosts, security headers) are server-side and the browser uses `window.location.origin`; (2) production-ness derives from the platform's runtime signal, not `import.meta.env.PROD` (a build-time constant identical across preview and production of one artifact); (3) the localhost fallback requires a POSITIVE local signal, never merely the absence of a production one. Also normalize each signal before applying `??` precedence (iris 0adcf25). |
| 16 | CSP nonce rewrite runs inside a request middleware and replaces the SSR Response, breaking TanStack Start's stream-cleanup contract — APP1 patched it, then deleted CSP entirely | defect | platform-runtime | hume | medium | high | Move `replaceCspNoncePlaceholderInHtmlResponse` out of `securityHeadersMiddleware` and into a `defineHandlerCallback` composed with `createStartHandler` in `src/server.ts`, keeping nonce minting in the middleware (src/router.tsx already reads it from the start context). Port APP1's `replaceCspNoncePlaceholderInSsrResponse` into `src/platform/http/csp-nonce-server.ts`. Add tests that the middleware passes the response through by identity and that SSR cleanup metadata survives the rewrite, and validate against a streamed SSR route with a real client abort — unit tests did not catch this. |
| 17 | Canonical app origin is a build-time `VITE_BASE_URL` whose only consumers are server-side; BOTH apps independently built runtime origin resolution | missing-practice | platform-runtime | both | medium | high | Introduce a server-only `APP_URL` in `src/modules/kernel/infrastructure/config/app-url.ts` modeled on APP2's: explicit `APP_URL` wins; otherwise branch on a normalized deployment environment (preview → `VERCEL_BRANCH_URL`, production → `VERCEL_PROJECT_PRODUCTION_URL`; never `VERCEL_URL`); fall back to localhost only on a positive local signal and fail loudly otherwise. Delete `VITE_BASE_URL` from `src/platform/env/config.ts`, change `security-headers.ts` to take `isHttps: boolean`, and point auth cookie/baseURL at the new config. Ship APP2's `config-diagnostics.ts` `[config]` boot line. |
| 18 | `runtimeEnv()` spreads frozen build-time `import.meta.env` AFTER `process.env`, so baked values override runtime injection — in both the platform and kernel copies | defect | platform-runtime | iris | trivial | high | Swap the spread order in both `src/platform/env/config.ts:11-14` and `src/modules/kernel/infrastructure/config/env-schema.ts:9-12` so `process.env` is spread last, with APP2's docblock. Widen `isTruthy` to accept `1`/`'1'` in both. Port `getEnvSourceDrift()`. Add a unit test asserting a runtime value beats a baked one — the defect is invisible in source and appears only in the built artifact. |
| 19 | Two dependency-cruiser layering rules use hardcoded module allowlists and fail open for every new module; Sheriff, the only backstop, runs nowhere | defect | template-friction | neither | trivial | high | Rewrite `domain-no-presentation` and `application-no-presentation` in `.dependency-cruiser.cjs` as `from: '^src/modules/([^/]+)/domain'` → `to: '^src/modules/$1/presentation'` (the backreference style `no-cross-feature-deep-import` already uses). DELETE `infrastructure-no-presentation` entirely — it is redundant (see refuted item). Replace `(?:account\|book\|genre\|user)` in `sheriff.config.ts:44,47` with `[^/]+`. Add `lint:sheriff` to both `check` and `check:ci`, and add it to `.github/workflows/code-quality.yml`. Replace the hardcoded arrays in `tests/architecture/modular-monolith.unit.spec.ts:426,439,454` with `fs.readdirSync('src/modules')`. Add one architecture test asserting no guardrail config contains a literal module name that is not a directory under `src/modules/`. |
| 20 | `pnpm env:client` validates nothing — it runs a file whose entire body re-exports a lazy Proxy | defect | template-friction | iris | trivial | high | Add `scripts/validate-client-config.ts` containing `import { getEnvClient } from '@/platform/env/client'; getEnvClient();`, point `env:client` at it, and add `getEnvClient` to the `src/platform/env/client.ts` re-export. Add a unit test that spawns the script with a deliberately invalid `VITE_*` value and asserts non-zero exit, so the gate cannot silently regress to a no-op. |
| 21 | The demo book/genre domain is load-bearing in kernel, auth, platform, routes, the app shell nav, and the DB seed — it is not deletable by removing two directories | dead-weight | template-friction | both | medium | high | Move the three branded-ID schemas and parsers out of `src/modules/kernel/domain/ids.ts` into the owning modules; remove `book`/`genre` from `kernel/backend.ts:18` and the kernel schema barrel by letting modules register their own Drizzle tables; drop the book/genre rows from `auth/domain/permissions.ts`; make `src/routes/api/upload.ts` dispatch through a registry rather than importing `handleBookUploadRequest`; make `drizzle/seed/index.ts` discover seeders instead of hardcoding `createBooks()`; make the shell nav config data-driven. Then either ship the demo on an `examples/` branch or provide a `pnpm remove:demo` script that performs the deletion mechanically. Repoint the platform/kernel/auth tests that use `book` as the generic fixture onto `user`, which every fork keeps. |
| 22 | The Better Auth client is constructed at module scope and reads `envClient.VITE_BASE_URL` during SSR — one missing env var takes down every route, not just auth | defect | template-friction | iris | small | high | Port Iris's fix into the template verbatim: build the client through a memoized `createBetterAuthClient()` factory called from each browser-triggered entry point, pass `baseURL: undefined` on the server, and remove the `envClient` import from `src/modules/auth/presentation/better-auth-client.ts`. Add an architecture or unit test asserting that no `src/modules/*/presentation/**` module reads `envClient.*` at module scope. This is the second of the three defects Iris's outage post-mortem identified; the investigator reported only the first. |

## High

| # | Finding | Kind | Area | Apps | Effort | Conf. | Recommendation |
|---|---|---|---|---|---|---|---|
| 1 | Template ships no CLAUDE.md; both apps independently wrote one carrying agent-behaviour rules that AGENTS.md structurally does not hold | missing-practice | Agentification | hume, iris | small | high | Ship a CLAUDE.md under ~1,000 words with: (1) an authoritative-references table (AGENTS.md / TESTING.md / CONTEXT.md / .claude/rules / docs/adr) plus an explicit precedence rule; (2) a 'what this is' block marked `<!-- FORK: rewrite -->`; (3) a commands table including the single-test invocations and, critically, the bootstrap step (`cp .env.example .env` before any `pnpm check` — see the knip/dotenv finding); (4) APP2's four Working Rules from /home/user/iris-insights-crm/CLAUDE.md:65-71 near-verbatim; (5) a ~30-line architecture summary pointing at AGENTS.md. |
| 2 | `.oxfmtrc.json` and `.oxlintrc.json` do not ignore `.claude`/`.agents`, so installing agent tooling makes `pnpm check` unpassable; both apps patched it locally | defect | Agentification | hume, iris | trivial | high | Apply APP2's db9bc39 diff verbatim to the template: `.agents` and `.claude` in .oxfmtrc.json ignorePatterns; `.agents/**` and `.claude/**` in .oxlintrc.json ignorePatterns. Separately add `/.claude/settings.local.json` to .gitignore while keeping `.claude/settings.json` and `.claude/rules/**` tracked — mirroring the existing `/.vscode/*` + `!/.vscode/settings.example.json` pattern at .gitignore:66-73. Do NOT gitignore `.claude/skills/` as the investigator suggested; that contradicts the vendoring recommendation below (APP1 tracks `.agents/skills/**` and the `.claude/skills` symlink deliberately). |
| 3 | `pnpm check` is an eleven-tool fail-fast parallel run with network, python, and .env dependencies, and there is no fast inner-loop tier | defect | Agentification | hume | small | high | (1) Add `-c` to `check` so all eleven gates report instead of the first failure killing nine siblings. (2) Add `check:fast` = `run-p -c -n format:check lint typecheck depcruise check:test-layering` — local, no network, no python, no graphviz, no .env — and change AGENTS.md:30 to `pnpm format:changed && pnpm check:fast && pnpm test:affected`, reserving `pnpm check` for pre-handoff. (3) Follow APP1 and move `security:audit` out of `check` into `verify`/CI only. (4) Make `semgrep` and `architecture:graph:check` degrade with a clear skip notice when their external binary is missing, rather than failing. |
| 4 | `.claude/settings.json` is three lines of attribution — no permissions allowlist/denylist and no PostToolUse format hook, despite lefthook already doing exactly that for humans | agentification | Agentification | — | small | high | Extend .claude/settings.json with (1) a PostToolUse hook matching `Edit\|Write` running `pnpm exec oxfmt "$CLAUDE_FILE_PATHS" && pnpm exec oxlint --fix "$CLAUDE_FILE_PATHS"` for `*.{ts,tsx,js,jsx,mjs,cjs,json,css}`, mirroring lefthook.yml; (2) a `permissions.allow` list for the repo's read-only/safe commands (`Bash(pnpm typecheck)`, `Bash(pnpm lint*)`, `Bash(pnpm format*)`, `Bash(pnpm depcruise)`, `Bash(pnpm test:*)`, `Bash(pnpm architecture:graph*)`, `Bash(git diff:*)`, `Bash(git status)`, `Bash(git log:*)`, `Bash(rg:*)`); (3) a `permissions.deny` list encoding the three prose rules that currently rely on the agent remembering them — `Bash(pnpm db:push:*)`, `Bash(pnpm test:e2e:visual:update)`, and Write/Edit on `drizzle/migrations/**` (AGENTS.md:119). |
| 5 | No CONTEXT.md / ubiquitous-language convention in the template; both apps wrote one, both in the same skill-defined format | missing-practice | Agentification | hume, iris | small | high | Ship a root CONTEXT.md seeded with the template's genuinely cross-cutting terms only — Module, Capability, Public Gate, Composition Root, Port, Adapter, Tagged Outcome, AppError, Clock — each in `**Term**:` / one-sentence definition / `_Avoid_:` form under `## Platform Language`, followed by an empty `## Domain Language` heading marked for the fork to fill. Reference it from AGENTS.md ('name domain types with the terms in CONTEXT.md') and from the new CLAUDE.md's reference table. Ship .agents/skills/domain-modeling/CONTEXT-FORMAT.md alongside it so the format spec travels with the convention. |
| 6 | `pnpm check` cannot pass on a fresh clone: `knip:deps` requires a `.env` file that AGENTS.md never mentions creating | defect | Agentification | — | trivial | medium | Add a '## Setup' section to AGENTS.md as the first section after 'What This Codebase Is': `cp .env.example .env && pnpm install`, and note which gates need Docker (`pnpm dk:init`), network (`security:audit`), python (`semgrep`), and graphviz (`architecture:graph`). Independently, make `knip:deps` tolerate a missing `.env` — `dotenv -e .env.example -- knip` alone, or add dotenv-cli's no-fail flag — so a fresh clone can run `pnpm check`. Combined with adding `-c` to `check`, this turns an all-or-nothing gate into one that reports. |
| 7 | The security workflows assume a public repo or GitHub Advanced Security; a private fork must gut them — APP1 deleted dependency-review.yml and rewrote CodeQL and both OSV workflows | defect | CI/CD, supply chain, and release engineering | hume | medium | high | Make Security-tab upload opt-in rather than assumed: gate `security-events: write` and `upload:` in codeql.yml behind a repo variable, and ship APP1's `scripts/check-codeql-sarif-baseline.mjs` + `.github/codeql/codeql-baseline.json` in the template as the always-on gate (seeded with `requiredRules` for all four custom queries and an empty `acceptedFindings`). Replace the two OSV reusable-workflow calls with APP1's inlined `osv-scanner-action` + `osv-reporter-action` form, which needs only `contents: read`. Guard dependency-review.yml with `if: github.event.repository.visibility == 'public'`. Update `allowedWritePermissions` in tests/security/github-actions-supply-chain.unit.spec.ts:13-21 accordingly. |
| 8 | The workflow-policy guardrail test is entangled with template-specific facts, so a lean fork drops it wholesale — APP1 ships 11 workflows with no automated SHA-pinning enforcement | defect | CI/CD, supply chain, and release engineering | — | medium | high | Split the file: move the capability-agnostic assertions (SHA pinning, `pull_request_target` ban, concurrency group, `persist-credentials: false`, Docker digest pinning, bounded readiness loops, `sha256sum -c` beside `curl -o`, write-permission least-privilege) into `tests/security/workflow-policy.unit.spec.ts` with zero app imports, reading allowed write-permissions from a committed `.github/workflow-policy.json` so a fork edits data not test code. Move the `check:migrations` assertion into an architecture test guarded by `it.skipIf(!existsSync('drizzle/migrations'))`. Make `collectDockerComposeImages()` and the testcontainers import tolerate absence. Add one new assertion: every job declares `timeout-minutes` (do not add a top-level-permissions assertion — already satisfied everywhere). |
| 9 | `docs/security practices.md` in the template describes a different repository's CI; both apps inherited the false claims | doc-drift | CI/CD, supply chain, and release engineering | — | small | high | Rewrite against the actual twelve workflows. Delete the Octoscan / `ci.yml` / `actions-scanning.yml` / `db-migrate.yml` / Pushover / Biome / jscpd / Vercel-preview claims. Stop hardcoding rule counts, or add `scripts/check-security-doc-counts.mjs` to `pnpm check` that recomputes the Semgrep and dependency-cruiser counts and fails on disagreement — the same enforcement pattern already used by `check-risk-register.mjs`. |
| 10 | No fast-fail job graph and no path filters: ~20 job runs and 12 identical `pnpm install`s fire on every PR, including a 3-browser 30-minute E2E matrix, before format:check can fail | missing-practice | CI/CD, supply chain, and release engineering | — | medium | high | Add a cheap `gate` job to code-quality.yml running format:check + lint + typecheck on one install, then `needs: gate` on affected-tests, coverage, postgres-integration, build, and on the E2E/Visual jobs. Add `paths-ignore: ['**/*.md', 'docs/**', '.github/assets/**']` to e2e-tests.yml, codeql.yml and mutation-testing.yml. Add `merge_group:` to code-quality.yml, codeql.yml, semgrep.yml and detect-secrets.yml. Trim the e2e-tests.yml push branch list to `[main]`. Restrict the full browser matrix to `push: branches: [main]` and run chromium-only on PRs. |
| 11 | `pnpm check` shells out to a `semgrep` binary that the template never installs — a second, independent way a fresh clone fails the documented local gate | defect | CI/CD, supply chain, and release engineering | — | small | high | Either (a) add `.github/requirements/semgrep.txt` mirroring the detect-secrets pattern plus a `pnpm setup:tools` script and a README/AGENTS prerequisite line, or (b) drop `semgrep` from `check` and rely on the dedicated semgrep.yml workflow, documenting `pnpm semgrep` as an opt-in that requires the binary. Whichever is chosen, AGENTS.md:15 must stop listing semgrep as part of `pnpm check` without a prerequisite. |
| 12 | The template's E2E workflow is hard-wired to Postgres + MinIO provisioning, so a database-less fork deletes it outright — APP1 now runs zero E2E and zero visual tests in CI while still shipping the specs | defect | CI/CD, supply chain, and release engineering | — | medium | high | Extract the provisioning into a composite action (`.github/actions/setup-stack/action.yml`) with `postgres: true\|false` and `s3: true\|false` inputs, and have both E2E and Visual jobs call it — a fork with no database sets both to false and keeps its Playwright coverage. This also removes the ~90 lines currently duplicated between the E2E job (:85-155) and the Visual job (:223-286). Add a note in AGENTS.md that forks should adjust the inputs rather than delete the workflow. |
| 13 | The template's `pnpm-workspace.yaml` overrides are behind BOTH apps' security floors — the expired risk-register rows are advisories both apps already overrode away | missing-practice | CI/CD, supply chain, and release engineering | — | small | high | Adopt the union of the two apps' security floors into the template's pnpm-workspace.yaml (as caret floors per the APP2 convention), regenerate the lockfile, then delete the now-resolved rows from docs/security-risk-register.md and move them into the existing 'Resolved Previous Accepted Advisories' section (which already lists three such rows at docs/security-risk-register.md:37-45). Only the `@skidding/launch-editor` and `nitro` pre-release rows should need a new review date. |
| 14 | .claude/rules/testing.md is a dead Prisma rule with a glob that matches zero files — and it is agent-facing | doc-drift | Documentation, onboarding, and doc-drift | neither | trivial | high | Replace the file's body with the template's real test-layer rules (mirror TESTING.md's map: cheapest layer first, PGlite adapter tests, `check:test-layering` for `*.workflow.integration.test.ts`) and fix the glob to `tests/**/*.{unit,browser,integration}.{test,spec}.{ts,tsx}`. Remove `Prisma.prisma` from .vscode/extensions.json in the same change. Agent-facing rule files need the same doc-accuracy discipline as AGENTS.md. |
| 15 | README.md carries upstream BearStudio claims that are false for this fork, including a getting-started command that scaffolds a different project | doc-drift | Documentation, onboarding, and doc-drift | neither | medium | high | Rewrite README.md as a fork-first document: (1) replace :1-30 with accurate identity, a `Use this template`/`degit` instruction, and a rename checklist; (2) delete the oRPC / React Hook Form / OpenAPI / husky content; (3) replace the upstream docs link with a `## Documentation` index table pointing at AGENTS.md, TESTING.md, CONTEXT.md, docs/strict-modular-monolith.md, docs/architecture/module-layer-dependencies.md, docs/security-risk-register.md, docs/adr/; (4) add the four sections a 2026 starter README needs and this one lacks — *fork & rename*, *add a capability module*, *remove the demo domain*, *pull template updates*; (5) name `pnpm db:migrate` in the deploy prerequisites. Keep :203-349 (deploy) — I spot-checked :205-224 and it is accurate. |
| 16 | The template has no documentation entry point; docs/strict-modular-monolith.md and docs/architecture/ are referenced by nothing, and both apps independently invented CLAUDE.md with an authoritative-references table | missing-practice | Documentation, onboarding, and doc-drift | both | small | high | Ship a template `CLAUDE.md` with (a) an authoritative-references table linking every file under docs/, (b) iris's "fix the doc in the same change" working rule verbatim, (c) a placeholder "What this app is" paragraph forks replace. Both apps converged on this shape independently — the single highest-signal missing artifact in this area. |
| 17 | No fork-and-rename story: 128 hardcoded brand occurrences across 45 files, including a CodeQL query-pack directory, shipped source defaults, LICENSE and CODE_OF_CONDUCT contacts | defect | Documentation, onboarding, and doc-drift | iris | medium | high | Add `scripts/rename-app.mjs` (args: kebab name + display name) rewriting package.json `name`, the CodeQL pack directory + qlpack/qls ids, public/site.webmanifest, .env.example defaults, workflow references, LICENSE copyright holder and CODE_OF_CONDUCT contact. Then funnel remaining brand strings through one exported constant (e.g. `APP_NAME` in src/platform/env/config.ts) so get-page-title.ts, the email footer and the OTel service-name default read from one place, and add a `pnpm check` guard failing on any surviving literal `start-ui` outside that constant. |
| 18 | No "remove the demo domain" guide; iris's removal cost 190 files / 9,695 deletions, and hume still carries orphaned docs pointing at deleted modules | defect | Documentation, onboarding, and doc-drift | iris | medium | high | Add `docs/removing-the-demo-domain.md` with iris's exact checklist (module dirs, composition file, drizzle schema + generated drop migration + seed, stryker config + tsconfig pair, package.json mutation scripts, the depcruise/sheriff/semgrep module enumerations, all four locale namespace JSONs, regenerate the architecture graph, delete the domain-coupled doc). Better: eliminate the checklist — derive module lists from the filesystem in sheriff.config.ts / .dependency-cruiser.cjs instead of hardcoding `(?:account\|book\|genre\|user)`, and move demo-domain docs under `docs/demo/` so they delete as a unit. Pair with `docs/adding-a-module.md` covering the same registration surface forward. |
| 19 | Three app-authored docs are generic template material the template should own, one of which documents an unfixed template security defect | missing-practice | Documentation, onboarding, and doc-drift | both | medium | high | Upstream three. (1) Fix the Better Auth `advanced.ipAddress` gap in src/modules/auth/infrastructure/better-auth/auth.tsx and ship a trimmed `docs/security-rate-limiting.md` describing the four limiters and their fail-open/fail-closed behaviour — a live security finding a fork made on the template's behalf. (2) Adopt `docs/toolchain-decisions.md` from hume's oxc doc (drop the Hume-specific paragraph). (3) Ship `docs/production-observability.md` as a skeleton runbook with Hume/Vercel specifics replaced by placeholders. The React-compiler evaluation belongs as a template ADR with its Revisit triggers. |
| 20 | docs/strict-modular-monolith.md documents a `tenant` router-context slot that does not exist — and iris propagated the falsehood into its own CLAUDE.md as an instruction to routes | doc-drift | Documentation, onboarding, and doc-drift | neither | trivial | high | Delete the `tenant` bullet (or add the field to RouterContext if it is genuinely planned), document `getSessionSnapshot`, and add a type-level pin: a unit test asserting `keyof RouterContext` equals the documented set, so the doc and the type cannot diverge silently. This is the second independent falsehood in the same 119-line file, which argues for rewriting it rather than patching it. |
| 21 | docs/strict-modular-monolith.md's Module Contract omits the `backend.ts` and `testing.ts` public gates that .dependency-cruiser.cjs actually enforces — the template's two architecture docs contradict each other | doc-drift | Documentation, onboarding, and doc-drift | neither | trivial | high | Regenerate the Module Contract section from the enforced list in .dependency-cruiser.cjs:134 (or better, derive both from one shared constant), add `src/app/` to the top-level tree, and add a docs-lint assertion that the gate names in docs match the depcruise rule. Three independent falsehoods in one 119-line file (CSRF, tenant, gates) means it should be rewritten from the code, not edited. |
| 22 | Stryker's `shared` scope mutates `src/platform/lib/dayjs/**`, deleted in commit 2fe0a4e; the mutate allowlist has drifted and both apps inherited it byte-identically | defect | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Replace `src/platform/lib/dayjs/**/*.ts` with `src/platform/lib/temporal/**/*.ts` in stryker.shared-only.config.mjs:7 and tsconfig.stryker.shared-only.json:5, and reconsider the allowlist as a denylist (`src/platform/**/*.ts` minus known-untestable subtrees) so it cannot silently under-cover. Add a guard inside `pnpm check`: assert every non-negated glob prefix in every `stryker.*.config.mjs` `mutationSourceFiles` resolves to at least one file on disk. Fix docs/strict-modular-monolith.md:36 in the same change. |
| 23 | Every committed visual baseline is `-chromium-darwin.png`, yet the template ships a `Visual (chromium)` CI job on ubuntu-latest that runs the full visual suite | defect | Testing strategy, test infrastructure, and quality gates | hume | small | high | Pick one and commit. Preferred: delete the `Visual` job from .github/workflows/e2e-tests.yml, adopt APP1's `&& !process.env.CI` guard at playwright.config.ts:5, and state in TESTING.md that visual baselines are a local developer-machine gate reviewed via `pnpm test:visual:update`. If CI visual is genuinely wanted, generate Linux baselines inside the CI container, commit them, and pin `resolveScreenshotPath` to a fixed platform string so mac and Linux compare the same files. |
| 24 | `tests/e2e/visual/authorization.visual.spec.ts` ships with no committed baseline at all — the template's visual suite is already broken at HEAD, even on macOS | defect | Testing strategy, test infrastructure, and quality gates | hume | trivial | high | Either commit the `access-denied-page-chromium-darwin.png` baseline for authorization.visual.spec.ts, or delete the spec along with the other demo-screen visual specs. Then add a cheap guard (a unit test under tests/unit/scripts/) asserting that every `screenshot(page, '<name>')` call in tests/e2e/visual has a corresponding committed file in the sibling `-snapshots` directory, so a spec can never be merged without its artifact again. |
| 25 | Nine layers, 23 top-level test config files, 17 of them Stryker for demo modules — both apps deleted most and neither ever added a scope for the module it actually built | dead-weight | Testing strategy, test infrastructure, and quality gates | both | medium | high | Collapse the per-module Stryker configs into one `stryker.config.mjs` reading the scope from `STRYKER_SCOPE`, with mutate/testFiles/tsconfig looked up in a single `stryker.scopes.mjs` map and one `tsconfig.stryker.json`. That turns 17 files + 40 scripts into 2 files + ~6 scripts and makes adding a scope one map entry. Ship exactly one worked scope (kernel or shared). Keep the layer vocabulary in TESTING.md but mark which layers are on by default and which are opt-in. |
| 26 | The default E2E path needs Docker, Postgres, MinIO, Maildev, a PGlite socket server and a hand-generated AUTH_SECRET, and no doc tells a local developer to install Playwright browsers; APP1 replaced the whole chain with `vite dev` | defect | Testing strategy, test infrastructure, and quality gates | hume | medium | high | Make the default E2E path dependency-free: ship a smoke spec that runs against `vite dev` (APP1's e2e:webserver), and gate the Maildev/MinIO/Postgres-dependent specs behind a separate `test:e2e:full` script plus a `test.skip(!process.env.E2E_FULL_STACK)` guard. Add `pnpm exec playwright install --with-deps` to README.md's E2E section and to TESTING.md. Reduce the default `projects` to chromium with firefox/webkit commented as opt-in, which also lets you delete console-error-guard.ts:22-49. |
| 27 | `tests/e2e/visual/authorization.visual.spec.ts` ships with no committed baseline — the template's own `pnpm test:e2e:visual` cannot pass on a clean checkout on ANY operating system | defect | Testing strategy, test infrastructure, and quality gates | hume | trivial | high | Either commit the `access-denied-page-chromium-darwin.png` baseline or delete authorization.visual.spec.ts along with the other demo-screen visual specs. Add a unit test under tests/unit/scripts/ asserting every `screenshot(page, '<name>')` call under tests/e2e/visual has a matching committed file in its sibling `-snapshots` directory, so a spec can never merge without its artifact. |
| 28 | `runtimeEnv()` lets build-time `import.meta.env` override runtime `process.env`, inverting its own contract; `isTruthy` rejects the `1` platforms actually write | defect | app-inventions | iris | small | high | Port APP2's `buildTimeEnv`/`processEnv`/`runtimeEnv` split, `TRUTHY_ENV_VALUES`, and `getEnvSourceDrift()` into the template kernel, and mirror the first two into `src/platform/env/config.ts:11-16` with a comment naming the kernel as canonical. Add an architecture/unit test that fails if the two copies' spread order or truthy set diverge — the layering rule forces the duplication, so it needs a guardrail rather than discipline. |
| 29 | A throwing telemetry adapter propagates into application code; the template states the invariant twice but enforces it nowhere | defect | app-inventions | iris | small | high | Port `src/platform/telemetry/report-failure.ts`, the per-adapter `fanOut` isolation in `src/composition/telemetry/adapter-chain.ts`, and the `write`/`writeOrThrow` split in `src/modules/kernel/infrastructure/logger/telemetry.ts`. Ship the two unit specs APP2 wrote so the invariant the template already asserts in prose is actually enforced. |
| 30 | No console telemetry fallback: with no OTLP collector and no Sentry DSN the template drops metrics, spans and captured exceptions into a no-op adapter — both apps built a replacement | missing-practice | app-inventions | both | small | high | Add APP2's `src/platform/telemetry/console.ts` and select it in `src/composition/telemetry/` when no collector URL and no Sentry DSN are configured, keeping the no-op for tests only — APP2's `sentry.server.ts:47` shows the exact selection including `emitLogs: !getLoggerConfig().consoleMirror` to avoid double-printing. Consider flipping `consoleMirror`'s production default, or at least documenting `LOGGER_CONSOLE_MIRROR` in `.env.example` as the no-backend escape hatch. Optionally lift APP1's `console-writer.ts` so the mirror and the fallback share one formatter. Document the `globalThis.console` bracket-access idiom in `.claude/rules/` next to the Semgrep rule it exists to satisfy. |
| 31 | Toolchain pinning and self-healing (`bin/run`, `mise.toml`, `.devcontainer`, `pnpm doctor:node`) — APP1 built a complete kit the template has no part of | missing-practice | app-inventions | hume | small | high | Add `bin/run`, `mise.toml`, `.devcontainer/{devcontainer.json,devcontainer-lock.json}`, `scripts/check-node-version.mjs` and its two `scripts/lib/` helpers; add `doctor:node` and wire `check:node-version` into both `check` and `check:ci`; prefix `lefthook.yml` commands with `./bin/run `. Generalize the devcontainer name/port label. |
| 32 | Approved-email-domain signup restriction: the template's only signup control is the all-or-nothing `VITE_AUTH_SIGNUP_ENABLED`, so an enabled deployment is an OTP mail relay | missing-practice | app-inventions | iris | small | high | Port `auth/domain/email-domain-policy.ts` and `auth/infrastructure/better-auth/email-domain-guard.ts`, the `AUTH_ALLOWED_EMAIL_DOMAINS` kernel config, and the `EMAIL_DOMAIN_NOT_ALLOWED` i18n keys. Default to empty (unrestricted) in the template rather than APP2's own domain, but state prominently in `.env.example` and README that empty + signup-enabled makes the deployment an open OTP mail relay. |
| 33 | The `rate_limit` AppError category has no HTTP representation — the template maps 429 to 400, and `TOO_MANY_REQUESTS` does not exist in `ServerFnErrorCode` | defect | app-inventions | hume | trivial | high | Add `'TOO_MANY_REQUESTS'` to `SERVER_FN_ERROR_CODES`, `TOO_MANY_REQUESTS: 429` to `STATUS_BY_CODE`, and `TOO_MANY_REQUESTS: 'rate_limit'` to `CATEGORY_BY_CODE` in `src/modules/kernel/transport/tanstack/server-fn-error.ts`, then change `result-mapper.ts:25` to `rate_limit: 'TOO_MANY_REQUESTS'`. Add a unit case asserting an `AppError` with category `rate_limit` maps to status 429 and round-trips. |
| 34 | dependency-cruiser, sheriff, and check-migration-edits hardcode the template's demo module names, leaving dead rules and one genuine fail-open hole in every fork | defect | config-divergence | iris | trivial | high | Replace every enumerated module list with the generic `[^/]+` / `$1` back-reference form already used by the neighbouring rules in the same file (`.dependency-cruiser.cjs:102`, `:132`, `:166` are working examples). Fix `sheriff.config.ts:44-48` first — that is the only one that is actually fail-open today. Replace `SCHEMA_PATHS` in `scripts/check-migration-edits.mjs` with a runtime glob (`src/modules/*/infrastructure/drizzle/schema.ts` plus the kernel paths). Delete `pathNot: '^src/modules/book/server\.ts$'` (.dependency-cruiser.cjs:209). Then add a meta-test under `tests/architecture/` that fails when any depcruise `from.path` or sheriff matcher regex matches zero files under `src/` — that converts the whole class from silent-pass to loud-fail. |
| 35 | Both apps independently rewrote pnpm-workspace security overrides the template is missing; APP2 also wrote down the floors-vs-pins policy | missing-practice | config-divergence | both | small | high | Bring `overrides:` up to the union of the two apps' entries using APP2's caret-floor form, and copy APP2's floors-vs-pins block comment and APP1's fast-uri unscoped-selector comment into the file as the standing policy. Add a scheduled workflow that runs `pnpm audit --audit-level=high` against the template weekly so the block cannot rot silently. Rewrite `.semgrep.yml:257` so it does not need hand-editing per advisory, or delete it — as written it is dead weight that gives a false sense of coverage. |
| 36 | `scripts/format-changed.mjs` keeps an extension allowlist that diverges from `.oxfmtrc.json`, and it contradicts that config on `.md` | defect | config-divergence | iris | trivial | high | Adopt APP2's `scripts/format-changed.mjs` verbatim, comment included: delete `FORMAT_EXTENSIONS`/`hasFormatterExtension`, drop the now-unused `node:path` import, and pass `--no-error-on-unmatched-pattern` as oxfmt's first argument. Adopt APP2's lefthook glob. Add a guardrail (test or semgrep rule) asserting `format-changed.mjs` contains no extension allowlist, next to the existing `lefthook-format-changed-stage-fixed` rule. |
| 37 | The generated architecture dependency graph omits every type-only import, and its own 'type-only' legend was unreachable | defect | config-divergence | iris | trivial | high | Apply APP2's change — `tsPreCompilationDeps: true` inside the `cruise()` call in `scripts/generate-module-dependency-graph.ts`, NOT in `.dependency-cruiser.cjs` — and keep the full comment including the `no-circular` caveat. Regenerate the committed graph in the same change so `architecture:graph:check` stays green. Then decide separately whether to break the type-only cycle that blocks repo-wide `tsPreCompilationDeps`; that is where the real lint-enforcement value sits. |
| 38 | Template dependency versions lag APP1 across exactly 54 packages; APP2 forked later and bumped only the four Vitest packages, so the template's freeze date becomes every fork's floor | modernization | config-divergence | hume | medium | high | Do one bump pass on the template to at least APP1's levels, treating the 15 OpenTelemetry packages, the 4 Vitest packages, and the TanStack cluster as atomic groups; review `dependency-cruiser` 17→18, `@foresightjs/react` 0.3→1.0, and `jscpd` 4→5 as majors. Do NOT rewrite dependabot.yml's groups — they are already correct. DO add a `cooldown` to dependabot.yml matching `minimumReleaseAge: 1440`. Add a scheduled workflow that opens one grouped bump PR weekly against the template so its freeze date never drifts more than a week from HEAD, and tell forks in README.md to run a bump pass immediately after forking. |
| 39 | Neither the template's deployment-environment model nor its app-URL resolution survives a preview-deployment platform; both apps invented replacements, and they disagree with each other | missing-practice | config-divergence | both | medium | high | Add a `DeploymentEnvironment` (`development \| preview \| production`) resolved from `VERCEL_TARGET_ENV` → `VERCEL_ENV` with a documented, platform-pluggable fallback and a fail-closed `production` default, and make `isProdRuntimeEnvironment` consult it — that also removes the dead `VERCEL_ENV` schema field. Port APP2's `src/modules/kernel/infrastructure/config/app-url.ts` and its unit spec, including the gating rules (preview → `VERCEL_BRANCH_URL`; production → `VERCEL_PROJECT_PRODUCTION_URL`; never `VERCEL_URL`; no localhost fallback in production). Retire client-visible `VITE_BASE_URL` for server-only `APP_URL` plus `window.location.origin` on the client, and update playwright.config.ts and the CI env blocks. Separately adopt APP1's vite.config.ts wiring of `VERCEL_ENV`/`VERCEL_GIT_COMMIT_SHA` into the Sentry `release` and the OTel defines. Do NOT port APP1's app-url.ts resolution order. |
| 40 | `pnpm lint:sheriff` runs in no aggregate and no CI workflow — Sheriff is inert in the template by default | defect | config-divergence | hume | trivial | high | Add `lint:sheriff` to `check` and `check:ci` as APP1 did, and add a CI step for it in code-quality.yml's static-analysis job (it needs no Postgres or Playwright, so it is cheap). Fix `sheriff.config.ts:44-48` in the same change — turning the tool on while the matcher is fail-open would give a green check that still enforces nothing. If Sheriff is genuinely redundant with dependency-cruiser, delete it and its config instead; carrying 140 lines of never-executed rules is worse than either alternative. |
| 41 | The template's ESLint security and SonarJS rules are warn-only and `lint:eslint` has no `--max-warnings`, so neither can ever fail `check` or CI | defect | config-divergence | — | trivial | high | Decide explicitly: either promote the rules that matter to `error` and keep the rest at `warn` with `--max-warnings=0` plus an inline-suppression policy, or add `--max-warnings=0` to `lint:eslint` and fix the resulting backlog. Whichever is chosen, document it — the current state reads as enforcement and is not. Apply the same decision to APP1's `lint:typed-sonar` when porting that migration. |
| 42 | Two divergent `isProd` implementations that disagree on unrecognised NODE_ENV values, one gating HTTPS enforcement and the other gating DB TLS | defect | config-divergence | iris | small | high | Port APP2's mirrored `isProd()` from src/platform/env/config.ts:36-52 including the comment explaining why duplication is required. Add a unit test that drives both `isProdRuntimeEnvironment` and the platform `isProd` over the same table of NODE_ENV/PROD/VERCEL_* inputs and asserts identical results — that is the only thing that keeps two deliberately-duplicated functions honest. Fold this into the deployment-environment work in the verified Vercel finding rather than doing it twice. |
| 43 | Adding one trivial capability costs ~27 files and ~1,090 LOC, with no scaffolding generator anywhere in the repo | missing-practice | dx-ergonomics | neither | medium | high | Ship `pnpm gen:module <name>` (plain Node script under scripts/, no new dependency) that emits the module dir + gate files + composition factory + placeholder tests + the Stryker pair + package.json entries, and appends the module name to every shared registration list. Simultaneously reduce the plumbing: the module rules already say 'only add gates the module needs', yet genre ships all six for one read-only query — either generate the trivial gates from convention or stop requiring them. |
| 44 | Architecture guardrails hardcode demo module names; both apps now ship rules that silently match nothing | defect | dx-ergonomics | neither | medium | high | Replace every hardcoded alternation with a derived pattern. For .dependency-cruiser.cjs:60-61/78-79/162-163 use the backreference form already present at :147-150. For sheriff.config.ts:41-50, switch `infrastructureTarget` to the same `(?!kernel\/)[^/]+` shape its two sibling matchers already use — the account\|book\|genre\|user list is not an allowlist of protected modules, it is an exception list that lets new modules through. For tests/architecture/modular-monolith.unit.spec.ts, replace the literal arrays with `readdirSync('src/modules')`. For .semgrep.yml, generate the brand alternation from src/modules/kernel/domain/ids.ts at check time. Add one architecture test asserting no guardrail file names a module that is not a directory under src/modules — that single test catches all three app-side regressions. |
| 45 | `pnpm check` cannot run on a clean machine: it needs the semgrep binary (Python) and network access, neither documented in README Requirements | defect | dx-ergonomics | hume | small | high | Tier the gates explicitly and honestly. `pnpm check` (inner loop, pure Node, offline): format:check, lint, typecheck, depcruise, check:test-layering, check:migrations, knip:deps. `pnpm check:full` (pre-push): adds architecture:graph:check and lint:sheriff. CI-only: semgrep (Docker), security:audit (network), detect-secrets, CodeQL. Have each optional-binary gate detect its tool and skip with 'skipped: semgrep not installed, runs in CI' rather than ENOENT. Add semgrep/Python and Graphviz to README.md's Requirements section at :17-21. |
| 46 | `docs/strict-modular-monolith.md` states `src/start.ts` does not exist; it does, and it is the CSRF registration point — both apps independently corrected it | doc-drift | dx-ergonomics | both | trivial | high | Replace docs/strict-modular-monolith.md:110-119 with iris's text. Correct AGENTS.md:15 to name all eleven check tasks. Adopt iris's 'fix the doc in the same change' rule into AGENTS.md. Add a cheap architecture-test guard asserting docs/ contains no sentence matching /does not (define\|exist).*start\.ts/ while src/start.ts is present. |
| 47 | The template ships a dead — and almost certainly broken — React Cosmos catalog: 63 fixture files, 4,907 LOC, 2 devDeps, a GitHub Pages workflow, and glob carve-outs in five config files. Iris deleted the whole thing in one commit; hume-demo still publishes it to Pages. | dead-weight | dx-ergonomics | iris | small | high | Delete React Cosmos from the template, following iris 5082abf as the reference diff: tools/cosmos/, vite.cosmos.ts, cosmos.config.json, the decorator and renderer shell, cosmos.css, .github/workflows/cosmos-pages.yml, both devDependencies, all 63 *.fixture.tsx files, and the now-dead fixture globs in .oxlintrc.json, eslint.config.mjs, .semgrep.yml, package.json's jscpd block, and the CodeQL configs. If a component catalog is genuinely wanted, replace it with something wired into `pnpm check` so it cannot silently rot — but shipping 4,907 LOC of fixtures whose renderer points at a file the repo does not contain is strictly negative value, and it also drags a security-advisory dependency (@skidding/launch-editor) into every fork's tree. |
| 48 | Template throws at boot when OTEL_COLLECTOR_URL is unset in production, and its graceful path silently no-ops — BOTH apps independently deleted the throw and added a console-backed telemetry fallback | defect | hume-archaeology | both | medium | high | Delete the throw at src/modules/kernel/infrastructure/config/telemetry.ts:84-87 (keep the HTTPS-when-set guard immediately below it). Port APP2's src/platform/telemetry/console.ts `createConsoleTelemetry` — it is the better template shape because it needs no OTel SDK — export it from src/platform/telemetry/index.ts, and select it in composition when no OpenTelemetry adapter is present (not merely when no adapters at all are present). Remove line 362 from .github/workflows/code-quality.yml. Update the README's fail-closed production-config list to state observability is optional. |
| 49 | src/platform/env/config.ts duplicates 'is production' with the naive check the kernel explicitly documents as a hazard, and both copies spread import.meta.env last | defect | hume-archaeology | iris | small | high | (1) Invert the spread in BOTH src/platform/env/config.ts:11-14 and src/modules/kernel/infrastructure/config/env-schema.ts:9-12 so runtime `process.env` wins, with APP2's rationale comment. (2) Replace src/platform/env/config.ts:18-21 with the same normalize-then-allowlist logic as `isProdRuntimeEnvironment`, including APP2's VERCEL_TARGET_ENV/VERCEL_ENV precedence and the `'1'`-accepting `isTruthy`. (3) Add a unit test asserting the two definitions agree across {production, development, test, staging, preview, unset} × {PROD true/false}, or better an architecture test that fails when they diverge — the duplication is structurally forced by the platform-must-not-import-modules rule, so only a test can keep them in sync. |
| 50 | getBaseUrl() keys on VITE_VERCEL_ENV / VITE_VERCEL_BRANCH_URL, which nothing in the template ever sets — BOTH apps deleted it and wrote a real app-URL resolver | defect | hume-archaeology | both | small | high | Delete `getBaseUrl` from src/platform/env/config.ts:30-38. Adopt APP1's src/platform/env/app-url.ts shape (explicit VITE_BASE_URL wins, then VERCEL_PROJECT_PRODUCTION_URL, then VERCEL_URL, https-prefixed) and call the same helper from vite.config.ts so build and runtime agree. Take APP2's logged origin-resolution diagnostic for the operator-facing version. Document in .env.example and README that VITE_BASE_URL is optional on Vercel. |
| 51 | telemetryProxy is an unguarded pass-through, so an exporter throw reaches the security middleware that emitted the metric | defect | hume-archaeology | iris | small | high | Port APP2's src/platform/telemetry/runtime.ts and src/platform/telemetry/report-failure.ts verbatim, including both rationale comments, and export `reportTelemetryFailure`/`setTelemetryFailureSink` from src/platform/telemetry/index.ts. Add unit tests: a throwing adapter must not propagate out of emitLog/recordMetric/captureException/setUser, and MUST propagate out of startSpan. Once the proxy is guarded, the ad-hoc try/catch at request-observability.ts:98-104 can be removed. |
| 52 | No OTel flush on shutdown and no forceFlush on the TelemetryAdapter contract — buffered spans, logs and metrics are lost on every container stop and serverless freeze | missing-practice | hume-archaeology | hume | small | high | Add `forceFlush?(): Promise<void>` to `TelemetryAdapter` in src/platform/telemetry/types.ts, fan it out in src/composition/telemetry/adapter-chain.ts, thread a `flushAll` over tracer/meter/logger providers into `createOpenTelemetryAdapter`, and install APP1's `installTelemetryShutdownFlush` (beforeExit + SIGTERM/SIGINT, bounded race, idempotent, unref'ed timer) from src/composition/telemetry/otel.server.ts. Document in AGENTS.md that server functions on serverless targets may call `telemetry.forceFlush?.()` at the response boundary. |
| 53 | No policy or shared helper for rate-limiting a request with no trustworthy client IP — the template's own two call sites disagree, one fails fully open, and the doc they point at does not exist | defect | hume-archaeology | hume | medium | high | Add a composed helper to src/platform/http — e.g. `enforceRateLimit({ request, scope, trustedProxyDepth, limit, unattributedLimit, windowMs, logger })` — implementing APP1's policy: strict per-IP bucket when the IP is trustworthy, a separate coarse `<scope>:unattributed` bucket at a much higher ceiling otherwise, plus a structured warn on the unattributed path. Migrate both template call sites onto it. Port APP1's semgrep rule banning `getClientIp(...) ?? 'unknown'` (generalise the `paths.include`). Either write docs/security-rate-limiting.md or remove the reference at transport.ts:139. |
| 54 | Kernel transport has no 429: AppError category `rate_limit` is mapped to BAD_REQUEST (HTTP 400), and `ServerFnErrorCode` has no TOO_MANY_REQUESTS member at all | defect | hume-archaeology | hume | trivial | high | Add `'TOO_MANY_REQUESTS'` to SERVER_FN_ERROR_CODES with `TOO_MANY_REQUESTS: 429` in STATUS_BY_CODE and `TOO_MANY_REQUESTS: 'rate_limit'` in CATEGORY_BY_CODE, and change result-mapper.ts:25 to `rate_limit: 'TOO_MANY_REQUESTS'`. Pair it with the rate-limit helper recommendation so the helper can set Retry-After and throw the right code. |
| 55 | mapAppErrorToServerFnError rethrows non-AppError exceptions raw, defeating the message-redaction contract stated three lines above it | defect | hume-archaeology | hume | trivial | medium | Replace `throw error;` at src/modules/kernel/transport/tanstack/result-mapper.ts:66 with a generic `ServerFnError('INTERNAL_SERVER_ERROR', { message: 'Internal server error' })`, and log the original error (with cause chain) before flattening — see the next finding. Add a unit test asserting that a thrown `new Error('SELECT * FROM users WHERE token = …')` does not reach the client message. |
| 56 | Application errors are flattened at the server-function boundary with no server-side log — a repository or gateway failure reaches the client as a 500 with no record of what broke | defect | hume-archaeology | iris | small | high | Port APP2's `reportApplicationError` and the `ResultMapperLogger` parameter into src/modules/kernel/transport/tanstack/result-mapper.ts, including the internal-vs-client severity split (error+exception vs warn) and a `describeErrorChain` helper. Note this is a breaking signature change to `unwrapApplicationResult`, so it needs a codemod or an optional-logger overload for existing forks. |
| 57 | Telemetry proxy forwards to the collector and Sentry with no timeout and no abort signal, and no try/catch — an unreachable collector hangs the request handler and a network error escapes the HTTP handler | defect | hume-archaeology | hume | small | high | Add `TELEMETRY_FORWARD_TIMEOUT_MS` (default 10_000) to the telemetry config schema, thread `request.signal` into `forwardToCollector` and `forwardSentryEnvelope`, and wrap both fetches in try/catch with `AbortSignal.any([requestSignal, AbortSignal.timeout(forwardTimeoutMs)])`, returning 502 and recording a `timeout` vs `transport_error` reason — i.e. port APP1's a711431 change to transport.ts verbatim. |
| 58 | `pnpm env:client` validates nothing — it runs a file that only re-exports a lazy Proxy | defect | iris-archaeology | iris | trivial | high | Add scripts/validate-client-config.ts (copy Iris's 5 lines verbatim including the comment) and repoint `env:client`. Trivial, and it is the reason the missing-VITE_BASE_URL outage above passed `pnpm build` clean. |
| 59 | PGlite teardown discards the test suite's exit code, so `pnpm test` and `pnpm verify` can exit 0 with failing tests | defect | iris-archaeology | iris | trivial | medium | Capture `process.exitCode` before `pglite.close()` and restore it after, in tests/server/pglite-global-setup.ts. Three lines, zero risk. Add a CI assertion that a deliberately failing test run exits non-zero, so the gate cannot be silently disarmed again. |
| 60 | A throwing telemetry exporter changes application behaviour — it replaces real errors and can suppress a security middleware's 403 | defect | iris-archaeology | iris | medium | high | Port Iris's src/platform/telemetry/report-failure.ts plus the guards at the three choke points: telemetryProxy's void methods, the adapter-chain fan-out, and createTelemetryLogger's write. Keep Iris's discrimination exactly — startSpan / startManualSpan / currentCorrelation stay UNGUARDED because they return values callers depend on. Also port the four regression specs, since the invariant is otherwise unenforceable and the template already asserts it only in comments. |
| 61 | Template requires `OTEL_COLLECTOR_URL` in production — both apps independently deleted the requirement, and both added a console fallback | defect | iris-archaeology | both | medium | high | Delete the throw at telemetry.ts:84-87 (keep the HTTPS-when-set guard at :89+), remove the CI workaround at code-quality.yml:362, and add a console-backed telemetry adapter that takes over when no OTel adapter is configured. Key on 'no OpenTelemetry adapter' rather than 'no adapters at all' so a Sentry-DSN-only deploy still gets logs and metrics, and gate log records on the logger's `consoleMirror` setting so exactly one writer prints. Two independent forks deleted this throw within three weeks; it is not a policy the template can defend. |
| 62 | Template forces `sslmode=verify-ca\|verify-full` on every non-Neon production DATABASE_URL, which no mainstream managed Postgres provider hands out | defect | iris-archaeology | iris | small | high | Do not simply relax it — make it a policy knob. Keep the cleartext rejections (http://, ws://, sslmode=disable) unconditional, and gate the authenticated-sslmode requirement behind an env var (e.g. `DATABASE_REQUIRE_VERIFIED_TLS`, defaulting false with a loud comment) so the starter deploys on Neon/Supabase/Fly/Railway out of the box while a hardened deployment opts in. Separately fix tests/security/config-boot-hardening.unit.spec.ts to use a genuinely cleartext URL (`sslmode=disable`) so it keeps testing the DB guard — that latent test defect is in the template today. |
| 63 | Object storage is mandatory: `validateServerConfig()` requires S3 credentials for a feature only the demo books domain uses | dead-weight | iris-archaeology | iris | medium | high | Make object storage optional: `getStorageConfig()` should return undefined and `validateServerConfig` skip it when no S3 vars are set, failing only if an upload route is actually mounted. Better, extract the whole upload stack behind one feature toggle or a removable `object-storage` capability. Whatever the shape, the invariant to fix is: a starter must boot with zero third-party credentials configured. Also drop `BetterUploadObjectStorage` from kernel/backend.ts — a removable feature should never be re-exported from the kernel's public gate. |
| 64 | The books/genre example domain is hardcoded into guardrail configs, test fixtures, the kernel public gate, and the DB schema — removing it cost 190 files and a drop migration | dead-weight | iris-archaeology | both | large | high | Two independent forks deleted this domain immediately, so the template must not make it load-bearing. (a) Make the guardrail regexes glob the modules directory (`^src/modules/[^/]+/domain`) rather than enumerating module names, with an explicit allowlist only where genuinely needed. (b) Never re-export demo-domain schema from kernel/backend.ts. (c) Stop using `book` as the generic fixture in platform/kernel/auth tests — use `user`, which every fork keeps. (d) Ship a documented `scripts/remove-example-domain` (or make the example a separate branch/overlay), and ship the initial migration WITHOUT the example tables so a fork does not author a drop migration on day one. |
| 65 | `src/start.ts` builds its logger on `createNoOpTelemetry()`, so every event the middleware chain logs — including the browser-mutation-guard rejection — has no sink in production | defect | iris-archaeology | iris | small | high | Swap `createNoOpTelemetry()` for `telemetryProxy` in src/start.ts, rename the memo from `browserMutationGuardLogger` to a general `startServerLogger` (the middleware chain grows other reporting needs), and add the `security.browser_mutation_rejected` counter. A security middleware whose rejections are unobservable in production is a guardrail you cannot audit. Note the ordering dependency: this only becomes safe once the telemetry guards from the throwing-exporter finding are in place, because it puts a live adapter on a security response path. |
| 66 | `unwrapApplicationResult` converts an `AppError` into a generic `ServerFnError` with no server-side record — internal failures reach the client as bare 500s with nothing logged | defect | iris-archaeology | iris | medium | high | Port both: make `unwrapApplicationResult` take a logger and report before flattening (internal/system/≥500 at `error` with `exception`, client categories at `warn` without), and port `describeErrorChain` into kernel/domain/errors + export it from kernel/index.ts. Also port `emitConfigDiagnostic` — it is 41 lines and it is the only way a boot-time config failure is diagnosable from a platform log stream, which is precisely the class of failure that caused Iris's first-deploy outage. |
| 67 | `isTruthy` accepts only `true`/`'true'`, silently ignoring the `VERCEL=1` / `CI=1` / `PROD=1` form platforms actually emit | defect | iris-archaeology | iris | trivial | high | One-line fix in both copies: accept '1' and trim/lowercase before comparing. Fix it TOGETHER with the runtimeEnv() spread-order defect — this bug is latent until that one is fixed, because the baked `import.meta.env.PROD === true` currently masks it. Add a unit test table over `{true, 'true', '1', ' 1 ', 'TRUE', '0', ''}`. |
| 68 | `src/modules/kernel/backend.ts` is a 20-line barrel that couples the kernel's public gate to three separately-removable subsystems and drags `pg` into any consumer's graph | defect | iris-archaeology | iris | medium | high | Split kernel's server gate by cost of import, not by 'server-ness': a pure `kernel/config.ts` (or keep backend.ts pure) exporting only config accessors, error mapping and the logger — safe from anywhere — and a separate `kernel/db.ts` gate for `getDefaultDbClient` / `createTransactionRunner` / schema, which anything in the browser graph must not import. Never re-export a demo domain's schema or a removable feature's adapter from any gate. This is the prerequisite for the example-domain and object-storage removals recommended elsewhere: without it, every fork's first deletion touches the kernel's public API. |
| 69 | `pnpm env:client` validates nothing — it runs a file that only re-exports a lazy Proxy, and it already runs inside `pnpm build` | defect | platform-runtime | iris | trivial | high | Add `scripts/validate-client-config.ts` containing APP2's three lines, export `getEnvClient` from `src/platform/env/client.ts`, and repoint `package.json:41`. No build-script change needed. |
| 70 | Better Auth browser client is constructed at module scope, turning one missing env var into a boot-time total outage | defect | platform-runtime | iris | small | high | Memoize construction in a `getBetterAuthClient()` called by each facade method; pass `baseURL: undefined` on the server branch instead of reading `envClient`; expose `authErrorCodes` through a getter or static constant. Add a Semgrep/architecture rule forbidding top-level SDK-client construction in `presentation/`. |
| 71 | Telemetry silently drops everything when no collector is configured, while simultaneously refusing to boot production without one — BOTH apps built a console fallback | defect | platform-runtime | both | medium | high | Delete the throw at telemetry.ts:83-87 (keep the HTTPS-when-set guard at :88-97). Port APP2's `src/platform/telemetry/console.ts` as the default adapter and select it in `sentry.server.ts`/`sentry.client.ts` whenever the adapter list contains no OpenTelemetry adapter (not merely when it is empty). Gate `emitLog` on `!getLoggerConfig().consoleMirror`. |
| 72 | A throwing telemetry sink propagates into application code: `createTelemetryLogger.write()`, `telemetryProxy`, and the adapter chain are all unguarded | defect | platform-runtime | iris | small | high | Wrap the entire body of `write()` in try/catch; guard only the void report-only methods (`captureException`, `emitLog`, `recordMetric`, `setUser`) on `telemetryProxy` and in `createTelemetryAdapterChain`, leaving `startSpan`/`startManualSpan`/`currentCorrelation` unguarded with a comment saying why. Isolate per-adapter failures in the chain. Add `src/platform/telemetry/report-failure.ts`. |
| 73 | Rate-limit policy is incoherent: the Resend webhook fails fully open, the telemetry proxy collapses all unattributed traffic into one shared bucket at the strict per-IP limit | defect | platform-runtime | hume | small | high | Give `src/platform/http/rate-limiter.ts` a `checkAttributed(ip, { perIpLimit, unattributedLimit, windowMs })` that keys `ip:<addr>` at the strict limit and a single `unattributed` key at a coarser ceiling, with a diagnostic when the unattributed path is taken. Convert resend-webhook-handlers.ts:267 and transport.ts:143. Promote APP1's Semgrep rule to repo-wide scope. Document that a missing IP is expected behind a directly-reachable origin. |
| 74 | Production DB TLS policy demands `sslmode=verify-ca\|verify-full`, which rejects the connection strings Neon/Supabase/RDS actually issue | defect | platform-runtime | iris | small | high | Either (a) accept `sslmode=require` in production with a loud one-time `[config]` warning naming the residual MITM exposure, gating the strict rule behind an opt-in `DATABASE_REQUIRE_VERIFIED_TLS=true`; or (b) keep the strict rule and ship the missing half — a `DATABASE_SSL_CA` variable threaded into node-pg `ssl` options in `createDbClient`, plus per-provider README instructions. Keep rejecting `sslmode=disable` and cleartext schemes either way. |
| 75 | `metrics.getMeter()` is called at MODULE SCOPE in the OTel adapter, capturing the no-op meter before the SDK registers a provider — APP1 fixed exactly this | defect | platform-runtime | hume | trivial | medium | Move `metrics.getMeter(...)`, the counter/histogram Maps and their getters inside `createOpenTelemetryAdapter()`, as APP1 did. Move `trace.getTracer`/`logs.getLogger` too unless the API's proxy providers are confirmed to re-resolve. Add a test that registers a MeterProvider after module import and asserts a recorded counter reaches it. Also adopt APP1's optional `forceFlush` hook — serverless runtimes freeze between invocations and need an explicit flush. |
| 76 | `mapAppErrorToServerFnError` re-throws arbitrary non-`AppError` exceptions straight across the server-fn boundary — APP1 replaced it with a generic 500 | defect | platform-runtime | hume | trivial | high | Replace `throw error;` at result-mapper.ts:65 with APP1's `throw new ServerFnError('INTERNAL_SERVER_ERROR', { message: 'Internal server error' });`, logging/capturing the original first (see the next finding). Add a unit test asserting a plain `Error('secret detail')` does not reach the client. |
| 77 | Internal errors are flattened into a `ServerFnError` with NO server-side log or exception capture — APP2 added `reportApplicationError` to the kernel result mapper | missing-practice | platform-runtime | iris | small | high | Port APP2's `reportApplicationError` and `describeErrorChain` into the kernel result mapper: log internal categories at `error` with `exception` attached (so it reaches captureException) and client-error categories at `warn`, with the cause chain, before flattening. Thread a `ResultMapperLogger` in from composition rather than importing one. |
| 78 | Template ships AGENTS.md but no CLAUDE.md; both apps hand-wrote one and both drifted (in both directions) | agentification-missing | research-agentic | hume, iris | trivial | high | Add `/home/user/start-ui-tanstack-drizzle-shadcn-hexagonal/CLAUDE.md` containing `@AGENTS.md` plus a short `## Claude Code` section, so there is exactly one source of truth. Before doing so, de-Codex AGENTS.md:37. State in AGENTS.md and CONTRIBUTING.md that forks edit AGENTS.md, never CLAUDE.md, and add a `pnpm check` assertion that CLAUDE.md contains no content other than the import plus the Claude section (the same generate-and-verify discipline already used by `architecture:graph:check` and `check:migrations`). |
| 79 | `.claude/rules/testing.md` is inherited Prisma/tRPC-era text, its `paths:` glob matches zero files, and it propagated byte-identical into hume-demo | agentification-defect | research-agentic | — | trivial | high | Delete `.claude/rules/testing.md` or rewrite it for the real stack: `paths: ["tests/**/*.{unit,browser,integration}.{test,spec}.{ts,tsx}"]`, with rules that hold here (assert on returned Boxed `Result` values rather than Drizzle builder calls; escalate to the `integration` project for SQL serialization — the point AGENTS.md:147 already makes). Drop `Prisma.prisma` from `.vscode/extensions.json:8`. Add a `pnpm check` step asserting every `paths:` glob in `.claude/rules/*.md` matches at least one tracked file — AGENTS.md:145 already prescribes exactly this ('When a regression class is likely to repeat, add a guardrail through depcruise, Semgrep, or an architecture test'). |
| 80 | `pnpm architecture:graph` cannot run without a system Graphviz binary — the fix command for a `pnpm check` failure is itself broken, and the prerequisite is documented nowhere | template-defect | research-agentic | — | small | high | Make SVG rendering optional: catch the spawn error in `renderSvgFromDot`, or gate it behind a `--svg` flag, so `pnpm architecture:graph` regenerates the DOT and Markdown on any machine and skips the SVG with a warning when `dot` is absent. Add Graphviz to README's prerequisites list alongside Node/pnpm/Docker, and mention it in the `architecture:graph:check` failure message. This must land before the Markdown artifact is added to `CHECKED_ARTIFACT_FILENAMES`. |
| 81 | Neither AGENTS.md nor TESTING.md says how to run a SINGLE test file or a single test by name — and both apps independently added exactly that recipe | template-missing-practice | research-agentic | hume, iris | trivial | high | Add a 'Running one test' block to TESTING.md (and a one-line pointer from AGENTS.md's command table) with the four invocations both apps converged on: single file per project, `-t` name filter, single Playwright spec, and the visual variant. Add iris's working rule verbatim in spirit — targeted tests during work, `pnpm check`/`pnpm verify` only at completion — since it is the single highest-leverage prose the apps wrote that the template lacks. |
| 82 | Template ships a NIGHTLY Nitro build as a production dependency; Nitro v3 never went stable and the pin is 3 months stale | defect | research-stack | neither | small | high | Move `nitro` to the documented beta channel (`"nitro": "3.0.260610-beta"`) so the dependency has release notes and an advisory surface. Add a 'Known pre-release dependencies' section to AGENTS.md naming Nitro v3 beta and why TanStack Start currently requires it, so forks make an informed choice rather than inheriting an opaque hash. Add `nitro` to a Dependabot `ignore` entry (Dependabot cannot resolve `npm:` aliases anyway) and pair it with a CI check that compares the pin against the `latest` dist-tag. |
| 83 | TypeScript 7.0 owns the `typescript` latest tag; the template pins 6.0.3 and its eslint step structurally blocks the upgrade | modernization | research-stack | hume | medium | high | (1) Alias the pin explicitly as APP1 did — `"typescript": "npm:@typescript/typescript6@6.0.2"` — so intent is recorded and no routine `pnpm up` silently lands 7.0.2 in a repo whose `lint:eslint` cannot survive it. (2) Sequence the real TS7 move behind the oxlint consolidation. Check `@stryker-mutator/typescript-checker` (package.json:234, `^9.6.1`) as the second programmatic-API consumer before flipping. Remove `ignoreDeprecations: "6.0"` and `baseUrl` from tsconfig.json. |
| 84 | oxlint type-aware linting is stable; running both oxlint and eslint is now obsolete, and APP1 proved the migration end to end | missing-practice | research-stack | hume | medium | high | Port APP1's `oxlint.config.ts` into the template: add `oxlint-tsgolint@7.0.2001`, move the six type-aware rules to their `typescript/*` equivalents, and move eslint-plugin-react-hooks / -security / -playwright into `jsPlugins`. Keep APP1's honest residual — a ~60-line `eslint.typed.config.mjs` for the type-aware SonarJS rules tsgolint lacks, discovered programmatically via `requiresTypeChecking` — rather than claiming parity. Swap `lint:eslint` for `lint:typed-sonar` in `check` and `check:ci`. Adopt the typed `oxlint.config.ts` format over `.oxlintrc.json`. This is also the prerequisite that unblocks TS7. |
| 85 | docs/strict-modular-monolith.md tells agents that src/start.ts does not exist — while src/start.ts IS the CSRF-registering security spine | defect | research-stack | hume, iris | trivial | high | Rewrite the CSRF Policy section of docs/strict-modular-monolith.md to describe the file as it exists — src/start.ts defines the Start instance, replaces the framework defaults, and therefore registers createCsrfMiddleware explicitly at line 238/252 — and state that removing it silently disables CSRF for every browser-callable RPC. Adopt APP2's doc-drift rule into the template's own AGENTS.md, since both forks independently needed it. Consider an architecture test asserting that src/start.ts registers a middleware named csrf*, so the guarantee is enforced rather than documented. |
| 86 | The template's pnpm-workspace.yaml security-override block is stale, and BOTH apps independently expanded it — a fresh fork likely fails the template's own `pnpm check` | defect | research-stack | hume, iris | small | high | Adopt APP2's caret-range 'security floor' model wholesale into the template's pnpm-workspace.yaml — it is better-designed than what either the template or APP1 has, and the rationale comment is worth copying verbatim because it encodes a lesson (exact minimums silently go stale; caret floors self-heal; `minimumReleaseAge` covers the compromised-release window). Merge in the union of both forks' entries, and refresh the three stale ones (body-parser, shell-quote, svgo). Then add a CI job that runs `pnpm audit --audit-level=high` on a schedule rather than only inside `check`, so the template discovers new transitive advisories before its forks do. Two independent forks doing this work six days apart is the clearest evidence in this audit that it belongs upstream. |
| 87 | Upstream BearStudio/start-ui-web has diverged irreconcilably (oRPC + Prisma + src/features); the template must stop presenting itself as a syncable fork | modernization | research-starters | neither | small | high | Rename the package away from `start-ui-web`, move Harold Martin to `author` and Ivan Dalmet/Yoann Fleury to `contributors`, rewrite CONTRIBUTING.md (see governance finding), and add a short 'Relationship to upstream' section in README stating that BearStudio/start-ui-web is the MIT origin (already credited in LICENSE) and that upstream sync is unsupported because upstream is Prisma+oRPC+src/features. Do not invest in any upstream-merge tooling. |
| 88 | README Getting Started scaffolds UPSTREAM (`pnpm create start-ui`), and the Technologies line advertises oRPC + React Hook Form that this fork does not use | doc-drift | research-starters | both | trivial | high | Three one-line edits, no tooling required: (1) replace README.md:26 with the GitHub template flow ('Click Use this template', or `gh repo create <name> --template hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal`); (2) replace README.md:15 with links to this repo's own AGENTS.md / TESTING.md / docs/; (3) fix README.md:11 to name TanStack Form, @base-ui/react, TanStack server functions, and Drizzle. Optionally add a versioned `tanstack template compile` output later, but the GitHub template button already covers the create path. |
| 89 | Zero tags, zero releases, no CHANGELOG, and no provenance stamp — forks have no merge base and cannot compute which template commits they are missing | missing-practice | research-starters | neither | trivial | high | (1) Tag the template (`v4.1.0`, …) and add CHANGELOG.md split into 'affects generated code' vs 'template-internal'. (2) Add a provenance key written at generation time — `"template": {"repo":"hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal","tag":"v4.1.0","sha":"<full sha>","createdAt":"…"}` — matching Epic Stack's `epic-stack` package.json field pattern. Because GitHub's 'Use this template' cannot run code, stamp it from a `postinstall`/`pnpm init:from-template` step or a one-time GitHub Action. (3) Backfill the key into hume-demo and iris-insights-crm now using their root-commit dates (2026-07-16 and 2026-07-31) to pick the nearest template SHA. (4) Add `pnpm template:diff` shelling out to `git diff <recorded-sha>..<latest-tag>` against a cached bare clone. |
| 90 | No module generator in any of the three repos, and AGENTS.md documents the rules but never the procedure — while the smallest module costs 18 files plus ~14 root-config edits | missing-practice | research-starters | neither | medium | high | Add `tsx scripts/generate-module.ts` exposed as `pnpm gen:module <name> --tier=<presentation-only\|service\|full>` (no new dependency — the repo already runs .ts scripts). It must emit the chosen layers/gates + factory + placeholder port/use-case/test, AND perform the registrations: `stryker.<name>.config.mjs`, `tsconfig.stryker.<name>.json`, the four `test:mutation:<name>*` scripts, and insertion into all eight aggregate run-s strings. Add `--dry-run` and `--json` (so agents can drive it), and open AGENTS.md with an 'Adding a capability' section whose first line is the command. Separately, replace the eight hand-listed aggregate scripts with a glob over `stryker.*.config.mjs` so the enumeration cannot drift. |
| 91 | The book/genre demo domain is dead weight welded into the immutable migration baseline — 115 files outside the module dirs, and both apps paid a demolition cost | dead-weight | research-starters | both | medium | high | (1) Regenerate migration 0000 to contain only platform tables (user/account/session/verification) and move author/book/genre/publisher DDL into a separately-numbered demo migration that a removal step deletes wholesale. (2) Make .dependency-cruiser.cjs, sheriff.config.ts and tests/architecture/modular-monolith.unit.spec.ts glob-driven over `src/modules/*` instead of naming book and genre. (3) Ship `pnpm demo:remove` that deletes the two modules, their routes, composition entries, all 8 i18n catalogs, seeds, the 2 stryker configs + 2 tsconfig.stryker files + their package.json scripts, fixtures, permissions entries in src/modules/auth/domain/permissions.ts, id types in src/modules/kernel/domain/ids.ts, and the nav entries in src/app/shell/presentation/app/main-nav-config.ts — and prints what it removed. |
| 92 | The product name is hardcoded in platform source instead of a config constant — BOTH apps independently edited the same file to fix it | defect | research-starters | both | small | high | Add a single `src/app/brand.ts` (or extend the existing env/runtime-config) exporting `APP_NAME`, `APP_TAGLINE`, `APP_URL`, and have get-page-title.ts, logo.tsx, and the email footer import it. Replace 'Start UI' / 'start-ui.com' / 'Ivan Dalmet' in all src/platform/components/ui/*.fixture.tsx with neutral placeholders ('Acme', 'https://example.com', 'Ada Lovelace'). Add a `pnpm check` grep guard that fails if the literals 'Start UI', 'start-ui.com', or 'bearstudio' appear anywhere under src/ — cheap, and it converts a recurring manual cleanup into an enforced invariant. |
| 93 | Upstream marketing branding is rendered into product output: every transactional email links to https://start-ui.com | defect | research-starters | both | trivial | high | Replace the hardcoded footer with values from the brand constant proposed above, defaulting to the app name with no outbound link. Add the email footer to the `pnpm check` grep guard for 'start-ui.com'. Because this ships to end users under the fork's domain, treat it as a release blocker in the demo-removal/init checklist rather than a cosmetic cleanup. |
| 94 | The immutable baseline migration bakes in demo tables, forcing every fork to ship a DROP migration; the migration-drift checker also hardcodes six schema paths | defect | template-friction | iris | small | high | Squash the shipped migrations into one baseline containing only platform tables (auth, account, email, user), and if the demo domain is retained give it its own late migration a fork can drop wholesale. Change `SCHEMA_PATHS` in `scripts/check-migration-edits.mjs:4-11` from a literal array to a glob over `src/modules/*/infrastructure/drizzle/schema.ts` plus the kernel schema directory. |
| 95 | No module scaffolding exists; adding one capability means hand-editing roughly a dozen locations, most of which fail silently when skipped | missing-practice | template-friction | neither | medium | high | Ship `pnpm gen:module <name>` under `scripts/` (no new dependency required) that writes the module skeleton and gate files, `src/composition/<name>.ts`, patches `src/composition/index.ts` and the four i18n barrels. Separately, eliminate most of the need: collapse the 17 Stryker root files into one `stryker.module.config.mjs` parameterised by env var, derive `.github/workflows/mutation-testing.yml:21`'s matrix from `fs.readdirSync('src/modules')`, and convert the literal module lists in `.dependency-cruiser.cjs`, `sheriff.config.ts`, `.semgrep.yml`, `scripts/check-migration-edits.mjs`, and `tests/architecture/modular-monolith.unit.spec.ts` to wildcards or directory enumeration. Add `docs/adding-a-module.md` for the residue. |
| 96 | README is upstream BearStudio's: wrong scaffold command, upstream docs link, two advertised technologies that are not dependencies | doc-drift | template-friction | both | small | high | Rewrite README.md for this fork: replace `pnpm create start-ui` with a clone/degit of hbmartin/start-ui-web; drop or repoint the docs.web.start-ui.com link; delete oRPC and React Hook Form from README:11 and correct shadcn/ui to '@base-ui/react with shadcn-style components'; add an Architecture section naming the four roots, the public-gate rule, and the guardrail stack (dependency-cruiser, Sheriff, Semgrep, CodeQL, Stryker); fix package.json's `author`; add a 'Make it yours' section covering the rename checklist and demo-domain removal. |
| 97 | First run fails out of the box: `.env.example` ships an AUTH_SECRET the config validator rejects twice over | defect | template-friction | neither | trivial | high | Add an explicit README Installation step between `cp .env.example .env` and `pnpm install` (`pnpm dlx @better-auth/cli@latest secret`, paste into `.env`), or better, ship a `pnpm setup` script that copies `.env.example` and injects a random dev secret. Add `pnpm run env` to the README verification list so the failure is self-diagnosing. |
| 98 | `src/platform/env/config.ts` and the kernel disagree on what 'production' means, so a staging build silently stops enforcing HTTPS on client URLs | defect | template-friction | iris | trivial | high | Replace `src/platform/env/config.ts:18-21` with the kernel's semantics (treat only `development`/`test` as non-prod; otherwise fall back to `PROD`), with a comment explaining why the definition is duplicated rather than imported (the platform-must-not-import-modules rule). Add a unit test pinning both predicates to the same truth table, including the `NODE_ENV=staging` + `PROD=true` case, so they cannot drift again. |
| 99 | CI never runs `pnpm check` or `pnpm check:ci`; it re-lists commands inline, and `lint:sheriff` plus `knip:deps` run in no workflow at all | defect | template-friction | hume | small | high | Make `.github/workflows/code-quality.yml` invoke `pnpm check:ci` as a single step rather than re-listing eleven commands inline, so adding a check to the script automatically runs in CI. Bring `check:ci` to parity with `check` (or document each omission with a comment naming the workflow that covers it — e.g. `semgrep` is covered by `.github/workflows/semgrep.yml`, which runs `semgrep scan --config .semgrep.yml --error`). Add `lint:sheriff` and `knip:deps` to both. |

## Medium

| # | Finding | Kind | Area | Apps | Effort | Conf. | Recommendation |
|---|---|---|---|---|---|---|---|
| 1 | The committed architecture artifact is Graphviz DOT only; the readable mermaid `.md` was deleted from the template and `architecture:graph:check` verifies only the `.dot` | defect | Agentification | hume, iris | trivial | high | Revert the c6a0dce deletion for docs/architecture/module-layer-dependencies.md (the .svg is optional — both apps kept it, but it costs 30-64 KB of churn per regeneration). Change scripts/generate-module-dependency-graph.ts:39 to `[ARTIFACT_FILENAMES.dot, ARTIFACT_FILENAMES.markdown]` so the already-in-`check` `architecture:graph:check` fails when the readable graph drifts. Do NOT add the .svg to the checked set — it depends on the `dot` binary (:404-425), which would reintroduce an environment-dependent gate failure. Link the .md from AGENTS.md as the first thing to read when orienting in the module graph. |
| 2 | The module-dependency graph generator omits type-only imports, under-reporting the graph agents read by 21 edges; APP2 found and fixed it, template still wrong | defect | Agentification | iris | trivial | high | Port APP2's b60c9a5 change verbatim into scripts/generate-module-dependency-graph.ts:479, comment included — the comment records the deliberate scoping (doc generator only, not .dependency-cruiser.cjs, because no-circular would newly fire on the user/domain type-only cycle) and that reasoning is exactly what would otherwise be lost. Regenerate and commit the .dot (and the .md, once restored). Add a regression test asserting `classifyDependencyStyle`'s `type-only` branch is reachable from a real cruise result, not just from a synthetic input. |
| 3 | Dependency-cruiser and Sheriff hardcode the demo module names, so three boundary rules are inert in any fork — and they already fail to cover the template's own `auth` and `email` modules | defect | Agentification | iris | small | high | Replace the three enumerated depcruise rules with the backreference form already used at .dependency-cruiser.cjs:90-95 — `from: { path: '^src/modules/([^/]+)/domain' }, to: { path: '^src/modules/$1/presentation' }` — so they cover every module including ones a fork adds. Do the same for sheriff.config.ts:44,47. Delete the `^src/modules/book/server\.ts$` exception at :209 (fix the offending file, or generalize it with a comment). Add a tests/architecture/ assertion — the file-reading pattern already exists at tests/architecture/dependency-cruiser.unit.spec.ts:23-31 — that every literal module name appearing in .dependency-cruiser.cjs and sheriff.config.ts corresponds to a directory under src/modules/. Separately, ship a `docs/FORKING.md` checklist or a `pnpm scaffold:strip-demo` covering the 53 source files, 57 test files, 2 stryker configs, and 2 tsconfigs. |
| 4 | Architecture rules are stated three times with nothing verifying they agree; four measurable drifts across the three repos, including one in the template's own AGENTS.md | doc-drift | Agentification | — | medium | high | Add scripts/check-docs-drift.mjs wired into `check` that (a) asserts every `pnpm <script>` named in AGENTS.md / CLAUDE.md / .claude/rules/*.md exists in package.json, (b) asserts the `pnpm check` row's description enumerates exactly the tasks in the `check` script, and (c) asserts every `src/`, `docs/`, and `tests/` path mentioned in those docs exists on disk. All four drifts above plus the src/start.ts one are caught by (b) and (c) alone. Then de-duplicate: make .claude/rules/*.md thin pointers ('Authoritative: AGENTS.md § Public Gates' plus the two or three most-violated rules) rather than parallel restatements. Adopt APP2 CLAUDE.md:71 into AGENTS.md verbatim — 'When a doc does not match the code, fix the doc in the same change.' |
| 5 | Guardrail output is not consistently agent-actionable: 20 of 57 depcruise rules carry no explanation, and the custom check scripts emit no line numbers or machine-readable output | agentification | Agentification | — | small | high | (1) Add a `comment:` to all 20 uncommented depcruise rules in the house style of :90-95 — one sentence naming the boundary, one naming the sanctioned alternative. (2) Change scripts/check-test-layering.mjs:128 to emit `path:line:col: message` using the positions `ts.preProcessFile` already returns. (3) Add `summary.json` alongside summary.md in scripts/task-verify.mjs:141 — `{ status, steps: [{ id, command, exitCode, durationMs, logPath }] }`. (4) Add a `depcruise:json` script with `--output-type json` for the same reason. |
| 6 | No ADR convention and no `docs/adr/` in the template; APP1 built a seven-ADR corpus with a house style richer than the skill's minimal format | missing-practice | Agentification | hume | trivial | high | Create docs/adr/ with a README.md stating the numbering rule and the ADR-FORMAT.md three-part qualification test, plus 0000-template.md in APP1's style (`# ADR NNNN: Title` / `- Status:` / `- Date:` / optional `- Supersedes:`/`- Amends:` / `## Context` / `## Decision` / `## Consequences`). Seed with two or three ADRs recording decisions the template has already silently made and that agents demonstrably re-litigate: 'Tagged Result.Ok outcomes over exceptions' (AGENTS.md:78-88), 'Public gate files over deep imports' (AGENTS.md:50-64), and 'oxlint+oxfmt primary with a narrow typed-ESLint pass' — APP1 already wrote the body of the third in docs/oxc-tooling-decisions.md. Add to AGENTS.md's guardrail section (currently one line, :145): when a change closes an alternative, record an ADR. |
| 7 | Template has no `.agents/skills/`, no `skills-lock.json`, and no skills discovery path; both apps carry the same three generic mattpocock skills pinned by identical SHA-256 | missing-practice | Agentification | hume, iris | small | medium | Ship .agents/skills/domain-modeling/ (SKILL.md + ADR-FORMAT.md + CONTEXT-FORMAT.md + agents/openai.yaml) at minimum — that is the one with demonstrated downstream effect on two other template gaps. Add the `.claude/skills -> ../.agents/skills` symlink so one vendored tree serves both ecosystems. Committing skills-lock.json with pinned hashes is a reasonable supply-chain control for a repo that already pins GitHub Action SHAs and runs detect-secrets/osv-scanner, but check the upstream licence before vendoring third-party skill bodies. The higher-value addition is a template-OWNED skill the apps could not have written: `.agents/skills/new-capability/SKILL.md` walking an agent through scaffolding a module (create domain/application/ports/application/use-cases/infrastructure/transport/presentation, add only the needed gates, wire src/composition/<capability>.ts with createCachedFactory + `??` merging + `__reset*Composition()`, add the depcruise/Sheriff entries, add the test-layer files) — the highest-frequency multi-file task here, currently only prose scattered across AGENTS.md:65-77 and .claude/rules/modules.md. |
| 8 | `.claude/rules/testing.md` is upstream Prisma boilerplate whose `paths:` glob matches nothing — and it is the template's only Claude-facing testing rule | dead-weight | Agentification | — | trivial | high | Delete the Prisma sentence and the `paths:` frontmatter. Replace .claude/rules/testing.md with a ~15-line condensation of TESTING.md — the layer table, the 'cheapest test that proves the behaviour' rule, the single-test invocations, and the escalation ladder — mirroring how architecture.md and modules.md condense AGENTS.md and using their no-frontmatter shape so it actually loads. Remove `"Prisma.prisma"` from .vscode/extensions.json:7. Add a tests/architecture/ guard that fails on any `prisma` occurrence outside the lockfile, so the fossil cannot return. |
| 9 | The fix-it command for a gate inside `pnpm check` requires Graphviz, which is documented nowhere outside the CI workflow — and neither `architecture:graph` nor `architecture:graph:check` appears in AGENTS.md, README.md, or TESTING.md | defect | Agentification | — | trivial | high | Document `pnpm architecture:graph` as the remedy in AGENTS.md's Canonical Commands table and in the new Setup section, with the Graphviz prerequisite stated. Better: make `createGraphArtifacts` degrade gracefully when `dot` is absent — write the .dot and the .md, skip the .svg with a warning — so the regenerate command works everywhere and only the optional SVG needs the binary. That also makes the recommended fix (adding the .md to CHECKED_ARTIFACT_FILENAMES) safe. |
| 10 | Template has no deployment or release automation, the build job produces no artifact, and a stale Next.js-era `.dockerignore` implies a container story that does not exist | missing-practice | CI/CD, supply chain, and release engineering | — | large | high | Prioritise the cheap parts: upload `.output/` from the `build` job so E2E and any future release can consume it instead of rebuilding; delete or rewrite `.dockerignore` for this stack (drop `/.next/`, `/out/`, storybook; add `.output`, `test-results`, `playwright-report`, `coverage`, `cosmos-export`); delete the inert `docker` dependabot entry. Only then consider a container path. NOTE: do NOT copy the investigator's suggested `HEALTHCHECK` hitting `/api/health` — no such route exists (`ls src/routes/api` → auth.$.ts, dev.email.$template.ts, telemetry.logs.ts, telemetry.otel.v1.metrics.ts, telemetry.otel.v1.traces.ts, telemetry.sentry-tunnel.ts, upload.ts, webhooks.resend.ts); a health endpoint would have to be added first. A `migrate` job wired to a release is the highest-value remaining piece since README.md:212 already asserts the requirement. |
| 11 | The Visual CI job re-runs the entire check suite — including a mutating `pnpm format:changed` and a network `pnpm audit` — inside a 30-minute Postgres+MinIO job with no mutation guard | defect | CI/CD, supply chain, and release engineering | — | small | high | Replace `pnpm verify:task -- --visual` at e2e-tests.yml:293 with `pnpm test:visual` directly. If the single-report artifact is wanted, add a `--only` flag to scripts/task-verify.mjs and skip the unconditional `format-changed` step when `process.env.CI` is set, or follow it with `git diff --exit-code -- .`. |
| 12 | `affected-tests` and `coverage` run the same tests in the same workflow; both pay a full install plus Playwright setup | dead-weight | CI/CD, supply chain, and release engineering | — | trivial | high | Keep `affected-tests` on `pull_request` and gate `coverage` to `if: github.event_name == 'push'` plus a nightly schedule (or the inverse if Codecov PR comments are wanted), and drop the duplicate `Setup Playwright` from whichever no longer runs on PRs. |
| 13 | `knip:deps`, `lint:sheriff`, and `pnpm codeql:test` never run in CI — the four custom CodeQL queries have no automated verification | missing-practice | CI/CD, supply chain, and release engineering | hume | small | high | Add `pnpm knip:deps` to the existing `lint-typecheck` job and `pnpm lint:sheriff` to the `architecture` job (both cheap, no new install). Add a `query-tests` job to codeql.yml that installs the CodeQL CLI and runs `pnpm codeql:test`, gated on `paths: ['.github/codeql/**']` plus a weekly schedule. Adopt APP1's `requiredRules` mechanism so the analyze step fails when a custom query produces no executed-rule entry. |
| 14 | `node-version: 24` is hardcoded at 16 workflow call sites, duplicating `.node-version`/`.nvmrc`/`engines` — APP1 fixed it with `node-version-file` | modernization | CI/CD, supply chain, and release engineering | hume | trivial | high | Delete `inputs.node-version` from .github/actions/setup-pnpm/action.yml and set `node-version-file: '.node-version'`; strip all sixteen `with: node-version: 24` blocks (keep `pnpm-version` where jittest passes it). Port APP1's `scripts/check-node-version.mjs` and add `check:node-version` to `check`/`check:ci` so `.nvmrc`, `engines`, and `.node-version` cannot diverge. |
| 15 | Template's action pins are 1-2 majors behind APP1's, and Dependabot's configuration cannot reach the composite actions or the pinned image digests | defect | CI/CD, supply chain, and release engineering | — | small | medium | Replace `directory: /` on the github-actions entry with `directories: ['/', '/.github/actions/setup-pnpm', '/.github/actions/setup-playwright']` and add a second group covering `update-types: [major]` so major bumps are proposed. Delete the inert `docker` ecosystem entry. Add a scheduled `scripts/check-image-digests.mjs` to supply-chain.yml that resolves each pinned image tag and reports drift. Adopt APP1's refreshed pin set. |
| 16 | CI-generated secrets are written to `$GITHUB_ENV` unmasked; APP1 added an `::add-mask::` helper | missing-practice | CI/CD, supply chain, and release engineering | hume | trivial | high | Port APP1's `write_masked_env` / `write_env` split into code-quality.yml:338-363 and both e2e-tests.yml blocks (:57-74, :195-211). Switch AUTH_SECRET to `openssl rand -hex 32`. The explicit split is the right shape — indiscriminate masking produces `***` noise that makes logs unreadable. |
| 17 | Security overrides are pinned to exact minimums, which silently reopens advisories — APP2 converted them to caret floors | modernization | CI/CD, supply chain, and release engineering | iris | trivial | high | Convert the advisory-floor entries in pnpm-workspace.yaml to caret ranges and adopt APP2's comment block verbatim so the floor-vs-pin distinction is documented at the point of use. Add a line to AGENTS.md: caret for security floors, exact for compatibility pins, and a comment naming the GHSA for every floor. Note APP1's separate lesson: avoid version-selector keys (`'fast-uri@3.1.2': …`) for advisory floors — they self-invalidate. |
| 18 | No advisory-suppression path for OSV — APP1 had to invent `osv-scanner.toml`, unlinked from the template's existing risk register | missing-practice | CI/CD, supply chain, and release engineering | hume | small | medium | Ship a seeded `osv-scanner.toml` in the template with a header comment establishing the convention (`ignoreUntil` mandatory, `reason` must name the docs/security-risk-register.md row) and a commented example entry. Extend scripts/check-risk-register.mjs to also parse osv-scanner.toml and fail when an `ignoreUntil` has passed or when an ignored ID has no matching register row, so the two suppression lists cannot drift. |
| 19 | Fork-time identity is untemplated: both apps still ship `"name": "start-ui-web"` and APP2 still points vulnerability reporters at the template author's repo | defect | CI/CD, supply chain, and release engineering | — | small | high | Add `scripts/init-from-template.mjs` (surfaced as `pnpm init:project`, prompted for in the README quickstart) that takes an app name and GitHub owner/repo and rewrites package.json name/version, .github/SECURITY.md URLs and prose, the EMAIL_FROM defaults in code-quality.yml and e2e-tests.yml, .env.example app-name strings, and the CodeQL pack directory/qlpack name/rule-id prefix. Move the 776 KB of README marketing images out of the template body. Add .github/CODEOWNERS (at minimum `/.github/ @owner` and `/docs/security* @owner`) and assert its existence in the workflow-policy test. |
| 20 | Promised supply-chain features were never implemented: no `attest-sbom`, no release-attached SBOM, no branch-ruleset automation | missing-practice | CI/CD, supply chain, and release engineering | — | medium | high | Add SHA-pinned `actions/attest-sbom` alongside `attest-build-provenance`. Add `on: release: types: [published]` plus a `gh release upload` step so SBOMs outlive the artifact window. Commit the promised ruleset as `scripts/apply-branch-ruleset.sh` — an idempotent `gh api` call listing required check contexts by exact job name — rather than leaving it in a commit message. Update .github/SECURITY.md to describe the verification path that actually exists. |
| 21 | AGENTS.md understates `pnpm check`, misdescribes `pnpm test`, and instructs agents to use the "Codex in-app Browser" in a `.claude/`-configured repo | doc-drift | Documentation, onboarding, and doc-drift | hume | trivial | high | Generate the command table from package.json, or add a unit test asserting (a) every command named in AGENTS.md exists in package.json and (b) the `check` row enumerates exactly the run-p targets. Add the module table and `src/start.ts` guardrail bullet hume had to invent. Replace "Codex in-app Browser" with a tool-neutral phrase. |
| 22 | No CONTEXT.md domain glossary in the template; both apps created one and both wired it into their agent instructions | missing-practice | Documentation, onboarding, and doc-drift | both | small | high | Add a `CONTEXT.md` stub with the book/genre/account/user vocabulary filled in as a worked example plus a clear 'replace this with your domain' header, referenced from AGENTS.md and the new CLAUDE.md. Adopt iris's rule that renames must update CONTEXT.md in the same change. |
| 23 | Template commits only the .dot architecture artifact; the generator emits mermaid .md and .svg, and both apps committed all three | missing-practice | Documentation, onboarding, and doc-drift | both | trivial | high | Commit all three artifacts and extend `CHECKED_ARTIFACT_FILENAMES` (scripts/generate-module-dependency-graph.ts:39) to include the markdown so `--check` gates the human-readable version (the SVG may stay unchecked if graphviz output is not byte-stable). Link `docs/architecture/module-layer-dependencies.md` from the new README documentation index — a rendered module graph is the best onboarding artifact this template has and it is currently invisible. |
| 24 | Sheriff is a dependency, a 4KB config and a script, but is never run by check, check:ci, or CI — while a template doc claims it runs in CI | dead-weight | Documentation, onboarding, and doc-drift | hume | trivial | high | Decide and document: either add `lint:sheriff` to `check` and `check:ci` as hume did, or delete sheriff.config.ts and the dependency. If it deliberately stays out, say so explicitly in AGENTS.md the way hume's CLAUDE.md does ("deliberately outside `pnpm check`; run when touching boundaries") — silence plus a doc claiming it runs in CI is the worst combination. |
| 25 | No ADR practice or decision-record format in the template; hume built docs/adr/0001-0007 and iris wrote long-form decision docs instead | missing-practice | Documentation, onboarding, and doc-drift | both | small | high | Ship `docs/adr/0000-template.md` using hume's Status/Date/Context/Decision/Consequences shape plus a numbering convention in the README doc index, seeded with 2-3 real template ADRs the forks would otherwise re-derive: why Boxed `Result` over exceptions, why public gate files over deep imports, why drizzle migrations are immutable. Adopt iris's "Revisit triggers" heading — it is the part that makes an ADR survive a later maintainer. |
| 26 | Inherited SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md and LICENSE still describe an open-source BearStudio starter inside private product repos | doc-drift | Documentation, onboarding, and doc-drift | hume | small | high | Mark these four files as fork-customization points with a `<!-- FORK: replace before first release -->` marker, list them in the README's fork-and-rename checklist, and have `scripts/rename-app.mjs` rewrite the project name, abuse contact and copyright holder. Drop the unfulfillable supported-versions table until releases exist. Add a minimal PR template asking for the `pnpm format:changed && pnpm check && pnpm test:affected` result and a "docs updated in the same change?" checkbox — the discipline iris had to write into CLAUDE.md by hand. |
| 27 | Onboarding volume is fine (1,300 lines) but there is no reading order, roughly a fifth is wrong, and TESTING.md's layer map omits three real test layers | missing-practice | Documentation, onboarding, and doc-drift | both | medium | high | Restructure: README.md (identity, quickstart, fork & rename, deploy, doc index) → CLAUDE.md/AGENTS.md (agent contract, commands, guardrails, module inventory) → CONTEXT.md → TESTING.md → docs/ split into `docs/architecture/` (strict-modular-monolith.md renamed architecture.md + the generated graph .md), `docs/guides/` (adding-a-module, removing-the-demo-domain, pulling-template-updates, toolchain-decisions), `docs/operations/` (production-observability, security-rate-limiting), `docs/security/` (practices — rewritten, risk-register, upload), `docs/adr/`. Add the three missing test layers to TESTING.md's map. Delete rather than migrate anything that cannot be verified against the code. |
| 28 | The template has no docs-lint, and the guard it needs is provably cheap — a 20-line checker I ran finds exactly the real defects with near-zero false positives | missing-practice | Documentation, onboarding, and doc-drift | neither | small | high | Add `scripts/check-docs.mjs` wired into `pnpm check` alongside the four existing guard scripts, asserting: (a) every `pnpm <script>` named in docs exists in package.json; (b) every backticked repo-relative path in docs exists, minus an explicit forbidden-roots allowlist; (c) every module name mentioned in AGENTS.md exists under src/modules; (d) no doc sentence of the form 'does not define <path>' names an existing file. On the current template this single script fails on `pnpm dupes`, `.github/workflows/ci.yml`, and the CSRF/start.ts claim — i.e. it catches three of the four critical doc findings in this report, and would have caught hume's orphaned security-upload.md too. |
| 29 | The 4-locale i18n surface (ar/en/fr/sw) with per-module namespace files is documented nowhere, and CONTRIBUTING.md actively misdescribes it | dead-weight | Documentation, onboarding, and doc-drift | neither | small | high | Document the locale set and the per-module namespace convention in AGENTS.md and in the new `docs/guides/adding-a-module.md` (add `<module>.json` to every locale) and `docs/guides/removing-the-demo-domain.md` (delete it from every locale). Rewrite CONTRIBUTING.md:30 to name the actual locales, or reduce the template to `en` and let forks add locales — shipping ar/fr/sw translations of a demo Book domain is dead weight nobody in either fork wanted but neither removed. |
| 30 | The `browser-visual` layer is entirely undocumented — its config, setup file, snapshot root and npm scripts appear in zero docs, while both apps use it | missing-practice | Testing strategy, test infrastructure, and quality gates | both | small | high | Add a TESTING.md layer-map row for `tests/browser-visual/**/*.browser.visual.spec.tsx` / `pnpm test:browser:visual` describing the contract (design-system component states, chromium-only, baselines are reviewed artifacts), and add `pnpm test:browser:visual` / `:update` to the AGENTS.md command table alongside the existing `test:e2e:visual*` rows. Promote this layer over the e2e-screen visual layer as the default, since it is capability-agnostic and both apps kept it. |
| 31 | The 7 committed Playwright visual baselines are photographs of demo screens (login, app shell, manager-users CRUD) that a forker is expected to delete | dead-weight | Testing strategy, test infrastructure, and quality gates | hume | small | high | Keep only the capability-agnostic `__visual_snapshots__` baselines. Move the demo-screen visual specs (auth, app-shell, manager-users, authorization), their `-snapshots/` directories, and the demo helpers at tests/e2e/visual/helpers.ts:30-44 into the same removable 'example app' bucket as the book/genre tests so a scaffold step deletes them in one action. |
| 32 | Two measured browser/visual flake fixes exist only in APP1: a scrollbar-gutter reservation in the shared browser setup, and a screenshot pixel budget | missing-practice | Testing strategy, test infrastructure, and quality gates | hume | trivial | high | Port the scrollbar-gutter block into the template's tests/setup.browser.ts verbatim, comment included — the comment is the valuable part. Add `toHaveScreenshot: { maxDiffPixels: 64 }` to playwright.config.ts:40 so both visual runners have a stated tolerance, and note in TESTING.md that page-level specs may raise it with a measured justification. |
| 33 | `scripts/check-test-layering.mjs` allows 4 public gates while AGENTS.md and TESTING.md document 6 — the script rejects the `testing.ts` import that TESTING.md explicitly permits | defect | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Add `'backend'` and `'testing'` to publicModuleFiles at scripts/check-test-layering.mjs:9-14 and update the error string at :76 to list all six. Then remove the duplication: export the gate list from one module (scripts/lib/public-gates.mjs) consumed by check-test-layering.mjs, tests/architecture/modular-monolith.unit.spec.ts, and .dependency-cruiser.cjs. |
| 34 | `.claude/rules/testing.md` is six lines of stale Prisma/tRPC boilerplate whose frontmatter glob cannot match any file in the repo | doc-drift | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Rewrite .claude/rules/testing.md as a condensed mirror of TESTING.md's layer map plus the three enforced rules: (1) import property helpers from `@tests/support/property-testing`, never `@fast-check/vitest` (check-test-layering.mjs:89-96); (2) everything under `tests/integration/` must end `.integration.test.ts` (:109-116); (3) `*.workflow.integration.test.ts` may import a module only through its public gates (:118-131). Fix the frontmatter to `tests/**/*.{spec,test}.{ts,tsx}`. |
| 35 | `tests/server/test-setup.ts` globally mocks Better Auth, the Drizzle client and telemetry — and installs an authenticated session — for all 136 unit/architecture/security specs, none of which use the db mock directly | defect | Testing strategy, test infrastructure, and quality gates | hume | medium | high | Remove tests/server/test-setup.ts from the unit project's setupFiles (vitest.config.ts:81-84). Re-expose its behaviour from tests/server/test-utils.ts as an opt-in fixture (`installAuthAndDbMocks()`) called explicitly by the 9 files that import from it. Delete the duplicate `vi.mock('@tanstack/react-start/server')` at test-setup.ts:32-37 — the alias at vitest.config.ts:13-16 already covers it. |
| 36 | `tests/mocks/tanstack-react-start*` stubs are incomplete; APP1 had to add `getRequest`, `deleteCookie` and `serverFnMeta` on its first real feature, and the postgres config omits the aliases entirely | missing-practice | Testing strategy, test infrastructure, and quality gates | hume | trivial | high | Port APP1's three additions verbatim into tests/mocks/tanstack-react-start-server.ts and tests/mocks/tanstack-react-start.ts. Add the two tanstack aliases to vitest.postgres.config.ts:14-24 so all four configs agree. Add a test under tests/unit/tests/ asserting the mock's export names are a superset of the `@tanstack/react-start/server` bindings imported anywhere under src/, so the stub cannot drift behind again. |
| 37 | The public-workflow-integration layer is ceremony: 1 test in the template (in a demo module), 0 in both apps, but it owns an npm script, 14 lines of bespoke enforcement, and four TESTING.md mentions | dead-weight | Testing strategy, test infrastructure, and quality gates | both | small | high | Drop `test:integration:workflow` from package.json:76, drop check-test-layering.mjs:118-131, and collapse TESTING.md's two integration rows into one 'Integration' row describing the adapter shape (PGlite, durable assertions) both apps actually used. Keep the `*.integration.test.ts` naming convention and its enforcement at :109-116, which is cheap and does work. |
| 38 | No coverage threshold exists in any of the three repos, while TESTING.md calls coverage a 'floor metric' and the far more expensive mutation gate does have a hard break | missing-practice | Testing strategy, test infrastructure, and quality gates | neither | small | high | Add `coverage.thresholds` to vitest.config.ts:30-35 with honest starting numbers scoped to what a forker keeps, plus a stricter per-glob entry for `src/modules/*/domain/**`. Add a codecov.yml with a `patch` target so new code is held higher than the legacy floor. If a threshold is deliberately unwanted, delete the 'floor metric' phrasing at TESTING.md:16 so the doc stops promising a gate that does not exist. |
| 39 | `tests/architecture/modular-monolith.unit.spec.ts` hardcodes demo module names, so deleting `src/modules/book` makes the architecture guardrail itself fail `pnpm check` | defect | Testing strategy, test infrastructure, and quality gates | both | medium | high | Replace the hardcoded arrays at :426/:439/:454 with filesystem discovery (`fs.readdirSync('src/modules').filter(d => d !== 'kernel')`) plus an opt-out convention, matching the documented rule in .claude/rules/modules.md ('Only add folders and public gates when the module needs them'). Do the same for parser-guardrails.unit.spec.ts:7-11 — that list is a migration artifact, not an architecture rule. |
| 40 | `pnpm test` — and therefore `pnpm verify`, the documented merge gate — requires an installed Playwright Chromium on a fresh clone, with no doc saying so | defect | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Add a one-line 'Install Playwright browsers once: `pnpm exec playwright install --with-deps chromium`' to README.md's setup section and to TESTING.md's Quality Gates block, or add a `postinstall`/`prepare` step that installs chromium. Both are cheaper than the current situation where the failure is discovered by every forker independently. |
| 41 | The e2e CI job provisions and seeds a Postgres container that the app under test almost certainly never uses, because the PGlite socket server injects its own DATABASE_URL and re-seeds | defect | Testing strategy, test infrastructure, and quality gates | neither | small | low | Verify before deleting — add `psql "$DATABASE_URL" -c 'select count(*) from \"user\"'` after the Playwright step and check whether the container's row count reflects the run. If confirmed, delete the Start PostgreSQL / Migrate database / Add default data steps from both jobs and drop the POSTGRES_*/DATABASE_URL generation, keeping MinIO which the app genuinely uses. |
| 42 | `pnpm verify` — the documented merge gate — runs 5 of the 9 layers; both visual layers, postgres integration, and E2E are outside it | missing-practice | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Make verify honest or make the docs honest. If the darwin-baseline problem is fixed first, extend verify to `pnpm check && pnpm test && pnpm test:browser:visual && pnpm build`. If not, amend TESTING.md:76 and AGENTS.md:25 to state which layers verify covers and that visual/e2e/postgres require `pnpm verify:task -- --visual --e2e-chromium`. |
| 43 | `test:jittest` and a 140-line jittest.yml ship with no `jittest.config.json` — the file the workflow's cache key hashes and a commit message claims was added | dead-weight | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Either commit the jittest.config.json the workflow and commit message assume, add a TESTING.md row explaining the layer and its cost, and change the missing-secret branch from `exit 1` to a skip; or delete .github/workflows/jittest.yml, the `test:jittest` script, and the `catching-jit-tests-vitest` devDependency. Given zero use across 170 app commits, deletion is the defensible default for a starter. |
| 44 | `scripts/affected-tests.ts` cannot select browser-visual tests, and its run-everything tripwire omits `.dependency-cruiser.cjs` — the file that defines the graph the script walks | defect | Testing strategy, test infrastructure, and quality gates | neither | small | high | Add `browser-visual` to the alternation at :26-27 and teach main() to route those files through `vitest run --config vitest.visual.config.ts`. Add `.dependency-cruiser.cjs` and `vitest.visual.config.ts` to GLOBAL_CONFIG_FILES at :13-24. Extend tests/unit/scripts/affected-tests.unit.spec.ts with an assertion that every path in GLOBAL_CONFIG_FILES exists on disk, so a fork that deletes tests/server/ learns immediately. |
| 45 | APP1 invented a semgrep rule plus an architecture test that pins the rule's own regex, guarding literal test credentials — a generic practice the template lacks despite having suffered the bug | missing-practice | Testing strategy, test infrastructure, and quality gates | hume | small | high | Port `no-literal-test-credential-properties` from hume-demo/.semgrep.yml into the template's .semgrep.yml and port tests/security/test-credential-guardrail.unit.spec.ts alongside it. Generalise the test into a small helper that any semgrep `pattern-regex` rule can be pinned with, since the template has 101 rules and pins none. |
| 46 | CI-generated secrets are written to $GITHUB_ENV unmasked in three places; APP1 wraps every one in `::add-mask::` | missing-practice | Testing strategy, test infrastructure, and quality gates | hume | trivial | high | Port APP1's `write_masked_env`/`write_env` helper pair into .github/workflows/code-quality.yml and both jobs of .github/workflows/e2e-tests.yml, and switch AUTH_SECRET generation to `openssl rand -hex 32`. Extend tests/security/github-actions-supply-chain.unit.spec.ts with a check that any line writing a *SECRET*/*PASSWORD*/*KEY*/*_URL variable to $GITHUB_ENV is preceded by an `::add-mask::` for the same value. |
| 47 | The entire `browser-visual` layer — config, setup file, snapshot root, two npm scripts — is documented nowhere, yet it is the visual layer both apps actually kept and one extended | missing-practice | Testing strategy, test infrastructure, and quality gates | both | small | high | Add a TESTING.md layer-map row for `tests/browser-visual/**/*.browser.visual.spec.tsx` / `pnpm test:browser:visual` (design-system component states, chromium-only, baselines are reviewed artifacts) and add the two scripts to the AGENTS.md command table beside the existing `test:e2e:visual*` rows. Promote it over the e2e-screen visual layer as the template's default visual gate. |
| 48 | `pnpm test` — and therefore `pnpm verify`, the documented merge gate — launches a real Chromium via the root vitest config, and no human-facing doc says to install Playwright browsers | defect | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Add 'Install Playwright browsers once: `pnpm exec playwright install --with-deps chromium`' to README.md's setup section and TESTING.md's Quality Gates block, or add a prepare/postinstall step that installs chromium. Port APP2's CLAUDE.md line back into the template's own docs. |
| 49 | The `shared` Stryker scope's mutate allowlist omits four existing `src/platform` subtrees whose tests are inside the scope's own testFiles glob | defect | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Fold into the same fix as the dayjs correction: invert the allowlist to `src/platform/**/*.ts` with explicit negations for anything genuinely untestable, so the mutate set tracks the tree instead of a hand-maintained enumeration. Combined with the glob-resolves-to-something guard, this closes the whole class. |
| 50 | `site-access` — a complete, clean, generic site-wide gated-preview module the template has no equivalent for | missing-practice | app-inventions | hume | medium | high | Lift into the template as an opt-in capability, parameterizing cookie name, env-var name, signing domain and public-path allowlist through `createSiteAccessMiddleware`'s dependency object and a config factory. Replace the hard-coded `src/modules/site-access/server.ts` filename check with an injected identifier. Ship it disabled by default (absent `SITE_ACCESS_CODE` ⇒ middleware not registered) and document registration in `src/start.ts` alongside CSRF, since that file replaces framework defaults rather than extending them. |
| 51 | `format-changed.mjs` forwards `.md` to oxfmt, which ignores markdown — pre-commit fails on any docs-only commit | defect | app-inventions | iris | trivial | high | Drop `'.md'` from `FORMAT_EXTENSIONS` in `scripts/format-changed.mjs:7-17` with APP2's explanatory comment, and align `lefthook.yml`'s glob with oxfmt's real extension set. Better: derive the extension set from `.oxfmtrc.json`'s `ignorePatterns` at runtime so the two cannot drift again. |
| 52 | The module-dependency-graph generator omits type-only imports, so the architecture diagram is missing 21 of 99 real edges | defect | app-inventions | iris | trivial | high | Set `tsPreCompilationDeps: true` on the generator's own `cruise()` call at `scripts/generate-module-dependency-graph.ts:479` (not in `.dependency-cruiser.cjs`) and regenerate `docs/architecture/module-layer-dependencies.dot`. Copy APP2's comment explaining why the setting is local. Add a test asserting at least one dashed (type-only) edge exists so an accidental revert is caught. |
| 53 | Log sanitization redacts far more than it protects: an unanchored email regex over every string value turns scoped-package paths and stack frames into `[REDACTED]`, and recursion has no depth cap | defect | app-inventions | hume | small | high | Delete `EMAIL_PATTERN`/`redactString` and stop scanning string *values*. Keep key-based redaction but scope it: `email`/`phone` are reasonable defaults, `name` is not (it collides with `sdk.name`, `service.name`, `error.name`). Add APP1's `MAX_SANITIZED_DEPTH` guard. Do NOT copy APP1 wholesale — it deliberately dropped all PII heuristics because it is prototype software with demo personas, which is not the right default for a starter. Add unit cases asserting a scoped-package stack frame and `service.name` survive. |
| 54 | CodeQL SARIF baseline diffing with expiring, owned, justified acceptances — a generic mechanism APP1 built and the template lacks | missing-practice | app-inventions | hume | small | high | Add `scripts/check-codeql-sarif-baseline.mjs` and a `codeql-baseline.json` seeded with `requiredRules` for all four shipped queries and an empty `acceptedFindings`. Wire it into `.github/workflows/codeql.yml` after the analyze step and document the acceptance workflow (justification + owner + `reviewBy`) next to the existing risk-register docs. Add `osv-scanner.toml` as the same pattern for dependency advisories — APP1 has one at repo root; the template does not. |
| 55 | Boot-time configuration diagnostics: no legitimate way to see how config resolved before the logger exists | missing-practice | app-inventions | iris | trivial | high | Add `src/modules/kernel/infrastructure/config/config-diagnostics.ts` plus its `.semgrep.yml` exclusion, export `emitConfigDiagnostic` from `kernel/backend.ts`, and call it from the existing config accessors (`database.ts`, `auth.ts`, `telemetry.ts`) with non-secret resolution summaries. Document the emission rules in `.claude/rules/` so app authors extend it rather than adding raw console calls. |
| 56 | Telemetry is never flushed on shutdown, and adapters have no `forceFlush` — batched signals are lost on every SIGTERM | missing-practice | app-inventions | hume | small | high | Add `forceFlush?: () => Promise<void>` to `TelemetryAdapter` in `src/platform/telemetry/types.ts`, implement the fan-out in `src/composition/telemetry/adapter-chain.ts`, and lift `installTelemetryShutdownFlush` into `src/composition/telemetry/otel.server.ts` (and the Sentry adapter, which has its own flush API). Keep the bounded race and the signal re-raise; port APP1's test that injects a fake `process`. |
| 57 | Deployment metadata (release SHA, environment, service version) never reaches the build, so Sentry releases and OTel service versions are empty in production | missing-practice | app-inventions | hume | small | high | In `vite.config.ts`, resolve `release` and `environment` from platform env with documented precedence (explicit `VITE_*` → platform variable → unset) and pass `release` to `sentryTanstackStart`. Add the `define` block for the four `import.meta.env` values. Put the Vercel variable names behind the same platform-signal table proposed for `app-url`. Note the interaction with the `runtimeEnv` ordering defect: once build-time values stop overriding runtime ones, baking these is safe. |
| 58 | Both apps added root `CLAUDE.md` and `CONTEXT.md`; the template ships `.claude/rules/` but no agent entry point and no skill pinning | agentification | app-inventions | both | small | high | Add a root `CLAUDE.md` to the template indexing AGENTS.md / TESTING.md / CONTEXT.md / `.claude/rules/*` and stating the commands loop, plus a `CONTEXT.md` stub instructing the fork to replace the placeholder glossary. Adopt `skills-lock.json` with the three shared engineering skills pre-pinned, and add a `check` step verifying each pinned `computedHash`. Do not present `.agents/skills/` vendoring as a two-app convention — offer it as an option, since only APP1 does it. |
| 59 | Type-aware linting is coupled to a full ESLint config; APP1 split it into a typed-only pass behind an oxlint-first setup | modernization | app-inventions | hume | medium | medium | Evaluate migrating `.oxlintrc.json` → a typed `oxlint.config.ts` and narrowing `eslint.config.mjs` to only the rules that require type information, keeping both under one `pnpm lint` via `run-p`. Adopt APP1's programmatic derivation plus its representative-rule assertion. Record the decision and its removal criterion in a template `docs/` note as APP1 did in `docs/oxc-tooling-decisions.md` — that document is itself the reusable artifact. While there, check whether the sonarjs block's file scope should be narrowed to `sourceTsFiles` so typed rules always have a program. |
| 60 | Rate-limit posture: fail-open on an untrustworthy client IP, and `TRUSTED_PROXY_DEPTH` silently does not reach the auth limiters | missing-practice | app-inventions | both | small | medium | Configure `advanced.ipAddress` (either `trustedProxies` or `ipAddressHeaders`) in the template's `createAuth` so the auth and OTP limiters attribute requests the same way `getClientIp` does, and state in `.env.example` that `TRUSTED_PROXY_DEPTH` alone does not cover them. Add an unattributed-bucket helper next to `defaultRateLimiter` so "no trustworthy IP" degrades to a coarse global ceiling rather than skipping the limit. Ship APP2's `docs/security-rate-limiting.md` as a template doc. Before implementing, verify the Better Auth behaviour against the installed library — this is the one claim in this area I could not check. |
| 61 | `mapAppErrorToServerFnError` rethrows non-`AppError` exceptions raw across the server-function boundary | defect | app-inventions | hume | trivial | medium | Replace `throw error;` at `result-mapper.ts:66` with `throw new ServerFnError('INTERNAL_SERVER_ERROR', { message: 'Internal server error' })`, keeping the original as the `cause` so server-side capture is unaffected. Add a unit case asserting a raw `TypeError` does not reach the client with its own message. Confirm the serialization behaviour empirically before deciding severity. |
| 62 | An `AppError` flattened at the server-function boundary leaves no server-side record — APP2 had to add logging inside the template's own transport mapper | missing-practice | app-inventions | iris | small | high | Give `mapAppErrorToServerFnError` an injected `Pick<Logger,'error'\|'warn'>` and report before flattening, splitting `warn` (client categories, no `exception` field) from `error` (system / status >= 500, with `exception`). Add `src/modules/kernel/domain/errors/describe-error-chain.ts` for the `causeChain` detail. Either make the template's `:49-51` comment true or delete the claim. |
| 63 | The template's production TLS rule rejects the connection strings managed Postgres providers actually issue — APP2 had to delete it to deploy | friction | app-inventions | iris | small | high | Keep the cleartext rejections (`http://`, `ws://`, `sslmode=disable`) as unconditional. Downgrade the `verify-ca`/`verify-full` requirement to an opt-out with a named env var (e.g. `DATABASE_REQUIRE_VERIFIED_TLS=false`) that emits a loud startup diagnostic via `emitConfigDiagnostic` naming the residual MITM exposure, rather than a hard failure. Document the provider reality in `.env.example` and README next to `DATABASE_URL`. This preserves the secure default while making the escape hatch visible and auditable instead of a silent deletion. |
| 64 | Both apps deleted the same demo surface — book/genre modules and the whole S3 object-storage/upload stack — including its hooks inside `src/platform` and `src/modules/kernel` | dead-weight | app-inventions | both | medium | high | Keep the demo capability but make it removable in one step: move the object-storage port, `upload-rejected-error`, `storage.ts` config and the `better-upload` adapter out of `kernel` into a `storage` module (or into `book` itself), make `/api/upload` come from an injected pathname list rather than a literal in `browser-mutation-protection.ts:17`, and drop `s3BucketPublicUrl` from `security-headers.ts` in favour of a generic extra-CSP-origins option. Then document a single `pnpm remove:demo` (or a `docs/` checklist enumerating the 29 files) so a fork does not have to rediscover the seams. At minimum, add the seam list to the template README — both forks paid this cost independently. |
| 65 | CI writes generated credentials to $GITHUB_ENV without `::add-mask::`, in the one repo that lints against secret logging | missing-practice | config-divergence | hume | trivial | high | Adopt APP1's `write_masked_env` / `write_env` pair in code-quality.yml and in both jobs of e2e-tests.yml, and switch `AUTH_SECRET` to `openssl rand -hex 32` (base64 emits `+`, `/`, `=`, which interact badly with shell quoting and log masking). Because the block is duplicated across three jobs in two workflows, factor it into `.github/actions/generate-ci-secrets/action.yml` so the fix cannot drift. |
| 66 | Template depends on unmaintained `npm-run-all`, which sits on the critical path of every documented command; APP1 swapped to `npm-run-all2` | modernization | config-divergence | hume | trivial | high | Replace `npm-run-all@4.1.5` with `npm-run-all2@^9` in devDependencies. No script text changes. Do not claim it resolves any override; re-run `pnpm audit` afterwards to see what, if anything, it actually clears. |
| 67 | APP1 migrated to a typed `oxlint.config.ts` with type-aware linting and folded four ESLint plugins into oxlint, cutting the second linter to a 60-line narrow pass | modernization | config-divergence | hume | large | high | Stage the migration: (1) convert `.oxlintrc.json` to `oxlint.config.ts` with `defineConfig` and no behaviour change, importing APP1's fuller `ignorePatterns`; (2) add `eslint-plugin-security`, `eslint-plugin-react-hooks`, `eslint-plugin-sonarjs`, `eslint-plugin-playwright` to `jsPlugins` and delete the corresponding ESLint blocks; (3) add `options.typeAware: true` + `oxlint-tsgolint`, moving the six rules over and pinning the other twelve `off` exactly as APP1 documents; (4) replace `eslint.config.mjs` with APP1's `eslint.typed.config.mjs`, keeping the metadata derivation, the four-rule assertion and `--no-inline-config`. Adopt oxfmt-native `sortImports` independently. Fix the warn-only severity problem (see separate finding) as part of step 4, not after. |
| 68 | Node/pnpm toolchain versions are declared in six places with nothing checking they agree; APP1 built a `bin/run` wrapper and a `doctor:node` verifier | missing-practice | config-divergence | hume | medium | high | Adopt `mise.toml`, `bin/run`, and the generic two-thirds of `scripts/check-node-version.mjs` (gate the devcontainer assertions on `.devcontainer/` existing). Add `check:node-version` to `check` and `check:ci` plus a `doctor:node` alias. Remove the `node-version` input from `.github/actions/setup-pnpm/action.yml`, switch to `node-version-file: '.node-version'`, and delete every `with: node-version: 24` call site. Route lefthook commands through `./bin/run` and document it in README.md. |
| 69 | Three shipped security workflows require GitHub Advanced Security; APP1 rewired CodeQL and OSV to work without it and deleted dependency-review | defect | config-divergence | hume | medium | high | Ship the upload-free path as the default and document the GHAS path as an opt-in: adopt `scripts/check-codeql-sarif-baseline.mjs` + `.github/codeql/codeql-baseline.json` with `upload: never` / `upload-database: false`, and set `upload-sarif: false` on the OSV workflow with a commented block showing how to re-enable upload where GHAS is available. Ship an `osv-scanner.toml` with a commented `[[IgnoredVulns]]` template (`id`, `ignoreUntil`, `reason` → `docs/security-risk-register.md`) so the suppression path mirrors the existing risk-register convention. Keep `dependency-review.yml` but add an `if:` guard or README note about the dependency-graph requirement. |
| 70 | tsconfig is not `strict`, keeps a redundant `baseUrl`, omits the erasable-syntax flags, and has no include/exclude | modernization | config-divergence | hume | medium | high | Turn on `strict: true` and drop the now-redundant `strictNullChecks`/`noImplicitAny` as a dedicated PR. Add `verbatimModuleSyntax`, `erasableSyntaxOnly`, `isolatedModules`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `noEmit`. Drop `baseUrl` (keep `paths`). Adopt APP1's `"types": ["node"]` allowlist with its comment. Add explicit `include`/`exclude`. Re-evaluate `ignoreDeprecations: '6.0'` — APP1 removed it and typechecks, so it is likely already unnecessary. |
| 71 | APP1 invented two portable semgrep rules and four depcruise rules, and closed a real hole: a module's `client.ts` gate cannot legally import its own infrastructure | missing-practice | config-divergence | hume | small | high | Close the `client.ts` hole first — it is a one-token edit to `.dependency-cruiser.cjs:257` and it currently contradicts `.claude/rules/modules.md`. Then adopt the `infrastructure/client/` convention with APP1's two `pathNot` carve-outs and the `client-infrastructure-no-server-or-kernel-adapters` reachability rule, and document `infrastructure/client/` in `.claude/rules/modules.md` and `AGENTS.md`. Add `no-process-exit-in-telemetry-shutdown` verbatim and `no-literal-test-credential-properties` together with a `tests/support/test-secrets.ts` helper. Add the four depcruise rules, generalising the two whose paths are APP1-shaped. Do not port `devcontainer-json-must-use-jsonc-parser` or `no-shared-unknown-evaluation-rate-limit-bucket`. |
| 72 | Semgrep's branded-ID guardrails are a hand-maintained 27-name alternation duplicated across two rules, seeded with demo brands and extended by neither app | defect | config-divergence | iris | small | high | Replace the hardcoded alternation with something structural, in order of preference: (1) generate the regex at check time from the branded-type declarations under `src/modules/*/domain/*.ts` and `src/modules/kernel/domain/ids.ts`, run with a `--check` mode like `architecture:graph:check`; (2) match on the branding mechanism instead of the names via metavariable constraints; (3) at minimum, hoist the alternation into a single YAML anchor referenced by both rules, strip the demo brands, and add a test asserting every exported branded type in `src/` appears in it. |
| 73 | Nine per-module Stryker configs, eight `tsconfig.stryker.*.json`, and 40 mutation scripts are fork tax neither app extended to its own modules | dead-weight | config-divergence | both | medium | high | Collapse to one parameterised config — a single `stryker.config.mjs` reading the module name from an env var or CLI argument, with `tsconfig.stryker.json` extending the root tsconfig and taking its `include` from the same parameter — plus two scripts (`test:mutation <module>`, `test:mutation:fast <module>`) and one aggregate that discovers modules from `src/modules/*`. That removes 16 root-level files, removes the knip workaround, and makes 'add a module' cost zero mutation-testing edits. |
| 74 | oxlint's ignore list has 2 entries where ESLint's has 9, so oxlint lints the generated route tree and generated icons | defect | config-divergence | hume | trivial | medium | Bring `.oxlintrc.json` `ignorePatterns` to parity with `eslint.config.mjs`'s `ignores` block (APP1's oxlint.config.ts:194-205 is the exact list to copy), and add `.agents/**`/`.claude/**` at the same time. If the oxlint config is migrated to `oxlint.config.ts` per the separate finding, this comes along for free — but do not defer it behind that larger migration. |
| 75 | `knip:deps` is in `check`/`check:ci` but runs in no CI workflow, so unused-dependency drift is caught only when a developer runs the full local aggregate | defect | config-divergence | — | trivial | high | Either add a `pnpm knip:deps` step to the static-analysis job in code-quality.yml (it is fast and needs no services), or — better — have CI call `pnpm check:ci` so the two lists cannot drift again, keeping the separate long-running jobs (tests, build, Postgres integration) as they are. Add `lint:sheriff` to the same aggregate. Then add a test or script asserting that every entry in `check:ci` is reachable from a workflow, so this class of divergence is detected rather than discovered. |
| 76 | The demo upload stack (`@better-upload/*`, MinIO service, `src/composition/book-upload.ts`) is fork tax both apps shed differently | dead-weight | config-divergence | both | medium | high | Move the upload capability behind a documented opt-in: keep the `ObjectStoragePort` in kernel (it is a legitimate hexagonal port) but ship the `@better-upload` adapter, the platform upload components, the MinIO docker services, the eight S3 CI variables, and the four guardrail rules as a removable slice with a one-command teardown, or as a documented 'add-on' the fork opts into. At minimum, rename `src/composition/book-upload.ts` to something capability-shaped and add a fork-setup checklist entry listing every file that must be touched to remove the stack — currently that list is at least eight files across four tools. |
| 77 | kernel/domain/ids.ts is a closed registry of demo-domain branded types that forks cannot cleanly shed | dead-weight | dx-ergonomics | neither | small | high | Move each capability's branded ids into that capability's own domain/ and re-export via its index.ts, leaving only genuinely cross-cutting brands (GeneratedId, RequestId, CorrelationId, CacheKey, UserId, SessionId, ScopeKey, EmailAddress, LanguageCode, OtpCode) in kernel. Make the semgrep brand-cast rule structural (any `as <Identifier>` where the identifier is imported from a `domain/` path) rather than name-enumerated, so deleting a module deletes its ids with no second edit. |
| 78 | `scripts/format-changed.mjs` fails the pre-commit hook on every docs-only commit | defect | dx-ergonomics | iris | trivial | high | Apply iris's fix: drop `'.md'` from FORMAT_EXTENSIONS and carry the invariant comment from 6d1f0c5. Better, derive FORMAT_EXTENSIONS from .oxfmtrc.json's ignorePatterns so the two files cannot drift again, and add a unit test that passes a markdown-only file list and asserts exit 0. Also take iris's widened lefthook glob so the hook and `pnpm format:check` cover the same extension set. |
| 79 | Both apps wrote 100% of their product UI with zero `useTranslation` while carrying four inherited locale trees | dead-weight | dx-ergonomics | both | small | high | Ship one locale (en) by default; move ar/fr/sw behind an opt-in `docs/i18n.md` recipe plus a `pnpm gen:locale <code>` cloner. Keep the i18next wiring and the key-emitting schema rule — those are cheap and correct — but stop making four-locale parity the default state a forker must maintain or delete. Optionally add a lint that flags a module presentation directory with zero useTranslation imports so opting out is a visible, suppressible decision. |
| 80 | tsconfig.json has no `incremental`, no `include`/`exclude`, and no project references — every typecheck is a cold pass over ~816 files | modernization | dx-ergonomics | neither | trivial | high | Add `"incremental": true` and `"tsBuildInfoFile": "node_modules/.cache/tsconfig.tsbuildinfo"`, plus an explicit `"include": ["src", "tests", "scripts", "drizzle", "*.ts", "*.mts"]` and `"exclude": ["node_modules", ".output", ".nitro", "dist", "test-results", "coverage", "cosmos-export"]` so the scope is intentional. Measure with `tsc --diagnostics` before/after and record it in AGENTS.md. Evaluate the native `tsgo` compiler separately, in the style of iris's react-compiler evaluation doc. |
| 81 | tsconfig enables three strict flags instead of `strict: true`; `useUnknownInCatchVariables` is off against 53 untyped catch blocks | missing-practice | dx-ergonomics | neither | small | high | Replace the three individual flags with `"strict": true` in one commit; `useUnknownInCatchVariables` is the payoff and forces explicit narrowing at the 53 boundary catches, which is exactly what the Result/AppError policy already asks for in prose. Add `"isolatedModules": true` and `"verbatimModuleSyntax": true` to match what the Vite/Rolldown per-file transpile already assumes, plus `"noImplicitOverride": true`. Leave `exactOptionalPropertyTypes` off initially — it interacts badly with Drizzle/Zod inference. |
| 82 | 40 of 120 package.json scripts and 17 of 53 root files are per-module Stryker boilerplate that is fully derivable | dead-weight | dx-ergonomics | neither | medium | high | Collapse to a single `stryker.config.mjs` parameterized by module (`STRYKER_MODULE=genre stryker run`), with createScopedStrykerConfig deriving globs and tsconfig from the name — the derivation is exactly the seven lines the diff shows. Generate the scoped tsconfig into a gitignored temp path at run time instead of committing eight near-identical files. Replace the 40 scripts with `test:mutation --module=<name>` plus three chains that enumerate `src/modules/*` from disk. That removes ~16 root files, ~36 scripts, and the knip workaround. |
| 83 | hume-demo invented a Node-version drift toolchain (`bin/run`, `mise.toml`, `check:node-version`) the template lacks | missing-practice | dx-ergonomics | hume | small | medium | Adopt hume-demo's bin/run verbatim, route both lefthook commands through it, and add `check:node-version` / `doctor:node` as template scripts. Ship mise.toml alongside .nvmrc/.node-version. Document the wrapper in README's Requirements section next to the Node/pnpm pins. |
| 84 | No pre-push hook anywhere: nothing catches a broken typecheck before CI, and the only escalation is an eleven-task command that cannot run offline | missing-practice | dx-ergonomics | neither | small | high | Add a `pre-push:` block running `typecheck` and `test:affected` in parallel with `skip: [merge, rebase]`. Pair it with the tiering from the check-gate finding so there are four named honest rungs: pre-commit (staged-file format+lint), pre-push (typecheck + affected tests), `pnpm check` (pure-Node static gates, offline), `pnpm verify`/CI (adds semgrep, audit, full test, build). Document the ladder in AGENTS.md. |
| 85 | The upload/object-storage subsystem (@better-upload + MinIO) is demo-only dead weight — both apps removed the server half independently, and hume was left with orphaned client components | dead-weight | dx-ergonomics | both | medium | high | Either make the upload capability a documented opt-in (an `docs/uploads.md` recipe plus the kernel port left in place, with the MinIO docker profile split out of `dk:init`), or move it out of kernel and into the `book` demo module it actually serves — `src/composition/book-upload.ts` already admits the coupling by its filename. As shipped it forces every fork to run a MinIO container for a feature neither real app wanted, and the half-removal in hume-demo shows the seam is not clean: deleting the server adapter leaves the client components and the shared `use-app-form` field wired to a dead backend. |
| 86 | README.md is the upstream BearStudio marketing README: its Getting Started scaffolds a *different* project, it links to docs that do not describe this fork, and it has no Architecture section — both apps discarded it wholesale | defect | dx-ergonomics | both | small | high | Rewrite README.md for this fork. Replace the Getting Started block at :23-29 with the actual way to start from this repo (degit/template-clone + `pnpm install` + `pnpm dk:init` + `pnpm db:init`), drop or qualify the docs.web.start-ui.com link since it documents a different codebase, and add a `## Architecture` section that states the four roots, the public-gate rule, and the composition rule in ~30 lines with pointers to AGENTS.md for detail. Fix the Requirements section at :17-21 at the same time (see the check-gate finding: semgrep/Python and Graphviz are missing). That both apps threw the file away and wrote 257 and 627 lines of their own is the signal — the template's front door does not describe the template. |
| 87 | sanitize-log-fields destroys legitimate diagnostics: an unbounded email regex applied to every string redacts scoped-package stack frames, and 'name' in the default key set wipes sdk.name/service.name | defect | hume-archaeology | hume | small | high | Three independent changes: (1) drop `'name'` from DEFAULT_SENSITIVE_LOG_KEYS at :25 — structural `name` fields are not PII and the key match is unconditional; (2) keep email/phone key redaction but either delete `redactString` or gate it to values under known free-text keys, and at minimum adopt 146e4ff's `value.includes('@')` fast path and bounded quantifiers `{1,64}@{1,189}.{1,63}` (the unbounded `+` form is also a backtracking hazard on a function that walks arbitrary SDK objects); (3) port a711431's `MAX_SANITIZED_DEPTH = 32` / `[MaxDepth]` bound. Add regression tests asserting `/node_modules/@sentry/core/index.js` and `{ sdk: { name: 'opentelemetry' } }` survive intact. |
| 88 | scopedListQueryOptions and scopedEntityQueryOptions build identical query keys while scopedInfiniteQueryOptions discriminates — latent cache collision | defect | hume-archaeology | hume | trivial | medium | Add `'list'` at :36 and `'entity'` at :86 to match `'infinite'` at :61. Add a unit test asserting that list/entity/infinite options built from the same baseKey and input produce three distinct queryKeys. Note in the changelog that this is a cache-key shape change and invalidates persisted query caches once on deploy. |
| 89 | Browser-test harness lacks a reserved scrollbar gutter, so the template's own combobox specs inherit a ~50%-of-runs 'element is not stable' flake that one CSS line removes | missing-practice | hume-archaeology | hume | trivial | high | Add the style injection plus APP1's explanatory comment to tests/setup.browser.ts. Consider `:root { scrollbar-gutter: stable; }` in src/platform/styles/app.css too if the production layout has the same jump, but the test harness is the minimum. |
| 90 | OSV, CodeQL and Dependency Review workflows all demand permissions private repos without GitHub Advanced Security do not have, and there is no mechanism for accepting a reviewed advisory | defect | hume-archaeology | hume | medium | high | Replace the reusable OSV workflow with APP1's inlined base-vs-PR scan + reporter steps under `contents: read` only (or at minimum set `upload-sarif: false` and drop the two extra permissions). Do the same for codeql.yml (`upload: never`, plus a versioned baseline check). Either drop dependency-review.yml or document that it requires GHAS on private repos. Ship an empty, commented osv-scanner.toml demonstrating the `[[IgnoredVulns]]` shape with a mandatory `ignoreUntil` and a `reason` pointing at docs/security-risk-register.md, and extend scripts/check-risk-register.mjs to assert every exception has a matching register entry and an unexpired date. Port scripts/check-codeql-sarif-baseline.mjs as the same accept-with-review pattern for CodeQL. |
| 91 | pnpm-workspace.yaml security overrides are stale exact pins missing six packages both apps had to add — and `security:audit` sits inside `pnpm check`, so an unrelated advisory blocks all local work | defect | hume-archaeology | both | small | high | Convert every security-motivated override to a caret-range floor (`^1.20.6`, not `1.20.4`), keep true compatibility pins (glob, kysely, effect, '@esbuild-kit/core-utils>esbuild') exact and comment them as such, and add the floors both apps needed: brace-expansion (@1/@2/@5), fast-uri, launch-editor, postcss, js-yaml@4, esbuild@0.28, protobufjs@7, socket.io-parser. Never use a version-scoped selector for an advisory floor — it invalidates itself. Separately, remove `security:audit` from `check` and `check:ci` and rely on the dedicated CI Audit job (.github/workflows/code-quality.yml:159-176 already runs `pnpm security:audit` on its own), so the local gate stays deterministic and offline. |
| 92 | Node 24 is declared in four places but CI reads a hardcoded literal, and there is no drift check, mise config, or wrapper for a mismatched shell | missing-practice | hume-archaeology | hume | medium | high | Minimum: change .github/actions/setup-pnpm/action.yml to `node-version-file: '.node-version'` and drop the `node-version` input so there is one source of truth. Then port APP1's scripts/check-node-version.mjs and wire `check:node-version` into `check`/`check:ci`. mise.toml is two lines. bin/run and .devcontainer are optional but cheap, and the `./bin/run pnpm …` idiom is what makes agent sessions on a mismatched host work. |
| 93 | FieldOtp spreads `{...rest}` after its own onComplete handler, so any caller-supplied onComplete silently disables the component's autoSubmit behaviour | defect | hume-archaeology | hume | trivial | high | Move `{...rest}` to be the first prop on `<InputOTP>` and explicitly destructure `onComplete` (and any other prop the component wraps) out of `rest`, matching APP1's fix. Audit the sibling field components in src/platform/components/form/* for the same ordering hazard — src/platform/components/form/field-upload-input/index.tsx also differs between template and APP1 and is worth checking. Extend the existing field-otp browser spec with a case that passes both `onComplete` and `autoSubmit` and asserts the form still submits. |
| 94 | CI writes generated credentials into $GITHUB_ENV without ::add-mask::, so they are not scrubbed from workflow logs | defect | hume-archaeology | hume | trivial | high | Adopt APP1's `write_masked_env`/`write_env` helper split in .github/workflows/code-quality.yml, mask every generated credential, and switch AUTH_SECRET generation to `openssl rand -hex 32`. |
| 95 | `pnpm lint:sheriff` and `pnpm knip:deps` are documented as CI gates but no workflow runs them, and no workflow ever invokes `check` or `check:ci` | defect | hume-archaeology | hume | trivial | high | Add `lint:sheriff` and `knip:deps` to `check` and `check:ci` (APP1's shape), and add a CI step that runs one of the aggregates so the two lists cannot drift — today the workflow enumerates individual scripts and silently omits whatever is added to `check`. Alternatively, if Sheriff is deliberately excluded from the fast path, correct docs/security practices.md:23 and :48 to say so. |
| 96 | Sentry build integration passes no release name and no environment, so uploaded source maps are not associated with a deploy (surviving fragment of the refuted loadEnv finding) | missing-practice | hume-archaeology | hume | trivial | high | Pass `release: { name: process.env.VERCEL_GIT_COMMIT_SHA ?? process.env.SENTRY_RELEASE }` to `sentryTanstackStart` when available, and add APP1's `define` block so the client bundle carries the matching release/environment. Warn loudly (or fail the build) when VITE_SENTRY_DSN is set but org/project/token are not, so the no-op case is not silent. Add `hmr: { overlay: false }` under VITE_VISUAL_TEST. Do NOT adopt APP1's `process.env.X ?? loadEnv(...).X` rewrite — it is redundant given loadEnv's actual semantics. |
| 97 | React Cosmos ships broken and unusable — its renderer loads `/src/main.tsx`, which does not exist — yet 63 `.fixture.tsx` files, a workflow, two dependencies and an override are wired around it | dead-weight | iris-archaeology | iris | medium | high | Either fix Cosmos (add the missing src/main.tsx renderer entry and root index.html, and put `pnpm cosmos-export` in CI so it cannot rot again) or delete it as Iris did. Do not ship the current state: 63 fixture files, ~6 config files' worth of glob plumbing, a pnpm override and a risk-register entry all supporting tooling that cannot start. Deleting it also removes one of the template's standing risk-register entries. |
| 98 | `platform/env/config.ts`'s `isProd()` disagrees with the kernel's `isProdRuntimeEnvironment` for `NODE_ENV=staging`, silently disabling client-side HTTPS enforcement while every server guard stays on | defect | iris-archaeology | iris | trivial | high | Make platform/env/config.ts's `isProd()` implement the same rule as the kernel's `isProdRuntimeEnvironment` (only 'development'/'test' downgrade; anything else falls back to build-time PROD). The two files are deliberately kept apart by the platform-must-not-import-modules rule, so the fix is duplication — add a cross-reference comment in each and an architecture test asserting they agree over a table of NODE_ENV × PROD inputs, or the drift will recur. The same applies to the duplicated `isTruthy` and `runtimeEnv`. |
| 99 | `format-changed.mjs` forwards `.md` to oxfmt, but `.oxfmtrc.json` ignores `**/*.md` — the pre-commit hook fails on every docs-only commit | defect | iris-archaeology | iris | trivial | high | Remove `.md` from FORMAT_EXTENSIONS in scripts/format-changed.mjs (keep Iris's docblock explaining the coupling), and align lefthook.yml's glob with what oxfmt actually formats. Add a note that FORMAT_EXTENSIONS must stay a subset of what .oxfmtrc.json will format — the two files drifting is the whole bug. |
| 100 | oxfmt and oxlint walk `.claude/` and `.agents/`, making `pnpm check` unpassable on any working tree with agent tooling present — and the template ships `.claude/` itself | defect | iris-archaeology | iris | trivial | high | Add `.agents` / `.claude` to .oxfmtrc.json ignorePatterns and `.agents/**` / `.claude/**` to .oxlintrc.json ignorePatterns. The template ships .claude/ itself, so this is self-inflicted. Also settle the .gitignore policy for agent scratch directories. |
| 101 | `docs/strict-modular-monolith.md` states `src/start.ts` does not exist — it does, and it is the security spine. Both apps flagged it independently | doc-drift | iris-archaeology | both | trivial | high | Rewrite that section to describe the actual src/start.ts chain and, critically, state the hazard: defining src/start.ts REPLACES TanStack Start's default middleware rather than extending it, so CSRF must stay explicitly registered. Add a guardrail (semgrep rule or architecture test) asserting `createCsrfMiddleware` appears in src/start.ts. This is a doc whose being wrong could cost a fork its CSRF protection, and two independent readers had to work it out from source. |
| 102 | `.env.example` tells operators to set `TRUSTED_PROXY_DEPTH=0`, which the schema rejects — following the template's own comment fails startup | doc-drift | iris-archaeology | iris | trivial | high | Fix the .env.example comment, and port docs/security-rate-limiting.md. The rate-limiting scope note is the more valuable half: a globally-shared per-path auth bucket behind a CDN is a real security property of the template as shipped and is documented nowhere in it. Consider also configuring Better Auth's `advanced.ipAddress.trustedProxies` from the same TRUSTED_PROXY_DEPTH setting so the knob means what the docs say. |
| 103 | The module dependency graph generator omits every `import type` edge — 21 of 99 edges were invisible, and the published legend described a case that could never occur | defect | iris-archaeology | iris | trivial | high | Set `tsPreCompilationDeps: true` on the generator's own cruise() call (NOT in .dependency-cruiser.cjs). Port Iris's docblock. In a codebase where ports, ApplicationResult and branded IDs are all type-only, a graph that elides type imports is systematically wrong about exactly the layer boundaries it exists to document. Add an assertion that a known type-only dependency produces an edge, so the legend and the generator cannot diverge again. |
| 104 | Kernel APIs the Better Auth adapter needs are exported from no public gate, and auth deep-imports kernel internals 46 times across 25 files | missing-practice | iris-archaeology | iris | small | high | Export `getUserLanguage` and the `Database` type from kernel/backend.ts, convert auth.tsx to gate imports, and pin it with a depcruise rule. Then address the barrel hazard: kernel/backend.ts re-exporting the DB client makes it unsafe for anything reachable from the browser graph — split kernel's server gate so config/diagnostics do not drag `pg` along, otherwise the gate rule and the browser tests will keep fighting. Separately, the 46 remaining deep imports across auth are the real scope of work; a one-file depcruise rule is a beachhead, not a fix. |
| 105 | The template ships no root `CLAUDE.md` and no `CONTEXT.md`; both apps wrote both from scratch | agentification | iris-archaeology | both | small | high | Ship a root CLAUDE.md in the template (deferring to AGENTS.md/TESTING.md as authoritative, covering the command set, how to run ONE test in each layer, the four roots, public gates, composition, router context, and Result/Clock conventions) and a CONTEXT.md stub with instructions for forks to fill in their domain glossary. Include Iris's Working Rules verbatim — 'run targeted tests during a change, reserve pnpm test/check/verify for the end' targets the largest avoidable time sink in a repo whose full suite takes minutes, and 'fix the doc in the same change' would have prevented half the doc-drift findings in this report. |
| 106 | A fresh fork lost every dotfile, and the template's README points at the wrong scaffolder and lists technologies the fork does not use | missing-practice | iris-archaeology | iris | small | medium | Two separable fixes. (1) Certain: rewrite README's Technologies line (drop React Hook Form and oRPC, name @tanstack/react-form), replace the `pnpm create start-ui` section with a `degit` / `git clone --depth 1 && rm -rf .git` recipe that explicitly preserves dotfiles, and either write fork-specific docs or stop linking upstream's. (2) Likely: add a first-run checklist (copy .env.example, generate AUTH_SECRET, rename the app, delete the example domain, reset the risk register) and consider a scaffolder of the template's own. |
| 107 | Sign-up is open to any syntactically valid email address; the only gate is the all-or-nothing `VITE_AUTH_SIGNUP_ENABLED` flag, so an enabled deployment doubles as a mail relay | missing-practice | iris-archaeology | iris | small | high | Port `AUTH_ALLOWED_EMAIL_DOMAINS` into the template's auth module with both enforcement points, defaulting to unset/permissive (Iris defaulted to its own domain, which a template cannot). The mail-relay framing is why this belongs in a starter: a template whose default posture lets an unauthenticated caller trigger arbitrary outbound email through the operator's Resend account is a liability, and the fix is ~120 lines. Carry Iris's caveat that the admin create-user form and the seed script write through Drizzle directly and are deliberately uncovered. |
| 108 | Every security-floor override in `pnpm-workspace.yaml` is an EXACT pin, so each goes stale silently the moment a new advisory lands above it | defect | iris-archaeology | iris | trivial | high | Split the overrides block in two with comments — compatibility pins stay exact, security floors become caret ranges. Port Iris's exact wording, including the postcss cautionary note and the 0.x caret-caps-the-minor explanation. This is a five-minute edit that converts a recurring manual audit chore into an automatic one, and it directly reduces the recurrence rate of the risk-register time bomb. |
| 109 | The template cannot boot from its own `.env.example`, and offers no first-run path that fixes it | missing-practice | iris-archaeology | — | trivial | high | Add a `pnpm setup` script that copies .env.example to .env and mints AUTH_SECRET via `npx @better-auth/cli secret` (or node crypto), and document it as step one in the README. Fix the .env.example comment typo. More broadly: add a smoke test to CI that does `git clone --depth 1 && pnpm install && pnpm setup && pnpm check && pnpm build` from a clean checkout — every critical finding in this report would have been caught by it, and its absence is why five separate first-run failures coexist in one release. |
| 110 | The template depends on a three-month-old pinned Nitro NIGHTLY, and neither app dared change it | modernization | iris-archaeology | — | medium | high | Two actions independent of whether Nitro 3 GA exists. (1) Add a scheduled CI job (weekly) that checks whether a Nitro 3 GA or a newer nightly exists and opens an issue/PR, so this does not depend on the expired manual review that is currently failing. (2) Document in the register entry what a fork should do if the pinned nightly is ever unpublished — a nightly tag is not a durable artifact, and three repos currently have no fallback. Flagged as a template-only item: notably, this is the one supply-chain risk both apps left alone, which suggests forks feel unable to move it and the template must own the decision. |
| 111 | Default log sanitizer's email regex redacts every scoped-package path in stack traces; `name` redaction destroys `sdk.name`/`service.name`; recursion has no depth bound | defect | platform-runtime | hume | trivial | high | Remove `'name'` from `DEFAULT_SENSITIVE_LOG_KEYS`; move `email`/`phone` behind an opt-in option rather than the default; add `MAX_SANITIZED_DEPTH` with a `[MaxDepth]` sentinel matching the existing array/non-plain-object guards. Consider APP1's stronger position (credential-bearing keys only) with a doc comment stating the tradeoff. |
| 112 | Browser-mutation-guard security events are logged into a no-op telemetry sink, so a rejected cross-origin mutation is invisible in production | defect | platform-runtime | iris | trivial | high | Change src/start.ts:103-105 to `createTelemetryLogger({ telemetry: telemetryProxy })` and widen the accessor to `Pick<Logger, 'warn' \| 'error'>` so other middleware and the auth-session failure path can use it. Add a counter metric alongside the warn so the event survives log-level filtering. Add a Semgrep rule forbidding `createNoOpTelemetry()` outside tests and outside the runtime default. |
| 113 | `getAuthUseCases()` silently constructs a SECOND Better Auth instance, because `buildAuthUseCases` always passes overrides to the cached factory | defect | platform-runtime | neither | small | high | Change auth.ts:142 to pass `undefined` when there is nothing to override, e.g. `const authInstance = getAuth(overrides?.authEmailPort ? { authEmailPort } : undefined);`. Add a test asserting `getAuth()` is the same instance the use cases hold. Consider making `createCachedFactory` treat an all-undefined/empty override object as 'no overrides'. |
| 114 | `getKernel(overrides)` always builds the default kernel first, so overriding `db` still requires DATABASE_URL and opens a real pg Pool | defect | platform-runtime | neither | small | high | Make the override path lazy — resolve each default only when its override is absent (`const db = overrides.db ?? getDefaultDbClient();`) rather than building the full base kernel. Export `__resetDefaultDbClient()` from `src/modules/kernel/testing.ts` that clears `defaultDb`/`globalForDb.db` and closes the pool, and call it from `__resetKernelComposition()`. Treat `{}` as 'no overrides'. |
| 115 | Same-origin browser-mutation guard derives the trusted origin from `request.url`, which is the internal origin behind a TLS-terminating proxy | defect | platform-runtime | neither | small | low | Once a runtime canonical origin exists, compare Origin/Referer against it, falling back to `request.url` only when unconfigured. At minimum accept a scheme-insensitive host match when `X-Forwarded-Proto: https` is present, and emit a distinct rejection reason. Add an integration test replaying an internal `http://` request URL with an external `https://` Origin. |
| 116 | Dead weight the apps had to delete: object storage / uploads / S3 sources baked into shared platform code, plus a zero-importer kernel `config/env.ts` bridge | dead-weight | platform-runtime | both | medium | high | (1) Delete `src/modules/kernel/infrastructure/config/env.ts` and investigate why knip misses it. (2) Make `VITE_S3_BUCKET_PUBLIC_URL` optional and remove `s3BucketPublicUrl`/`sourceOriginFromUrl` from `ContentSecurityPolicyOptions`; give `buildContentSecurityPolicy` an `extraSources?: Partial<Record<'img-src'\|'connect-src'\|'media-src'\|'frame-src', string[]>>` that `src/start.ts` populates from app config. (3) Have `src/start.ts` pass `protectedPathnames` into `shouldProtectBrowserMutation` — the parameter already exists but is never used. (4) Move object storage + upload UI behind an opt-in module or ship a removal checklist. |
| 117 | `shouldProtectBrowserMutation` accepts a `protectedPathnames` override that `src/start.ts` never passes — both apps forked the platform file to change one path | missing-practice | platform-runtime | both | trivial | high | Have `src/start.ts` own the protected-path list (and a CSP extra-sources list) and pass them into the platform helpers. Move `/api/upload` out of the platform default and into the app-level list alongside the upload feature. |
| 118 | `.claude/settings.json` carries only an `attribution` block — no permissions allow/deny, no env, no hooks — and one of the two apps never received `.claude/` at all | agentification-missing | research-agentic | — | small | high | Expand `.claude/settings.json` with (a) `permissions.allow` for the read-only/idempotent scripts an agent runs constantly — `Bash(pnpm check)`, `Bash(pnpm check:*)`, `Bash(pnpm test:*)`, `Bash(pnpm lint*)`, `Bash(pnpm typecheck)`, `Bash(pnpm format:changed*)`, `Bash(pnpm depcruise)`, `Bash(pnpm architecture:graph*)`, `Bash(git status)`, `Bash(git diff *)`; (b) `permissions.deny` converting AGENTS.md:119 and AGENTS.md:48 into enforcement — `Edit(drizzle/migrations/**)`, `Write(__visual_snapshots__/**)`, `Read(./.env)`, `Write(./.env*)`. Add `.claude/settings.local.json` to `.gitignore`. Separately, make `.claude/` discoverable — add it to the README 'Setup your IDE' section next to the `.vscode`/`.zed` instructions, so a fork does not lose it the way iris did. |
| 119 | No SessionStart/bootstrap hook for fresh cloud or container sessions; hume-demo invented the equivalent (devcontainer + `bin/run` + mise), iris did not | agentification-missing | research-agentic | hume | small | medium | Promote hume-demo's trio upstream verbatim — `bin/run` (chmod +x), `mise.toml`, and lefthook commands routed through `./bin/run` — since a wrong ambient Node major is a silent, recurring failure for any agent shell. Add `.devcontainer/devcontainer.json` with the same postCreateCommand. Then, and separately, add a `.claude/hooks/session-start.sh` registered under `hooks.SessionStart` that is idempotent, fast, and no-ops on local machines; validate the exact env-var contract against current Claude Code docs before shipping, since this verification could not confirm it. |
| 120 | No PostToolUse formatting hook, though lefthook already formats the same globs at commit time | modernization | research-agentic | — | trivial | medium | Add a `PostToolUse` hook with `matcher: "Edit\|Write"` running `.claude/hooks/format-edited.sh`, which reads `tool_input.file_path` from stdin JSON, filters to the exact glob lefthook already uses (`*.{js,ts,cjs,mjs,jsx,tsx,json,md,css}`), and runs `pnpm exec oxfmt` on that single path. Silent on success so it costs zero context. Reuse `scripts/format-changed.mjs` logic rather than duplicating the glob, so the hook and lefthook cannot diverge. |
| 121 | `pnpm test:affected` exists but is bound to nothing — verification is advisory prose in AGENTS.md | agentification-missing | research-agentic | — | small | medium | Add a `Stop` hook running `.claude/hooks/verify-turn.sh` = `pnpm exec tsc -p tsconfig.json --noEmit && pnpm test:affected`, early-exiting when `git diff --name-only` shows no changes under `src/` or `tests/`. Explicitly keep full `pnpm check` out of it. Document the ladder in AGENTS.md so the tiers are named rather than implied: turn gate = typecheck + affected tests; pre-handoff = `pnpm check`; pre-merge = `pnpm verify`. Confirm the Stop-hook exit-code contract against current docs before shipping. |
| 122 | No first-party `.claude/skills/` for the template's golden paths; the apps' skills evidence is weaker than reported | agentification-missing | research-agentic | hume, iris | medium | high | Ship first-party `.claude/skills/` for the template's golden paths — `new-module` (scaffold `src/modules/<capability>/` gates + layers + a `src/composition/<capability>.ts` entry), `add-migration` (edit Drizzle schema → `pnpm db:generate` → `pnpm check:migrations`, never touch `drizzle/migrations`), `verify-task` (the gate ladder). Move the corresponding prose out of AGENTS.md so always-on context shrinks. Do NOT bless the `skills-lock.json` pattern — iris proves it ships locks with no files; if third-party skills are wanted, vendor them as real committed files and add a `pnpm check` step asserting every lock entry has a file on disk. |
| 123 | Committed module-dependency artifact is Graphviz DOT only; the generated Markdown/Mermaid rendering is neither committed nor staleness-checked | agentification-missing | research-agentic | — | trivial | high | Add `ARTIFACT_FILENAMES.markdown` to `CHECKED_ARTIFACT_FILENAMES` at scripts/generate-module-dependency-graph.ts:39, commit `docs/architecture/module-layer-dependencies.md`, and link it from AGENTS.md's architecture section as the readable view of the actual graph. Additionally emit a `module-graph.json` of `{module, layer, allowedImports[], gates[]}` derived from `.dependency-cruiser.cjs` — that, not the Mermaid diagram, is what answers the 'what may X import' question structurally. All three stay verified for free since `architecture:graph:check` is already in `pnpm check`. See the separate Graphviz finding: adding markdown to the checked set is only safe once `--check` no longer depends on a system `dot` binary. |
| 124 | AGENTS.md is a flat 147-line always-on file; path-scoped rules exist as a mechanism but are used only by the one broken rule, and `.claude/rules/` is referenced nowhere | agentification-missing | research-agentic | — | medium | medium | Split `.claude/rules/` by path with working `paths:` globs — `platform.md` (`src/platform/**`), `routes.md` (`src/routes/**`), `composition.md` (`src/composition/**`), `migrations.md` (`drizzle/**`, `src/**/schema.ts`), `presentation-schema.md` (`src/modules/*/presentation/schema.ts`) — moving the matching AGENTS.md:107-119 bullets out of the always-on file. Add a `## Agent configuration` section to AGENTS.md and to README's 'Setup your IDE' block that names `.claude/rules/`, `.claude/settings.json`, and CLAUDE.md, states the ownership rule for forks, and is verified by the same glob-coverage check proposed for the testing.md finding. |
| 125 | The template's AGENTS.md hardcodes an OpenAI-Codex-only instruction, and it propagated verbatim into both apps | template-defect | research-agentic | — | trivial | high | Rewrite AGENTS.md:37 tool-neutrally — 'inspect the affected flow in a browser (your agent's built-in browser, Playwright UI mode via `pnpm e2e:ui`, or a manual browser at localhost:3000)' — and move any genuinely Codex-specific guidance into a Codex-only file. This is a prerequisite for the CLAUDE.md `@AGENTS.md` import recommendation. |
| 126 | Both apps independently created a `CONTEXT.md` domain glossary; the template has none and no convention for one | template-missing-practice | research-agentic | hume, iris | small | high | Ship a `CONTEXT.md` stub in the template — a short header explaining its role (ubiquitous language: term, definition, where it appears in code) plus the template's own book/genre/user demo vocabulary as a worked example — and reference it from AGENTS.md alongside TESTING.md. Add iris's maintenance rule to AGENTS.md: when a domain term is renamed, update CONTEXT.md in the same change. Note that hume-demo's vendored `domain-modeling` skill ships a `CONTEXT-FORMAT.md` (/home/user/hume-demo/.agents/skills/domain-modeling/CONTEXT-FORMAT.md) — worth reading before fixing the template's format. |
| 127 | AGENTS.md's canonical-commands table omits bootstrap (`pnpm install`, `dk:init`, `db:init`) — an agent has no documented path to a runnable app; both apps added it | template-missing-practice | research-agentic | hume, iris | trivial | high | Extend AGENTS.md's canonical-commands table with a bootstrap block — `pnpm install`, `pnpm dk:init` (Docker: Postgres + MinIO + Maildev), `pnpm db:init` (push + seed, with the seeded account emails), `pnpm exec playwright install --with-deps` — and add the Docker and Graphviz prerequisites plus the `AUTH_SECRET` requirement. Cross-link README.md from AGENTS.md so the dependency is bidirectional. |
| 128 | `pnpm lint:sheriff` and the CodeQL scripts are deliberately outside `pnpm check` and are named nowhere in AGENTS.md — so an agent never runs the Sheriff boundary check at all | template-defect | research-agentic | hume | trivial | high | Add `pnpm lint:sheriff` and the `pnpm codeql:*` scripts to AGENTS.md's canonical-commands table with an explicit 'not in `pnpm check` — run when touching module boundaries or `.github/codeql/**`' note, i.e. adopt hume-demo/CLAUDE.md:45 upstream. Better: add `lint:sheriff` to `pnpm check` if its runtime allows, since a boundary enforcer excluded from the default gate in a repo whose entire value proposition is enforced boundaries is a policy inversion worth revisiting. |
| 129 | @babel/core and @types/babel__core are undeclared peer dependencies of the React Compiler setup | defect | research-stack | neither | trivial | high | Add `"@babel/core": "^7.29.7"` and `"@types/babel__core": "^7"` to devDependencies, matching what the lockfile already resolves. This makes the React Compiler toolchain pinnable and reproducible instead of dependent on pnpm's `autoInstallPeers` default, and makes the package visible to `pnpm audit` and the license/SBOM checks in `security:audit`. |
| 130 | oxfmt sorts imports natively now; the template still routes import sorting through a JS plugin inside oxlint | modernization | research-stack | hume | small | high | Port a generalized `sortImports` block into the template's `.oxfmtrc.json` using the template's own vocabulary — group `@/platform/lib`+`@/platform/hooks`, then `@/platform/components`, then `@/modules`, then `@/composition`/`@/app`/`@/routes` — rather than copying hume's `@/data`/`@/store`/`@/container` groups verbatim. Delete the `eslint-plugin-simple-import-sort` jsPlugin entry and its four rule blocks from `.oxlintrc.json` and drop the devDependency (package.json:264). Bump oxfmt 0.56.0 → 0.62.0 at the same time. |
| 131 | tsconfig.json never enables `strict`, and carries a TS6-era `ignoreDeprecations` escape hatch | missing-practice | research-stack | hume | medium | high | Set `"strict": true` and drop the three flags it subsumes. Add `"verbatimModuleSyntax": true` and `"isolatedModules": true` for determinism under the Rolldown/oxc per-module transform. Add an explicit `"lib"`. Remove `ignoreDeprecations: "6.0"` and `baseUrl`. Adopt APP1's documented `"types": ["node"]` allowlist. Do this before the TS7 migration so the two remain separately bisectable. Expect real churn: enabling `useUnknownInCatchVariables` will surface every SDK-boundary catch, which is the point. |
| 132 | shadcn/Base UI integration is a version and provenance generation behind | modernization | research-stack | neither | small | medium | Bump @base-ui/react 1.6.0 → 1.7.0 and check the 1.7.0 release notes for the OTPField stabilization before deciding whether `input-otp@1.4.2` plus the hand-maintained `input-otp.tsx` can be dropped. Add `shadcn` as a pinned devDependency so the generating CLI version is recorded rather than implied by `npx shadcn@latest`. Most valuable: record provenance for the 85 files in src/platform/components/ui — a manifest or a header comment marking registry-derived vs hand-written — because that is the actual friction an agent hits when asked to add a component. |
| 133 | engines.node: "24.x" combined with engineStrict: true is a HARD install failure once Node 26 reaches LTS, and pnpm is 11 minors behind | defect | research-stack | hume | trivial | high | Widen `engines.node` to `">=24"` (or `"24.x \|\| 26.x"`) ahead of the October 2026 Node 26 LTS promotion — with `engineStrict: true` set, leaving it at `24.x` converts an LTS upgrade into an install-blocking failure for every fork. Adopt APP1's `check:node-version` script into `check`/`check:ci` and have it verify all seven declaration sites agree, including the workflow file. Bump `packageManager` to pnpm 11.20.0; ignore the 12 RC. |
| 134 | Non-TanStack tooling has drifted, including a full major on dependency-cruiser — the tool that enforces the architecture | modernization | research-stack | hume | small | high | Prioritize dependency-cruiser 17 → 18 specifically: a major in the tool that enforces every hexagonal boundary must be validated inside the template, not discovered in a fork — and APP1 already ran it at ^18.1.0, so the migration risk is already partly retired. Also bump jscpd 4 → 5 (APP1 did) and tsx. Bump the rest routinely; hold Vitest at 4.x. See the separate finding on the Dependabot config, which already exists and is the reason this drift is surprising. |
| 135 | @tanstack/zod-adapter declares a Zod 3 peer and is force-fitted onto Zod 4 by a pnpm override; it is 3 minors stale and APP1 removed it | defect | research-stack | hume | small | high | Evaluate dropping @tanstack/zod-adapter from the template. Modern TanStack Router accepts Standard Schema validators directly, and Zod 4 implements Standard Schema, so `validateSearch: schema.parse` (APP1's approach) removes both the stale package and the `peerDependencyRules` override that currently masks a genuine peer mismatch. The one thing to replace deliberately is `fallback()`, which four template routes import from the adapter — verify whether Router now exposes an equivalent before removing. If the adapter is kept, at minimum add a comment beside the peerDependencyRules entry explaining why forcing Zod 4 onto a `^3.23.8` peer is believed safe, so a fork does not inherit a silent compatibility assertion. |
| 136 | The template's Dependabot config is well-designed but demonstrably inert, and has no ignore-list for the pins that must not be auto-bumped | defect | research-stack | neither | trivial | medium | Diagnose why Dependabot has never opened a PR (check the repository's Insights → Dependency graph → Dependabot tab for failed runs; the `minimumReleaseAge` and `engineStrict` workspace settings are the most likely culprits since Dependabot resolves with its own pnpm invocation). Then add an `ignore` block for `typescript` (until the oxlint consolidation lands) and a comment noting that `nitro` cannot be managed by Dependabot because it is an `npm:` alias, so it needs a manual review cadence. A grouped config that never fires is worse than none, because it creates the appearance of managed dependencies — which is precisely the state both forks inherited. |
| 137 | Sheriff is a declared architecture guardrail that the template never runs in `check`; APP1 wired it in | missing-practice | research-stack | hume | trivial | high | Add `lint:sheriff` to the template's `check` and `check:ci`, as APP1 did. If it is intentionally excluded — e.g. because it is slow or currently failing — say so in AGENTS.md the way CodeQL's exclusion is justified, because right now the docs present four boundary enforcers and only three run. Run `pnpm lint:sheriff` against the template first: a guardrail that has not been in the gate since it was added may have accumulated violations. |
| 138 | No fork-update mechanism; actions-template-sync is the only surveyed tool that works across unrelated histories | missing-practice | research-starters | neither | small | high | Ship `.github/workflows/template-sync.yml` in the template (so future forks inherit it) using AndreasAugustin/actions-template-sync@v2, monthly cron + workflow_dispatch, `is_dry_run: true` for the first rollout, `pr_labels: template-sync`. Ship a `.templatesyncignore` scoped to the shared substrate only — sync `.github/workflows/**`, `scripts/**`, `.semgrep.yml`, `.dependency-cruiser.cjs`, `sheriff.config.ts`, `src/platform/**`, `tests/architecture/**`; ignore `src/modules/**`, `src/routes/**`, `src/composition/**`, `drizzle/**`, `src/app/i18n/**`, `README.md`, `CLAUDE.md`, `package.json`, `stryker.*.config.mjs`, `tsconfig.stryker.*.json`. Document the manual fallback (`git remote add template …; git fetch template; git merge template/main --allow-unrelated-histories`) in AGENTS.md. |
| 139 | Template ships no CLAUDE.md; both apps wrote one independently, and the .claude/rules the template does ship hardcode optional subsystems and go stale on contact | agentification | research-starters | both | small | high | (1) Ship a template CLAUDE.md whose stable half is lifted from the two apps' independently-written versions (targeted-tests-first discipline, never-branch-unless-asked, fix-the-doc-in-the-same-change). (2) Split .claude/rules/architecture.md into a core file plus per-subsystem fragments (drizzle.md, better-auth.md, email.md) that the demo/subsystem-removal step deletes together with the subsystem, so a fork can never carry a rule for code it does not have. (3) Add a .claude/skills entry wrapping `pnpm gen:module` once the generator exists. |
| 140 | Ceremony is not calibrated: the template demonstrates exactly one module shape, and both apps needed thinner ones | modernization | research-starters | both | small | medium | Name three tiers in .claude/rules/modules.md and AGENTS.md — `presentation-only` (domain + presentation + index/presentation/testing, exactly iris's launch-workspace), `service` (adds application + infrastructure + transport, exactly hume's site-access), `full` (adds client/server/backend + factory) — make `pnpm gen:module --tier=` accept them, and convert the shipped `genre` module down to the thinnest tier that still works so the template demonstrates more than one shape. |
| 141 | The template's own package.json points at the wrong GitHub repository — homepage, bugs and repository all reference hbmartin/start-ui-web, which is not this repo | defect | research-starters | neither | trivial | high | Fix package.json:5, :6-8 and :28-31 to https://github.com/hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal, and change name/description at package.json:2-4 at the same time. Add a check to `pnpm check` that asserts package.json.repository.url matches `git remote get-url origin` — a five-line script that permanently prevents this class of drift in the template and in every fork. |
| 142 | Governance documents route contributors and harassment reports to the upstream organization, and one app shipped them verbatim | dead-weight | research-starters | one | trivial | high | Rewrite CONTRIBUTING.md for this fork (drop the BearStudio-assignment step and the en/fr translation policy, keep the AI-attribution section, which is good and fork-general). Replace the CODE_OF_CONDUCT.md:63 contact with a `<CONTACT_EMAIL>` placeholder and add it to the init/demo-removal checklist so it fails loudly if unreplaced. Add SECURITY.md — the template runs twelve security-oriented CI workflows (codeql, semgrep, osv-scanner, osv-scanner-full, detect-secrets, dependency-review, supply-chain) but ships no vulnerability-disclosure policy and no .github/ISSUE_TEMPLATE, both of which I confirmed absent. |
| 143 | The template's deployment knowledge lives only in README prose, and the README is the one file every fork rewrites wholesale | missing-practice | research-starters | both | medium | high | (1) Move README.md:194-350 into docs/deployment.md and leave a one-line pointer in README, so a fork's README rewrite cannot delete it. (2) Commit the artifacts the guide already describes in prose: render.yaml (verbatim from README.md:334-350) and a multi-stage Dockerfile ending `CMD ["node", ".output/server/index.mjs"]` — package.json:39 already defines exactly that as the `start` script, and a .dockerignore already exists. (3) Promote iris's origin-resolution logic upstream: port src/modules/kernel/infrastructure/config/app-url.ts into the template's kernel config and fold the VERCEL_URL-vs-VERCEL_PROJECT_PRODUCTION_URL reasoning from iris README:254 into docs/deployment.md. It is a genuine auth-cookie bug class discovered downstream that the template should own. (4) Add .github/workflows/deploy.yml gated on `pnpm verify`. |
| 144 | hume-demo still carries dead demo-domain code and stale guardrail regexes 123 commits in — evidence the entanglement is not self-healing | dead-weight | template-friction | iris | small | high | Diagnostic for the template rather than actionable in the apps. Add a `tests/architecture` assertion that every module name appearing in `.dependency-cruiser.cjs`, `sheriff.config.ts`, `.semgrep.yml`, and `.github/workflows/mutation-testing.yml` corresponds to an existing directory under `src/modules/`, so stale references become a hard failure in any fork. |
| 145 | Semgrep's two branded-ID guardrails are a closed enumeration of 27 type names, so every domain type a forker adds is silently unguarded | defect | template-friction | neither | small | high | Replace the closed enumeration with something maintenance-free: either derive the type list at check time from `src/modules/*/domain/*.ts` and `src/modules/kernel/domain/ids.ts` in a small script, or restate the rule structurally in semgrep's TypeScript mode (ban `as` casts to any `z.infer<typeof z*Schema>` brand) instead of `languages: [generic]` regex matching. At minimum add an architecture test asserting every `zBrandedNonEmptyString<'X'>` in the codebase appears in both regexes. |
| 146 | Upstream branding is spread across 45 files and some of it is load-bearing infrastructure, not cosmetics | missing-practice | template-friction | iris | small | high | Introduce one `APP_SLUG` constant (or read `name` from package.json) and derive the OTel tracer/meter/logger names, the `OTEL_SERVICE_NAME`/`VITE_OTEL_SERVICE_NAME` defaults, the CSP nonce bridge key, and the migration advisory-lock namespace from it. Ship a `pnpm rename <slug> <"Display Name">` script for the literals. Independently, fix `tests/unit/platform/http/security-headers.unit.spec.ts:271-273` to derive its slice offsets from `CSP_NONCE_PLACEHOLDER.length` — that test is only correct by coincidence of the current brand string's length and is a template defect regardless of renaming. Document that the migration-lock namespace must not change while old and new code run concurrently. |
| 147 | Four locales × ten i18n namespaces that both apps kept but neither used for its own features | dead-weight | template-friction | neither | small | medium | Reduce shipped locales to `en` plus one demonstration locale (keep the plumbing, drop the ~20 Arabic/Swahili JSON files the template cannot keep accurate). Replace the four hand-maintained `index.ts` barrels with a glob loader. Add an AGENTS.md note stating the i18n layer is optional and how to opt a module out. |
| 148 | Stryker configuration is 17 root files and 40 of 120 package.json scripts of mechanical duplication | modernization | template-friction | neither | small | high | Collapse to a single `stryker.module.config.mjs` reading the target module from an env var or CLI arg and computing `mutate`, `testFiles`, `moduleName`, and the tsconfig from it; or replace the eight per-module tsconfigs with one `tsconfig.stryker.json` using `include: ['src/modules/**/{domain,application}/**/*.ts']`. Reduce the 40 scripts to `test:mutation`, `test:mutation:module`, and the `:fast`/`:dry` variants. Derive the CI matrix at `.github/workflows/mutation-testing.yml:21` from `fs.readdirSync('src/modules')`. The knip.jsonc:5-8 workaround can then be deleted. |

## Low

| # | Finding | Kind | Area | Apps | Effort | Conf. | Recommendation |
|---|---|---|---|---|---|---|---|
| 1 | Nothing exists for non-Claude agents beyond AGENTS.md, and AGENTS.md's only discovery link is buried in a testing paragraph of the README | agentification | Agentification | hume | trivial | high | Adopt APP1's layout: .agents/ holds neutral content, .claude/skills symlinks into it. Add a 'Working with AI agents' section near the top of README.md linking AGENTS.md, CLAUDE.md, CONTEXT.md, docs/adr/, and docs/architecture/module-layer-dependencies.md. Add .github/copilot-instructions.md as a ~15-line POINTER ('Read AGENTS.md and CLAUDE.md; run `pnpm format:changed && pnpm check:fast && pnpm test:affected`; cross-module imports go through public gates only') rather than a fourth full copy — a fourth copy would worsen the triplication drift documented above. |
| 2 | Template ships no devcontainer, no toolchain wrapper, and no pin-drift check; APP1 added all four after hitting Node-version mismatch | missing-practice | Agentification | hume | small | high | Ship a .devcontainer/devcontainer.json modelled on APP1's (base:ubuntu-24.04, node feature pinned to 24 + pnpm 11.9.0, node_modules volume, playwright chromium install in postCreate, forwardPorts 3000) and a scripts/check-node-version.mjs asserting .nvmrc / .node-version / package.json engines / the CI setup-node version all agree. Add a '## Toolchain' section to AGENTS.md — it currently has none (`grep '^## ' AGENTS.md` confirms). The `bin/run` mise wrapper is more opinionated; make it optional. |
| 3 | No scratch-file convention: agent working files are tracked in APP2's committed repo root | agentification | Agentification | — | trivial | high | Add a `.scratch/` convention: create `.scratch/README.md` explaining it is for agent and human working files, add `/.scratch/` plus `_tmp_*` and `compass_artifact_*` to .gitignore, add `.scratch` to the .oxfmtrc.json / .oxlintrc.json ignorePatterns alongside `.claude`/`.agents`, and state in AGENTS.md and CLAUDE.md that all experiment output, research dumps, and one-off scripts go under `.scratch/<topic>/`. |
| 4 | README's Getting Started points forks at BearStudio's upstream scaffolder, and CONTRIBUTING.md still routes contributors to BearStudio's issue process — inherited onboarding that is wrong for this fork | dead-weight | Agentification | hume | trivial | high | Rewrite README.md:23-29 to describe cloning/forking THIS repo (or use-this-template), not `pnpm create start-ui`. Genericize CONTRIBUTING.md:9 (drop the BearStudio reference) and move the '## AI usage' section (CONTRIBUTING.md:17-25) into the new CLAUDE.md or AGENTS.md so it survives a fork that deletes CONTRIBUTING.md — as APP1 did. |
| 5 | No nested / directory-scoped AGENTS.md anywhere: one 1,778-word root file carries per-layer rules for 7 modules × 5 layers | modernization | Agentification | — | medium | medium | Split the directory-specific rules out of the root AGENTS.md into `src/modules/AGENTS.md` (module shape + gates + layer table, replacing .claude/rules/modules.md's duplication), `src/composition/AGENTS.md` (wiring rules), and `tests/AGENTS.md` (layer map + escalation ladder, replacing the broken .claude/rules/testing.md). Keep the root AGENTS.md as commands + boundaries + policy. Wire the docs-drift script recommended elsewhere to assert every nested AGENTS.md sits in a directory that exists. |
| 6 | APP1 modernized the oxc toolchain (TypeScript lint config, split lint task, a decision doc) while the template stays on `.oxlintrc.json` | modernization | Agentification | hume | small | medium | Read /home/user/hume-demo/docs/oxc-tooling-decisions.md and decide deliberately: either adopt oxlint.config.ts and the `lint:oxlint` / `lint:typed-sonar` split, or record why the template stays on JSON. Either way this is a good first ADR once docs/adr/ exists, since the alternative-closing reasoning is already written. |
| 7 | Five jobs have no `timeout-minutes`, and the workflow-policy test does not check for it | defect | CI/CD, supply chain, and release engineering | — | trivial | high | Add `timeout-minutes` to the three settable jobs (cosmos-pages build/deploy, semgrep) and add exactly one new assertion to the workflow-policy test: every job that defines `steps` declares `timeout-minutes`. Do not add a top-level-permissions assertion. |
| 8 | Stryker mutation testing and JiTTest are `workflow_dispatch`-only, so two of the template's advertised quality gates never execute automatically anywhere | dead-weight | CI/CD, supply chain, and release engineering | — | trivial | high | Give mutation-testing.yml a `schedule:` trigger (weekly is enough for a 45-minute job) so mutation scores are actually tracked, or move the Stryker configs behind a documented opt-in and stop listing mutation testing as an active gate. For jittest.yml, either document the required Vercel AI Gateway secret in README/AGENTS or move it out of the template into an optional add-on — a fork that never configures the credential is carrying a 60-minute workflow it cannot run. |
| 9 | Hand-written staleness warnings decay too: hume's CLAUDE.md warning about AGENTS.md is itself now half-wrong | doc-drift | Documentation, onboarding, and doc-drift | neither | trivial | high | Do not solve template doc-drift with prose warnings; they rot as fast as what they warn about. Solve it mechanically — see the docs-lint finding below, which I built and ran to measure feasibility. |
| 10 | TESTING.md points at a scheduled mutation-testing CI workflow that has no schedule, and AGENTS.md undercounts what `pnpm test` runs | doc-drift | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Add `schedule: [{ cron: '0 3 * * 1' }]` to .github/workflows/mutation-testing.yml — weekly matches the 45-min-per-scope cost — or change TESTING.md:78 to 'or by dispatching the Mutation Testing workflow manually'. Fix AGENTS.md:16 to 'Vitest unit, browser, and integration projects' in the same pass. |
| 11 | Property tests use a single frozen seed and are discovered by a raw substring scan of file text | defect | Testing strategy, test infrastructure, and quality gates | neither | small | high | Read the seed from the environment with the current value as default (`seed: Number(process.env.PROPERTY_SEED ?? 0x5eed)`) and print it on failure, so CI can randomize while local runs stay reproducible. Replace the substring discovery at run-property-tests.mjs:22-28 with APP1's `extractModuleSpecifiers` AST helper — a direct port-back. Change the empty-set case at :30-33 to exit 0 with a notice. |
| 12 | The lefthook pre-commit format glob covers fewer extensions than `oxfmt --check .`, and the gap sits exactly over the files that configure the test suite | defect | Testing strategy, test infrastructure, and quality gates | iris | trivial | high | Adopt APP2's glob at lefthook.yml:6 but keep `md` (the template has substantial docs/ plus AGENTS.md and TESTING.md, all formatted). Better: derive the hook glob from the same source as format:check's scope, or add a unit test under tests/unit/scripts/ asserting the lefthook glob's extension set is a superset of what `oxfmt --check .` visits. |
| 13 | Flakiness hygiene is otherwise clean — no skipped/todo/only tests, forbidOnly in CI — but Playwright retries twice in CI while Vitest's zero-retry policy is implicit and undocumented | missing-practice | Testing strategy, test infrastructure, and quality gates | neither | trivial | high | Set `retry: 0` explicitly in vitest.config.ts's browser project with a one-line comment pointing at AGENTS.md:44 and at the scrollbar-gutter fix as the worked example of fixing a flake at its cause, and add a TESTING.md sentence stating the split (Playwright retries twice in CI to absorb network/dev-server startup; Vitest never retries). |
| 14 | `tests/setup.browser.ts` hardcodes the full `envClient` object in a `vi.mock`, so every client env-var change forces an edit to a shared test setup file — both apps edited it | defect | Testing strategy, test infrastructure, and quality gates | neither | small | high | Derive the mocked `envClient` from the Zod client-env schema's parsed defaults (or a single exported `TEST_CLIENT_ENV` constant in tests/support/) and import it from both tests/setup.browser.ts and tests/server/test-setup.ts, so adding a variable is a one-line change in one place. |
| 15 | The manifests-plus-pins pattern for git-managed external resource state is generic and undocumented in the template | missing-practice | app-inventions | hume | medium | medium | Lift `scripts/hume/canonical-json.ts` to `scripts/lib/canonical-json.ts` — it is already provider-agnostic and useful for any hash-pinned artifact (including the `skills-lock.json` hash check proposed above). Add a template `docs/` note or ADR describing the manifests-plus-pins shape with pointers to where a `<resource>:validate` script hooks into `pnpm check`. Do not ship a generic sync CLI. |
| 16 | The template's brand name is hardcoded across `src/platform`, `src/modules/kernel` and `src/composition`, so every fork edits framework-layer source to rename itself | friction | app-inventions | both | small | high | Introduce a single `APP_NAME` (server) / `VITE_APP_NAME` (client) with the template's own name as the default, and consume it in `get-page-title.ts`, `logo.tsx`, `email-footer.tsx`, and as the fallback in `telemetry.ts:120` / `config.ts:97`. Make `otel-adapter.ts:22-24` read the resolved service name instead of the three literals. Derive `MIGRATION_LOCK_NAMESPACE` from it too. Add the file list to a `docs/` rename checklist for anything that genuinely cannot be parameterized. |
| 17 | Both apps had to add agent-tooling directories to formatter ignore lists the template lacks; only APP2 also fixed the linter | missing-practice | config-divergence | both | trivial | high | Add `.agents/**` and `.claude/**` to `.oxlintrc.json` `ignorePatterns`, and `.agents`, `.claude`, `skills-lock.json` to `.oxfmtrc.json` `ignorePatterns`. Ship a minimal `CLAUDE.md` stub pointing at AGENTS.md/TESTING.md/CONTEXT.md. Address the separate, larger oxlint-ignore-parity defect at the same time (see missed findings). |
| 18 | `jscpd` is a devDependency with a config block, wired to no script and no workflow, in all three repos | dead-weight | config-divergence | — | trivial | high | Either wire it up (`"lint:duplication": "jscpd src"` in `check`, and remove the knip ignore entry) or delete the devDependency, the `jscpd` package.json block, and the knip ignore line. Do not ship it in the current state. |
| 19 | Dependabot has no `cooldown`, contradicting pnpm-workspace's `minimumReleaseAge: 1440` | defect | config-divergence | — | trivial | medium | Add a `cooldown` block to the npm ecosystem entry in `.github/dependabot.yml` with a default-days value matching `minimumReleaseAge: 1440` (1 day), and add a comment in each file pointing at the other. Low confidence is on the exact Dependabot option name/shape only — verify against current GitHub docs before applying; the mismatch itself is certain. |
| 20 | iris's React Compiler toolchain evaluation validates the template's Babel pipeline and should ship with the template | missing-practice | dx-ergonomics | iris | trivial | high | Copy docs/react-compiler-toolchain-evaluation.md into the template with app-specific framing generalized, and link it from a comment at vite.config.ts:110 so the next person who wonders finds the answer at the point of the decision. Keep the revisit triggers verbatim. Adopt its research method as a template working rule: verify package capabilities by executing a transform, not by grepping .d.ts files. |
| 21 | Committed generated artifact `module-layer-dependencies.dot` gates `pnpm check`, and the only way to un-stale it requires Graphviz | defect | dx-ergonomics | neither | small | high | Split regeneration: `pnpm architecture:graph` should write the DOT with pure JS and only attempt the SVG when `dot` is on PATH, skipping with a note otherwise. The DOT is the checked artifact, so the Graphviz dependency must never block un-blocking yourself. Consider a .gitattributes merge strategy for the .dot, or moving the gate out of the inner-loop `check` to pre-push/CI. |
| 22 | Minor hygiene: a documentation filename contains a space and propagated unchanged to both apps | defect | dx-ergonomics | neither | trivial | high | Rename to docs/security-practices.md and update references. Add a trivial guard (a line in an existing architecture test) asserting no tracked file path contains a space. |
| 23 | `npm-run-all@4.1.5` — unmaintained since 2018 — sits on the critical path of `dev`, `build`, `check`, and `verify`; hume-demo swapped to the maintained fork `npm-run-all2@9.0.2` | modernization | dx-ergonomics | hume | trivial | medium | Replace `npm-run-all@4.1.5` with `npm-run-all2` — `run-p`/`run-s` are binary-compatible, so no script text changes, as hume-demo demonstrates. Low risk, and it removes an eight-year-unmaintained package from the critical path of every command in the repo including the supply-chain-audited CI workflows. |
| 24 | Template runs oxlint AND a full ESLint pass with eight plugins over the same tree; APP1 consolidated on Oxlint and kept one narrow typed-SonarJS pass with a documented removal criterion | modernization | hume-archaeology | hume | large | high | Port oxlint.config.ts and .oxfmtrc.json; drop eslint.config.mjs and the plugins whose rules move into oxlint (@eslint-react, eslint-plugin-security, eslint-plugin-simple-import-sort, eslint-plugin-react-hooks, the TanStack plugins); keep a single narrow eslint.typed.config.mjs for typed SonarJS with docs/oxc-tooling-decisions.md's removal criterion copied verbatim. Diff diagnostics from both paths over the template's own src before deleting anything, as that doc instructs. Port scripts/lib/repository-ast.ts as a separate, lower-risk change — it is useful on its own for check-test-layering.mjs and tests/architecture. |
| 25 | Committed visual baselines are darwin-only, so the template's own visual workflow cannot pass on the ubuntu runners it ships | defect | hume-archaeology | — | small | low | Decide and document one model: either generate and commit linux baselines via a container (so CI and local agree), or mark __visual_snapshots__ as developer-local and gitignore it, keeping the CI visual job as an artifact-producing informational run. Whichever is chosen, state it in TESTING.md — the current state gives a fork six macOS PNGs and an ubuntu workflow with no explanation of how they relate. |
| 26 | `.gitignore` has no pattern for agent scratch artifacts — four junk files were committed in Iris's first commit and are still tracked | missing-practice | iris-archaeology | — | trivial | high | Add an 'agent/scratch artifacts' block to the template .gitignore: `_tmp*`, `compass_artifact_*`, `.agents/`, `.claude/settings.local.json`, `videos/`. Zero cost, and every fork otherwise rediscovers it by finding junk already committed. |
| 27 | Iris produced a reproducible React-compiler / Vite-plugin toolchain evaluation concluding the template's current configuration is correct — the template should carry it rather than have each fork re-derive it | modernization | iris-archaeology | iris | trivial | medium | Copy docs/react-compiler-toolchain-evaluation.md into the template (genericized), keeping its 'Revisit triggers' section. Rare case where the answer is 'the template is already right' — but the template says nothing about it, so every fork that notices the Babel pass will re-litigate it and at least one will act on the fabricated `oxc.transform.reactCompiler` advice. Carry forward the two harm-preventing conclusions: do not add include/exclude path scoping to the Babel plugin, and do not switch to @vitejs/plugin-react-swc via `useAtYourOwnRisk_mutateSwcOptions`. |
| 28 | `throwServerFnErrorForReason` indexes a plain record with an unvalidated key (prototype-chain lookup) | defect | platform-runtime | neither | trivial | medium | `const config = Object.hasOwn(reasons, reason) ? reasons[reason] : undefined;`. Sweep `src/` for bracket lookups on `Record<string, …>` with runtime-derived keys and add a Semgrep rule. |
| 29 | Dangling documentation reference: `docs/security-rate-limiting.md` is cited twice in template source but does not exist | doc-drift | platform-runtime | neither | trivial | high | Write `docs/security-rate-limiting.md` covering limited endpoints and defaults, why the limiter is per-process, the `TRUSTED_PROXY_DEPTH` topology requirement and its failure mode, `TELEMETRY_REQUIRE_AUTH`, and edge/WAF configuration; fold in the unattributed-bucket policy. Add a CI check for dangling `docs/*.md` references in source comments. |
| 30 | Suppression and unsafe-cast hygiene is good — no material findings (calibration note) | modernization | platform-runtime | neither | trivial | high | Optionally drop the ambient `type TODO = any` (keep `ExplicitAny`, which is used) so a template does not hand every downstream app a frictionless `any`. No other action. |
| 31 | OTel instrumentation-scope names are hardcoded to `'start-ui-web'` in composition — both apps had to edit the file | missing-practice | platform-runtime | both | trivial | high | Read the instrumentation-scope name from `getTelemetryConfig().serviceName` (or add a dedicated `instrumentationScopeName`) instead of the literal. |
| 32 | The product name is hardcoded in `src/platform/lib/get-page-title.ts` — both apps edited the platform file to rename their app | defect | platform-runtime | both | trivial | high | Add an app-identity config (name, marketing URL, SMTP HELO domain / message-id domain) read from env or a single `src/app/app-config.ts`, consumed by `get-page-title.ts`, the email footer, the SMTP gateway and the migration lock namespace. Add a `pnpm check` rule flagging residual `start-ui` literals outside fixtures so a fork can tell when the rename is complete. |
| 33 | react-cosmos fixtures are 63 co-located files of demo weight that APP2 deleted wholesale | dead-weight | platform-runtime | iris | small | high | Either relocate fixtures under a single `cosmos/` root so a fork can drop them in one operation, or keep them co-located and document the opt-out (delete `**/*.fixture.tsx`, cosmos.css, the two scripts and the two devDeps). De-brand the fixtures that link to start-ui.com regardless. |
| 34 | No Codex `.agents/skills/` mirror, though hume-demo carries one with openai.yaml sidecars | modernization | research-agentic | hume | trivial | medium | Defer until first-party `.claude/skills/` exist. Then author once and mirror with a `pnpm gen:agent-skills` script wired into `pnpm check`, following the existing generate-and-verify precedents (`architecture:graph:check`, `check:migrations`), so a drifted mirror fails the gate. Prefer the script over a symlink for Windows portability. Add `agents/openai.yaml` sidecars only where implicit invocation should be suppressed — and verify that field name against current Codex docs first, since no example in this corpus uses it. |
| 35 | No Copilot or Cursor bridge files; Zed and VS Code are already served | modernization | research-agentic | — | trivial | high | Add `.github/copilot-instructions.md` and `.cursor/rules/project.mdc`, each about five lines, pointing at AGENTS.md and naming the three canonical commands (`pnpm check`, `pnpm test:affected`, `pnpm verify`) — pointers only, so there remains one place to update. Skip `.cursorrules`/`.windsurfrules`. Do this after the path-scoped rule split, so `.github/instructions/*.instructions.md` files can reuse content that already exists. |
| 36 | Guardrails should be encoded as machine checks or hooks rather than added as more always-on prose | guidance | research-agentic | — | trivial | low | Adopt an explicit template rule: every guardrail lives in exactly one of three places — a machine check (depcruise / Semgrep / Sheriff / architecture test), a hook, or a path-scoped rule that loads only when relevant. Reserve AGENTS.md prose for orientation, escape hatches, and rationale. Concretely: audit AGENTS.md:107-119 against `.dependency-cruiser.cjs` and `.semgrep.yml`, delete bullets that merely restate an already-enforced rule, and replace each with a one-line pointer to the enforcing config. Do NOT cite the ETH/JetBrains/arXiv figures in the final report — they are unverified secondary sources. |
| 37 | The template's own git history is polluted with a machine-specific absolute path in commit messages, degrading the one durable record an agent can read | template-defect | research-agentic | — | trivial | high | Document a commit-message convention in CONTRIBUTING.md next to the existing attribution note: repo-relative paths only, imperative subject line under ~72 chars, body prose optional. Enforce with a `commit-msg` lefthook rule rejecting `/Users/`, `/home/`, and `C:\\` in the message body — a two-line addition to the existing `lefthook.yml`, in the spirit of AGENTS.md:145. |
| 38 | Vite 8 / Rolldown / React Compiler plugin ordering is correct — do not 'fix' it; the actionable residual is the reactCompilerPreset filter | modernization | research-stack | both | trivial | high | Leave the ordering alone. Add a source comment beside `babel()` citing the upstream README snippet so a future 'fix' cannot regress it silently. Adopt the README's `rolldown.filter.id.exclude` narrowing (e.g. exclude `src/composition/**`, `src/platform/env/**`, `src/modules/*/{domain,application,infrastructure}/**`) to cut Babel out of the hot path for non-component trees. Bump vite 8.1.0 → 8.2.1 and @vitejs/plugin-react 6.0.3 → 6.0.5 as routine maintenance. |
| 39 | TanStack packages are patch-level behind; no major migration is pending — say so explicitly | modernization | research-stack | hume | small | medium | Routine patch bumps across the TanStack set. State explicitly in AGENTS.md that no TanStack major migration is pending (Query v6 is Svelte/Solid-only; React stays on v5), so nobody schedules one. Before recommending `createServerFn({ strict: true })` or deferred Hydrate boundaries, confirm they exist in the version the template pins — both were sourced from changelogs at or above 1.171.0, which the template has not reached. |
| 40 | Better Auth is 4 patches behind; Drizzle and Zod are exactly current and must NOT be speculatively upgraded | modernization | research-stack | neither | trivial | high | Bump better-auth 1.6.22 → 1.6.26, and re-test the `kysely: 0.28.17` override at the same time since the comment ties that pin to a specific better-auth adapter behaviour. Do not chase 1.7.0-rc.4. Add an explicit note to AGENTS.md that `drizzle-orm@0.45.x`, `drizzle-kit@0.31.x` and `zod@4.4.3` ARE the current stable releases despite the visible 1.0.0-rc / canary tags, so nobody moves a starter's persistence layer onto a release candidate on pattern-match. |
| 41 | OpenTelemetry caret floors are 1-2 minors stale across the whole set | modernization | research-stack | neither | trivial | high | Refresh the caret floors to current minors (2.10.0 / 0.221.0 / 1.43.0 / 0.66.0) so a fresh install starts current and the package.json reads as maintained. Do not adopt `@opentelemetry/browser-sdk` at 0.1.0; note in AGENTS.md that it is the expected eventual replacement for the hand-assembled sdk-trace-web + instrumentation-document-load + instrumentation-fetch browser setup. |
| 42 | `catching-jit-tests-vitest` is a 4-version, single-maintainer 0.x package wired into the template's CI, and both forks inherited it unexamined | modernization | research-stack | neither | trivial | medium | Keep it, but document it. Add a line to AGENTS.md naming `catching-jit-tests-vitest` as a first-party experimental tool authored by the template maintainer, with a note on what `pnpm test:jittest` does and that it can be removed by deleting the script, the devDependency, and .github/workflows/jittest.yml. A starter template that ships a maintainer's own 0.x package into every downstream repo should make that an explicit choice rather than an inherited default — the same standard this audit applies to the nitro nightly pin. |
| 43 | Template is NOT behind on dependencies — it leads upstream on every shared package; the gap premise (b) points at is process, not versions | modernization | research-starters | neither | trivial | high | Adopt hume-demo's mise.toml (3 lines) and bin/run wrapper into the template — zero risk, no new dependency, and it removes a class of 'wrong Node major' failures that .nvmrc cannot prevent. Do NOT change the dependabot config; it is already well-grouped. If anything, add a `template-affecting` label rule so the changelog can double as a fork update feed. |
| 44 | Template ships no root CLAUDE.md or CONTEXT.md; both apps independently created both with the same structure | missing-practice | template-friction | both | small | high | Ship a root CLAUDE.md indexing AGENTS.md, TESTING.md, .claude/rules/*.md and docs/, plus the canonical command list (`pnpm check`, `pnpm test:affected`, `pnpm verify`, single-test invocations) — the template-level material both apps' CLAUDE.md files have in common. Ship a CONTEXT.md stub explaining what belongs in it (domain glossary / ubiquitous language) rather than a book-store glossary. |
| 45 | Vestigial dependency-cruiser waiver for `src/modules/book/server.ts` | dead-weight | template-friction | iris | trivial | medium | Delete the `pathNot` clause from `.dependency-cruiser.cjs:210` and confirm `pnpm depcruise` still passes. If it does not, fix `book/server.ts` rather than re-adding the waiver — an example module carrying a named guardrail waiver teaches forkers the wrong lesson. |
| 46 | No Node/pnpm version guard at runtime; hume-demo invented one because the pins alone were not enough | missing-practice | template-friction | hume | trivial | medium | Port hume-demo's `scripts/check-node-version.mjs` into the template and add `check:node-version` to `check` and `check:ci`. The `bin/run` wrapper is more opinionated and can be left out. Low confidence on how widely this is felt, since only one of the two forks invented it. |

---

# Appendix A2 — Refuted claims (do not resurrect)

85 claims were raised by an investigator and killed by adversarial verification. They are recorded so nobody re-derives them.

| # | Claim | Why it does not hold |
|---|---|---|
| 1 | `infrastructure-no-presentation` fails open for new modules (sub-claim of the guardrail finding) | I required the real `.dependency-cruiser.cjs` in node and enumerated every forbidden rule whose `to` mentions `presentation`. The edge `widget/infrastructure → widget/presentation` IS caught, by the generic sibling rule `infrastructure-no-feature-presentation-or-transport` (.dependency-cruiser.cjs:165-172): `from: '^src/modules/(?!kernel/)[^/]+/infrastructure'` → `to: '^src/modules/(?!kernel/)[^/]+/(presentation\|transport)(?:/\|$)\|…'`. So `infrastructure-no-presentation` is redundant dead config, not a fail-open gap. Only `domain-no-presentation` and `application-no-presentation` genuinely fail open — two rules, not three. The recommendation should be to DELETE the infrastructure rule, not to genericize it. |
| 2 | 'README.md never mentions … OpenTelemetry' and 'AGENTS.md, which the README never links' | Both are factually wrong. README.md:79 reads `See [AGENTS.md](AGENTS.md) and [Testing Strategy](TESTING.md) for the full verification workflow.` README.md:94-109 is an entire OpenTelemetry section (`The app uses OpenTelemetry for traces, metrics, and server-emitted logs…`, browser OTLP endpoints, `docker compose --profile observability up otel-collector`), and README.md:116-123 is a CodeQL section. The README is genuinely silent on hexagonal layering, modular monolith, `src/composition`, public gates, dependency-cruiser, Sheriff, Semgrep, and Stryker — that narrower claim survives and I kept it. The overstated version does not. |
| 3 | '57 files carry the upstream brand' | Measured 45. `grep -rilI --exclude-dir=node_modules --exclude-dir=.git --exclude=pnpm-lock.yaml 'start-ui\\|startui\\|bearstudio\\|ivan-dalmet\\|hbmartin' . \| wc -l` → 45; without the binary-file exclusion, still 45; including pnpm-lock.yaml, 46. I could not reproduce 57 under any exclusion combination. The qualitative point (branding is load-bearing, not cosmetic) is unaffected and I kept it; the number was inflated by ~27%. |
| 4 | '`RESEND_API_KEY="REPLACE ME"` has the same shape' as the AUTH_SECRET first-run blocker, and the S3 `startui-*` placeholders block first run | The storage and email placeholder rejections are production-only. `src/modules/kernel/infrastructure/config/storage.ts:70-71` opens its `superRefine` with `if (!isProdRuntimeEnvironment(env)) return;`, so `S3_ACCESS_KEY_ID="startui-access-key"`, `S3_SECRET_ACCESS_KEY="startui-secret-key"`, and `DOCKER_MINIO_PASSWORD="minioadmin"` all pass in local dev. By contrast the AUTH_SECRET checks in `auth.ts:75-92` are gated only on `shouldSkipEnvValidation`, not on production. AUTH_SECRET is the sole out-of-the-box first-run blocker; presenting the S3/Resend values as the same class of defect is wrong and would send a maintainer to fix a non-problem. |
| 5 | 'book/genre appear in 174 files and 3,369 word-boundary occurrences, 67 outside the modules' | The numbers do not reproduce and the description is internally inconsistent — 3,369 is not a word-boundary count. My measurements: case-insensitive substring match → 178 files, 3,749 occurrences, 68 files outside `src/modules/{book,genre}` and `tests/`; strict word-boundary match → 144 files. The investigator appears to have used substring matching while labelling it word-boundary. Direction and magnitude of the finding are correct, so I kept the finding with corrected figures; the specific numbers as stated should not be quoted. |
| 6 | Vite reads Sentry configuration only from .env files via loadEnv, so source-map upload silently never runs on Vercel or CI | The central mechanism is factually wrong. Vite's `loadEnv` explicitly merges `process.env` keys matching the prefix, and gives them priority. I fetched the upstream source (vitejs/vite, packages/vite/src/node/env.ts): the doc comment reads 'Load `.env` files within the `envDir` and merge them with the matching variables already present in `process.env`', and the function ends with `// check if there are actual env variables starting with VITE_* / these are typically provided inline and should be prioritized / for (const key in process.env) { if (prefixes.some((prefix) => key.startsWith(prefix))) { env[key] = process.env[key]! } }`. Therefore `loadEnv(mode, cwd, 'SENTRY_')` in TEMPLATE vite.config.ts:74 DOES pick up `process.env.SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN`, and `loadEnv(mode, cwd, 'VITE_')` at :73 picks up `process.env.VITE_SENTRY_DSN`, on a Vercel or GitHub Actions build. The plugin is NOT a guaranteed no-op on hosted builds, and APP1's `process.env.X ?? loadEnvResult.X` rewrite is redundant for exactly this reason. The investigator asserted the loadEnv semantics without checking them. Two smaller sub-claims bundled into this finding do survive and are re-filed under `missed`: the template passes no Sentry `release` name, and it has no HMR-overlay suppression for visual-baseline runs. |
| 7 | "Three kernel APIs the Better Auth adapter needs are exported from no public gate" (as stated) | Two of the three named APIs already have supported gate paths, and the third does not exist in the template. `AppError` is exported at src/modules/kernel/index.ts:10 (`export * from './domain/errors/app-error';`) and the branded IDs at :15 (`export * from './domain/ids';`), so auth.tsx:10 and :15 are avoidable deep imports, not gate gaps. `getBetterAuthConfig` is exported from src/modules/kernel/backend.ts, so auth.tsx:16 is likewise avoidable. `emitConfigDiagnostic` — the investigator's third API — is an Iris invention (/home/user/iris-insights-crm/src/modules/kernel/infrastructure/config/config-diagnostics.ts) that does not exist anywhere in the template, so it cannot be missing from a template gate. Only TWO template APIs genuinely lack any gate: the `Database` type (only `isRootDatabase` is exported) and `getUserLanguage` (grep of src/modules/kernel/*.ts returns nothing). The underlying finding survives in corrected form and is kept in `verified`; the headline claim as written is false. |
| 8 | "29 deep imports across 12 files in the auth module alone" | Measured wrong. Counting non-gate imports (excluding `@/modules/kernel/{index,server,backend,client,presentation,testing}`) across src/modules/auth/ gives 46 occurrences in 25 files, not 29 in 12. The error is in the direction that understates the problem, but the number was asserted as measured and is not. |
| 9 | "Confirmed outside Vitest per the commit: process.exitCode = 1; await db.close(); // process.exitCode === 0" | Presented as a reproduction; it is a quotation from iris commit 588a92b's message. I attempted the reproduction and could not run it: no node_modules are installed in the template, hume-demo, or iris-insights-crm (`ls node_modules/@electric-sql` → nothing in all three), and the ambient node is 22.22.2 against repos pinned to 24. The finding is retained with severity lowered critical→high and confidence lowered high→medium, because what is independently verifiable — the template's teardown at tests/server/pglite-global-setup.ts:55-58 has no exit-code preservation and Iris's does — is sufficient to justify the three-line fix regardless. |
| 10 | "eslint already ignored [.claude and .agents], so only the two oxc configs needed the entry" | eslint.config.mjs:48-58 contains no `.claude` or `.agents` entry. ESLint avoids the problem incidentally, because its `files` globs are TS/JS-scoped and the shipped `.claude/` tree holds only .md and .json. A future `.agents/*.mjs` or `.claude/hooks/*.js` would be linted. The recommendation should add the ignore to all three configs, not two. |
| 11 | "Deleting [React Cosmos] removes the template's last standing security-risk-register entry" | Overstated. Removing Cosmos clears exactly one register entry — `@skidding/launch-editor 2.13.2`, whose 'Current path' cell reads `react-cosmos > @skidding/launch-editor`. Four advisory entries (esbuild, protobufjs, launch-editor via @tanstack/devtools-vite, js-yaml via @svgr/cli) and the separate `nitro` pre-release entry are unaffected, and all six expire on the same date. Iris's own commit 5082abf says only 'the last ACCEPTED advisory' after efac8c6 had already cleared the others via overrides — the investigator dropped the ordering and made it sound like Cosmos removal alone fixes the register. |
| 12 | Summary claim: "APP2's vitest.config.ts, vitest.visual.config.ts, vitest.postgres.config.ts, sheriff.config.ts ... are byte-identical to the template" | sheriff.config.ts is NOT byte-identical in APP2. md5sum: template = 6a9442f1af9a44d8078138e68aa993fe, /home/user/hume-demo/sheriff.config.ts = 6a9442f1af9a44d8078138e68aa993fe (identical), /home/user/iris-insights-crm/sheriff.config.ts = 669595ac23df1435711deb84453bb339 (differs at :44-48). The investigator swapped the apps: APP1 is the one that kept it verbatim, APP2 is the one that narrowed the enumeration — which the same investigator's own finding #1 correctly states, so the summary contradicts the finding. vitest.config.ts is confirmed identical (e9da01… in both). |
| 13 | Finding #1: "in APP1 those three depcruise layer rules now match zero files and enforce nothing" | The 'match zero files' half is right; 'enforce nothing' is wrong for two of the three. `application-no-presentation` is fully subsumed by the generic `application-only-own-domain-and-application` (.dependency-cruiser.cjs:102-113, `from: '^src/modules/([^/]+)/application'` → `to: '^src/modules/$1/(?!application(?:/\|$)\|domain(?:/\|$))'`), which forbids application→presentation for every non-kernel module. `infrastructure-no-presentation` is fully subsumed by the generic `infrastructure-no-feature-presentation-or-transport` (:166-174, `from: '^src/modules/(?!kernel/)[^/]+/infrastructure'` → `to: '^src/modules/(?!kernel/)[^/]+/(presentation\|transport)(?:/\|$)\|…'`). Cross-module variants of all three are caught by `no-cross-feature-deep-import` (:132-142). The only genuine depcruise hole is same-module `domain/` → `presentation/`. Severity 'critical' and the claim that this is 'the single highest-impact defect in the area' are not supported; corrected to 'high' in the verified list. (The sheriff.config.ts enumeration IS genuinely fail-open — that part survives.) |
| 14 | Finding #3: "a fresh clone of the template today fails its own `pnpm check`" | Unverifiable and unverified. None of the three repos has node_modules and there is no package registry access in this environment, so `pnpm audit --audit-level=high` could not be run against the template. The statement is a plausible inference from the apps' override comments, not evidence. It should not appear in the final report as a fact; state it as 'likely, untested'. |
| 15 | Finding #3: semgrep rule `no-known-vulnerable-tooling-transitives` "whose whole purpose is to police this file" | The rule at .semgrep.yml:257-265 is `pattern-regex: '(?:shell-quote(?:@\|:\s*)1\.8\.3\|svgo(?:@\|:\s*)3\.(?:[0-2]\.[0-9]+\|3\.[0-3]))'` scoped to package.json and pnpm-lock.yaml — two hardcoded version strings for two specific advisories, both already superseded by the current override set. It does not police the overrides file in any general sense, and it is itself an instance of the hardcoding antipattern the report criticises elsewhere. Extending it 'to cover the new entries' would make the problem worse. |
| 16 | Finding #6 recommendation: "Verify `.github/dependabot.yml` groups them the same way (`groups:` with patterns @opentelemetry/*, @tanstack/*, vitest/@vitest/*)" | Already done. `.github/dependabot.yml` is byte-identical in all three repos (md5 23a8fd…) and already defines groups for `@tanstack/*`, `@sentry/*`, react/react-dom/@types/react*, `drizzle-*`, all development-type minor/patch (`dev-tooling`, which covers vitest and @vitest/*), and all remaining production minor/patch (`production-minor-patch`, which covers every @opentelemetry/* package). There is no grouping gap. The actual, unmentioned gap is that dependabot.yml sets no `cooldown` while pnpm-workspace.yaml sets `minimumReleaseAge: 1440`, so Dependabot can propose a version pnpm will refuse to install. |
| 17 | Finding #7: npm-run-all "drags in an old cross-spawn/minimatch/shell-quote transitive tail, which is likely part of why both apps had to add shell-quote overrides" | Unsupported. No lockfile or node_modules analysis was performed (none exists in these checkouts), and the apps' own override comments attribute the advisories to different parents: APP2 credits `esbuild` to `tsx > drizzle-kit` and `launch-editor` to `@tanstack/devtools-vite`, and neither app's shell-quote comment mentions npm-run-all. The swap to npm-run-all2 is still worth doing, but this justification should be removed. |
| 18 | Finding #9 title: "Both apps had to add agent-tooling directories to lint AND format ignore lists" | Only APP2 touched a lint ignore list. APP1 has no `.oxlintrc.json` at all; its `oxlint.config.ts:194-205` `ignorePatterns` contains `.github/codeql/**`, `.output/**`, `**/*.testproj/**`, `**/__screenshots__/**`, `coverage/**`, `dist/**`, `node_modules/**`, `playwright-report/**`, generated icons, `src/routeTree.gen.ts`, `test-results/**` — and neither `.claude` nor `.agents`. The independent convergence is real but confined to the formatter (`.oxfmtrc.json`): APP1 added `.claude/settings.local.json`, APP2 added `.claude` and `.agents`. Severity lowered to low. Additionally, APP2 has no `.agents/` directory at all, so its entry is prophylactic, not a response to observed churn. |
| 19 | Finding #10: "the template has no concept of a deployment environment distinct from `import.meta.env.PROD`, and no way to resolve the app's own canonical URL at runtime" | Overstated. `src/platform/env/config.ts:30-39` already contains `const vercelUrlPreviewUrl = env.VITE_VERCEL_ENV === 'preview' ? env.VITE_VERCEL_BRANCH_URL : null;` with an https fallback, and `src/modules/kernel/infrastructure/config/env-schema.ts:100` declares `VERCEL_ENV: z.string().optional()`. The accurate defect is sharper and I have restated it that way: the declared `VERCEL_ENV` is never read by the canonical `isProdRuntimeEnvironment` (:49-58, which uses NODE_ENV/PROD only), and the Vercel logic that does exist is client-visible and build-time. The narrower sub-claim that vite.config.ts contains no `process.env.VERCEL_*` is correct. |
| 20 | Finding #10 framing: both apps independently invented the same solution | They invented solutions to the same problem, but the solutions conflict. APP1's /home/user/hume-demo/src/platform/env/app-url.ts:21-23 resolves `nonEmptyString(env.VERCEL_PROJECT_PRODUCTION_URL) ?? nonEmptyString(env.VERCEL_URL)` with no deployment-environment gate — which is precisely the two mistakes APP2's commit 9fcb5c1 argues against ('VERCEL_PROJECT_PRODUCTION_URL … is set even on previews, so it must not be consulted there' and 'VERCEL_URL is excluded'). The convergence signal is on the missing primitive, not on the fix; the report should port APP2's version and explicitly not APP1's ordering. |
| 21 | Finding #14 severity counts and the 'three separate config files, one bug class' scope | Two numeric corrections that affect how the finding reads: the branded-ID alternation has 26 names, not 27; and the template's pnpm-workspace override block has 12 entries, not 13 (APP1 20 not 21, APP2 21 not 22). Neither changes the conclusion, but the report is consumed as evidence and the numbers should be right. |
| 22 | "14 identical `pnpm install`s fire on every PR" | Wrong on both counts. `grep -rho 'uses: ./.github/actions/setup-pnpm' .github/workflows/ \| wc -l` = 16 total call sites, and only 12 of them fire on a pull_request: code-quality.yml (10) and e2e-tests.yml (2). cosmos-pages.yml and supply-chain.yml are `push`-only; jittest.yml and mutation-testing.yml are `workflow_dispatch`-only (verified by parsing each workflow's `on:` block with PyYAML). The underlying point survives; the number does not. |
| 23 | "`node-version: 24` is hardcoded at 13 workflow call sites" / "14 `with: node-version: 24` blocks" | Both figures are wrong. `grep -rn "node-version: 24" .github/workflows/ \| wc -l` = 16: code-quality.yml lines 30, 53, 79, 126, 171, 193, 216, 255, 302, 336; e2e-tests.yml 78, 216; cosmos-pages.yml 28; jittest.yml 58; mutation-testing.yml 34; supply-chain.yml 32. |
| 24 | "I checked all 47 `uses:` lines" across all three repos | The template alone has 50 non-local `uses:` lines (`grep -rhoE 'uses: [^ ]+' .github/ \| grep -v 'uses: \./'`). The pinning conclusion is correct — every one carries a 40-hex SHA, and the only non-SHA hit is `uses: security-extended` at .github/codeql/codeql-config.yml:6, which is a CodeQL query suite, not an action — but the count is understated. |
| 25 | "`scripts/check-codeql-sarif-baseline.mjs` (213 lines)" / "a 213-line local SARIF-baseline gate" | 213 was the line count at the moment it was added (`git show --stat e181825` → `scripts/check-codeql-sarif-baseline.mjs \| 213 ++++`). The file at APP1 HEAD is 358 lines (`wc -l`). Describing it in the present tense as 213 lines is stale. |
| 26 | Recommendation to add a Dockerfile `HEALTHCHECK CMD … fetch('http://127.0.0.1:3000/api/health')` | No such route exists. `ls src/routes/api` → auth.$.ts, dev.email.$template.ts, telemetry.logs.ts, telemetry.otel.v1.metrics.ts, telemetry.otel.v1.traces.ts, telemetry.sentry-tunnel.ts, upload.ts, webhooks.resend.ts. A healthcheck against a non-existent endpoint would fail the container on startup. A health route would have to be added first. |
| 27 | Recommendation to "add two assertions the template currently lacks: every job declares `timeout-minutes`, and every workflow declares a top-level `permissions` block" | The second is already satisfied. I parsed all twelve workflows with PyYAML: every one declares a top-level `permissions` key (code-quality, codeql, cosmos-pages, dependency-review, detect-secrets, e2e-tests, jittest, mutation-testing, osv-scanner, osv-scanner-full, semgrep, supply-chain). Only the `timeout-minutes` assertion is a real gap. |
| 28 | "Two apps, two independent discoveries of the same failure mode" (exact pins vs caret security floors) | APP1 did not discover or adopt this. /home/user/hume-demo/pnpm-workspace.yaml:10-28 uses exact pins throughout — `body-parser: 1.20.6`, `brace-expansion: 5.0.9`, `esbuild: 0.28.1`, `fast-uri: 3.1.5`, `launch-editor: 2.14.1`. APP1's cited comment (:15-17) is about a *version-selector key* (`'fast-uri@3.1.2'`) ceasing to match once the override moved the resolved version — a different mechanism from an exact pin freezing at an old advisory floor. Only APP2 (pnpm-workspace.yaml:11-18) converted floors to carets. The cross-app-convergence signal the investigator claimed does not exist here. |
| 29 | "Both `osv-scanner.yml` and `osv-scanner-full.yml` fail the build on any finding" | Unverifiable from these repos and asserted without evidence. Both workflows delegate to `google/osv-scanner-action/.github/workflows/osv-scanner-reusable*.yml@9a49870` (osv-scanner.yml:18, osv-scanner-full.yml:21), whose failure semantics live outside this codebase. APP1's inlined replacement explicitly passes `--fail-on-vuln=true`, which suggests the reusable default may not be failing. The finding's core (no suppression convention shipped) does not depend on this claim, but the claim itself should not be reported. |
| 30 | "APP2 kept both files unchanged — consistent with APP2 being public or GHAS-enabled" | Speculation presented as an inference from evidence. Nothing on disk indicates APP2's repository visibility or GHAS status. The equally consistent explanation is that APP2 simply has not yet hit the failure APP1 hit. The observation that APP2 kept the files is fine; the causal gloss should be dropped. |
| 31 | "Three jobs have no `timeout-minutes`" | Five jobs lack it, not three: cosmos-pages::build, cosmos-pages::deploy, semgrep::semgrep, osv-scanner::scan-pr, osv-scanner-full::scan-full (full PyYAML parse of all twelve workflows). The investigator noticed the OSV cases parenthetically but stated the headline count as three. Of the five, three are fixable — `timeout-minutes` is not settable on a caller job that delegates via `uses:`. |
| 32 | "only 8 of 135 unit files use mockDb" (evidence inside the global-test-setup finding) | False. `grep -rn 'mockDb' tests/` returns hits ONLY in tests/server/test-setup.ts and tests/server/test-utils.ts — ZERO test files reference mockDb. What is true is that 9 test files import from @tests/server/test-utils, and they import createAuthenticatedContext / mockSession / mockUser / mockLogger, never mockDb. The count is also against 136 unit spec files, not 135. The finding itself survives (I kept it), but this specific number was fabricated or mis-grepped. |
| 33 | "APP1 had to delete tests/server/test-setup.ts — the first thing APP1 did was drop it" | False as stated. `git log --all --oneline -- 'tests/server/**'` in /home/user/hume-demo returns EMPTY, and `git show 845552c:vitest.config.ts` (the squashed first commit) already reads `setupFiles: [resolve('tests/setup.base.ts')]`. The directory never existed in APP1's history — it was excluded when the fork was scaffolded, not deleted in response to friction. The signal is weaker than 'a developer hit a problem and removed it'. |
| 34 | "APP2 solved the visual-baseline dead-weight problem — appsThatSolvedIt: both" | False. `find /home/user/iris-insights-crm/tests/e2e -name '*.png'` returns exactly the template's 7 demo baselines (login-page, login-verify-page, authenticated-app-shell, manager-users-list/create/edit/delete-confirmation), all `-chromium-darwin.png`, unchanged. Only APP1 replaced them. Corrected to hume-demo and severity lowered from high to medium. |
| 35 | "APP2 solved the stale .claude/rules/testing.md — appsThatSolvedIt: iris-insights-crm" | Misleading. `find /home/user/iris-insights-crm/.claude -type f` -> No such file or directory: APP2 deleted the entire .claude directory, discarding the two ACCURATE rules files (architecture.md, modules.md) along with the stale one. That is collateral removal, not a fix, and it is not a practice the template should adopt. Corrected to `neither`. |
| 36 | "package.json exposes 61 distinct test:mutation* scripts" | False. `grep -c '"test:mutation' package.json` -> 40 (8 aggregate run-s entries + 8 scopes x 4 variants). The finding's argument is unaffected but the number was inflated ~1.5x. |
| 37 | "tests/server/test-setup.ts and tests/server/pglite-global-setup.ts in GLOBAL_CONFIG_FILES are dangerous stale entries" | Overstated. scripts/affected-tests.ts:32 defines `TEST_SUPPORT_PATTERN = /^tests\/(?:support\|mocks\|server)\//`, which already classifies any change under tests/server/ as a test-support change and routes it to the escalation path at :611-626. The two entries are redundant, not misaiming the tripwire. The genuine half of that finding — .dependency-cruiser.cjs and vitest.visual.config.ts being absent — I confirmed and kept. |
| 38 | "the template's test suite is 26,743 lines, of which book/genre tests are 2,630 across 19 spec files (~10%)" | Two numbers wrong. `find tests -name '*.ts' -o -name '*.tsx' \| xargs wc -l` -> 28,422 total, and `find tests \( -path '*book*' -o -path '*genre*' \) -name '*.ts*'` -> 18 files / 2,630 lines (~9.3%). Minor, but the finding cited them as measured. |
| 39 | "TESTING.md differs from APP2 by one word" | Two words. The only difference is at line 42: the template reads '...extensions, queues, object storage, or network semantics...' and APP2 reads '...extensions, queues, or network semantics...'. Trivial, but the investigator presented it as a precise measurement. |
| 40 | Sub-claims I could NOT verify and therefore removed from the evidence rather than pass through | (1) The attribution of the tanstack mock additions to APP1 commit 9539729 — I did not open that commit. (2) APP1 commit ad112e3's sub-pixel-glyph analysis and the claim that Chromium raster flags are inert headless on macOS — unverified web/experiment claim, dropped from the flake finding. (3) Per-repo property-test file counts (10/2/6). (4) `du -sh` snapshot sizes. (5) Semgrep-rule attribution to commit d538538 beyond its subject line. None of these change a verdict; all were removed from the kept findings' evidence lists. |
| 41 | "APP2 had to maintain the mermaid graph by hand across five commits, because architecture:graph:check never told anyone it was stale" | The five commits are af6ae45 (first commit), 8d2b7b4 ('Remove books features and rename the Theme UI term to KIT/KIQ'), a0811c0 ('auth debugging and other followups'), 3f60ded ('Route the Better Auth adapter's kernel imports through public gates'), and b60c9a5 (the tsPreCompilationDeps fix). Every one is a commit that legitimately changed the module dependency graph and therefore legitimately regenerated the artifact. None of them is evidence of undetected staleness or of manual maintenance. The underlying finding — that CHECKED_ARTIFACT_FILENAMES covers only the .dot (generate-module-dependency-graph.ts:39) so a restored .md would rot silently — is real and I kept it; this specific supporting evidence is not. |
| 42 | "An agent sees a formatting error in `.claude/skills/whatever/SKILL.md` and no signal about the code it changed" | oxfmt never touches markdown in this repo: .oxfmtrc.json ignorePatterns already contains `**/*.md` and `**/*.mdx` (lines 24-25). SKILL.md files cannot produce a format:check failure. APP2's 190 files were non-markdown files inside those trees. The `.claude`/`.agents` ignore-pattern finding stands on APP2's commit message db9bc39 (which I read in full); only this illustration is wrong. |
| 43 | Template `.dependency-cruiser.cjs:70-71`, `:88-89`, `:~183-184`, `:230` for the enumerated `(book\|user\|genre\|account)` rules | Those are hume-demo's line numbers, not the template's. In /home/user/start-ui-tanstack-drizzle-shadcn-hexagonal/.dependency-cruiser.cjs the enumerations are at :60-61 (domain-no-presentation), :78-79 (application-no-presentation), :162-163 (infrastructure equivalent), and the dangling exception is at :209 (`pathNot: '^src/modules/book/server\\.ts$'`). The investigator cited APP1's file while labelling it the template. The substance of the finding is correct and I kept it with corrected refs — and added two things they missed (the template's own auth and email modules are outside the enumeration, and sheriff.config.ts:44,47 carries the same fossil list). |
| 44 | "AGENTS.md:15 omits ... `check:migrations` ... five of the eleven actual tasks" | `check:migrations` does have its own row in AGENTS.md's Canonical Commands table ('\| `pnpm check:migrations` \| Guard against invalid manual migration edits. \|'), so it is not undocumented. The accurate and narrower claim is that AGENTS.md:15's *description of `pnpm check`* names six tools while package.json:132 runs eleven. I kept the drift finding with the corrected wording. |
| 45 | "Both apps independently invented `skills-lock.json`, a lockfile pinning skill markdown by SHA-256 — the strongest signal in this audit" | skills-lock.json is a generated artifact with a fixed schema (`version: 1`, `sourceType: 'github'`, `computedHash`), written by a skills-installer tool, not designed by either app author. Two teams running the same installer is not two teams independently inventing a mechanism. Further, the claim that both apps demonstrate a vendoring practice is false: `git ls-files` shows APP1 tracks `.agents/skills/**` plus the `.claude/skills` symlink, while APP2 tracks ONLY skills-lock.json — `git log --all -- .claude .agents` in APP2 is empty and neither directory exists on disk. The surviving, weaker signal is that both authors chose the same three mattpocock skills; I kept the finding at medium severity and medium confidence on that basis. |
| 46 | Recommendation to add `.claude/skills/` to `.gitignore` (in the oxfmt/oxlint finding) | It contradicts the same report's skills finding, which recommends vendoring `.agents/skills/**` and the `.claude/skills` symlink into the template — exactly what APP1 tracks in git (`git ls-files` shows all seven vendored skill files plus the symlink). Gitignoring the skills tree would make the template's own vendored skills untracked. Only `.claude/settings.local.json` should be gitignored; the ignore-pattern fix belongs in the two oxc configs, which is what APP2's db9bc39 actually did. |
| 47 | "src/start.ts has existed since commit ce591d8 (2026-06-02)" (dating inside the CSRF finding) | Wrong commit and wrong date, and it understates the defect. `git cat-file -e 4459a84:src/start.ts` succeeds — src/start.ts is present in the exact tree that INTRODUCED docs/strict-modular-monolith.md (4459a84, 2026-05-29 07:38:33, merge of PR #53). The earliest commit touching it in `git log -- src/start.ts` is 9819ad3 at 2026-05-29 07:39:08, the same PR. The doc was false the day it was written, not drifted-into-falsehood a week later. |
| 48 | "Add a `tests/unit/start.unit.spec.ts`-style pin of the middleware chain to the template so the doc claim is executable" | The file already exists in the template: tests/unit/start.unit.spec.ts, 243 lines, whose test at :17 is titled 'adds Sentry, telemetry, security headers, auth context, browser mutation guard, server-function body limit, and server-function CSRF middleware' and which asserts at :25-39 `csrf.type === 'csrf'`, `filter({handlerType:'serverFn'}) === true`, `secFetchSite === 'same-origin'`, `referer === true`. The template therefore already ships a passing test contradicting its own doc — the missing piece is a docs-lint, not a test. |
| 49 | "`grep -rn 'docs/' README.md AGENTS.md TESTING.md CONTRIBUTING.md .claude/rules/*.md` → only two hits … No template doc links to any file in docs/" | The conclusion is overstated because the grep excluded .github/SECURITY.md, which does link into docs/: :47 and :56 link `docs/security-risk-register.md`, and :54 links `docs/security practices.md`. The defensible version — which I verified with a repo-wide grep — is that `strict-modular-monolith` returns ZERO hits anywhere in the repo, and docs/architecture/ is likewise unlinked. |
| 50 | "AGENTS.md contains no module inventory, no `src/app` description, no mention of `src/start.ts` …" | The `src/app` sub-claim is false. AGENTS.md:6 reads "app-owned shell/support resources live under `src/app`". The other three sub-claims (no module inventory, no src/start.ts, no sheriff) are correct and were verified by grep. |
| 51 | "hume/docs/security practices.md is 8575 bytes (lightly edited)" implying only the template and iris are affected | Directionally right but the framing understates hume: the file is 8575 vs the template's 7759, i.e. hume ADDED ~800 bytes to a document about Stripe/Twilio/Attio/MongoDB/WhatsApp rather than deleting it. The foreign content was retained in all three repos. Not a refutation of the finding, but the 'lightly edited' gloss reads as partial remediation when it is the opposite. |
| 52 | Recommended remote: `git remote add template https://github.com/hbmartin/start-ui-web` | That repository URL is wrong. `git remote -v` in the template → `origin https://github.com/hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal`. `start-ui-web` is only the never-renamed package.json `name` field. Following the recommendation as written would fail to fetch. |
| 53 | "128 hardcoded brand occurrences across ~40 files" | Occurrence count is right (128) but the file count is low: `grep -rli 'start-ui\|Start UI\|startui' --exclude=pnpm-lock.yaml --exclude-dir=node_modules --exclude-dir=.git .` → **45 files**, not ~40. Minor, but the finding is a counting finding so the number should be exact. |
| 54 | "`.claude/rules/testing.md` … appsThatSolvedIt: neither" with the implication that both apps inherited it | Half wrong. hume inherited it byte-identically (diff shows IDENTICAL). iris did NOT inherit it — `ls /home/user/iris-insights-crm/.claude/` → 'No such file or directory'. iris deleted the entire `.claude/` directory (rules and settings.json alike) and hand-wrote CLAUDE.md instead. That is a materially different fork response and is itself a signal about the template's agent-rules directory. |
| 55 | "docs/security practices.md … `pnpm lint:sheriff` in CI" cited as line :22 | Line number off by one: the Sheriff row is at docs/security practices.md:23, not :22. Line :22 is the Dependency-Cruiser row (the '51 rules' claim). The substance of both claims is confirmed. |
| 56 | SUB-CLAIM: 'src/modules/kernel/infrastructure/config/env.ts … absent from hume-demo' | False. The file is still present at /home/user/hume-demo/src/modules/kernel/infrastructure/config/env.ts; only APP2 (iris) deleted it. The same error applies to the investigator's grouping of src/platform/components/upload/*, src/platform/components/form/field-upload-input/, cosmos.css and the icon-book-open icons — APP2 deleted those, APP1 retained all of them (APP1 also kept all 63 *.fixture.tsx files; APP2 deleted all 63). Do not present any of these as both-apps signals. |
| 57 | SUB-CLAIM: 'add scripts/validate-client-config.ts to the pnpm build pre-step alongside validate-server-config.ts' | The pre-step already exists. package.json:38 is `"build": "cross-env NODE_ENV=production run-p env gen:build-info && vite build"` and package.json:40 is `"env": "run-p env:*"`, so the no-op env:client already runs during every build. The correct framing is worse than the investigator's: the build asserts client config is valid while checking nothing. Only the script needs fixing, not the build wiring. |
| 58 | SUB-CLAIM: 'as any / as unknown as → 50 hits, 38 generated, 12 hand-written' | Wrong counts. `grep -rn 'as any\\|as unknown as' src \| wc -l` → 43; 33 of those lines are in src/routeTree.gen.ts; 10 are hand-written. The qualitative conclusion is unaffected but the numbers must not be published as stated. |
| 59 | SUB-CLAIM: browser-mutation-protection.ts line references :223-227, :198-202, :213-217, :65-72 | All four are wrong; the file is 196 lines long, so :223-227 cannot exist. Real refs: the `new URL(request.url).origin` parse at :170-175, `validateOriginSignal` at :132-149, `validateRefererSignal` at :152-165, the protected-path constant at :12-17. Also omitted: an Origin mismatch is a hard reject (:184-186 returns on the first `!ok`), so a valid `Sec-Fetch-Site: same-origin` does not rescue it. |
| 60 | SUB-CLAIM: 'hume-demo bounded the email pattern to /[^\s@]{1,64}@[^\s@]{1,189}\.[^\s@]{1,63}/g and added a value.includes("@") fast path before deleting it' | Not present anywhere in hume-demo's current sanitize-log-fields.ts, which has no EMAIL_PATTERN and no redactString at all. The claimed intermediate state was not demonstrated. Drop it; the surviving evidence (deletion, the explicit docblock rationale, MAX_SANITIZED_DEPTH = 32) is sufficient. |
| 61 | SUB-CLAIM: 'iris-insights-crm additionally emits telemetryProxy.recordMetric({ name: "security.browser_mutation_rejected", type: "counter", … }) because the warn is dropped at production log level' | Not found in the template-vs-APP2 diff of src/start.ts that I read. APP2's verified change is the swap from createNoOpTelemetry() to telemetryProxy plus widening the accessor to 'warn' \| 'error' and reusing it for the auth-session failure path. The counter recommendation is sound but must not be attributed to APP2 as observed fact. |
| 62 | SUB-CLAIM: 'hume-demo tests/unit/start.unit.spec.ts after 907a42f asserts expect(result.response).toBe(originalResponse)' | Not confirmed. 907a42f does touch tests/unit/start.unit.spec.ts (20 lines) and tests/unit/server.unit.spec.ts (+59), but that assertion does not appear in the hunks I read. Likewise the mechanism claim about client-abort/disposer ordering is the investigator's inference, not stated by either commit. Keep the finding; drop the unsupported specifics. |
| 63 | SUB-CLAIM (attribution): 'throwServerFnErrorForReason … the same prototype-lookup class APP1 shipped a fail-closed fix for (commit 4c8eceb, v2 key-ring / Object.hasOwn)' | The supporting APP1 commit was cited but I could not confirm it, and the template finding itself does not depend on it. I have re-tagged the finding appsThatSolvedIt=['neither'] and kept it as a standalone low-severity hardening item. Do not publish the 'APP1 hit this class' framing without re-verifying 4c8eceb. |
| 64 | Both apps adopted a `.agents/skills/` vendoring convention alongside `skills-lock.json` | `/home/user/iris-insights-crm/.agents` does not exist — `ls` returns 'No such file or directory'. Only APP1 (/home/user/hume-demo/.agents/skills/{domain-modeling,grill-with-docs,grilling}) vendors skill content into the repo. The investigator cited only APP1's directory as evidence for a claim about both. The `skills-lock.json` convergence itself survives (both files exist, and the three mattpocock skills carry byte-identical `computedHash` values), but APP2's lock has 14 entries, 11 of which are app-specific `heygen-com/hyperframes` video skills — so the shared surface is 3 skills, not a shared vendoring convention. The parent finding is kept with this correction applied; only the '.agents/skills/ convention' sub-claim is refuted. |
| 65 | With no OTLP collector and no Sentry DSN the template 'produces literally nothing in the platform log stream' | Overstated. The template's kernel logger has a console mirror: `src/modules/kernel/infrastructure/logger/telemetry.ts:182` `const shouldMirrorToConsole = consoleMirror ?? config.consoleMirror;`, driven by `src/modules/kernel/infrastructure/config/logger.ts:31` `consoleMirror: env.LOGGER_CONSOLE_MIRROR ?? env.LOGGER_PRETTY ?? !isProd`. So logs DO reach stdout when `LOGGER_CONSOLE_MIRROR=true`, and the default is only off because `isProd` is true. The accurate claim — which I substituted into the kept finding — is that the mirror is off by default in production and covers only `logger.*` calls, while `recordMetric`, `startSpan`, `startManualSpan` and `captureException` are dropped unconditionally by the no-op adapter with no escape hatch at all. |
| 66 | The generator's missing `tsPreCompilationDeps` under-reports the architecture diagram 'by ~27%' | Arithmetic framing is wrong relative to the truth. APP2's own commit b60c9a5 records '78 -> 99 edges'. 21 missing edges out of 99 real edges is 21%, not 27%; 27% is the increase relative to the already-wrong number. The defect itself is fully confirmed (template `scripts/generate-module-dependency-graph.ts:473-490` passes options straight through, and `grep tsPreCompilationDeps .dependency-cruiser.cjs` returns nothing), so the finding is kept with the exact figure '21 of 99 edges' substituted for the percentage. |
| 67 | `config-diagnostics.ts` routes console output through `globalThis` with a bracket-accessed method to stay outside the Semgrep rule | Wrong module. `/home/user/iris-insights-crm/src/modules/kernel/infrastructure/config/config-diagnostics.ts:1` opens with `/* oxlint-disable no-console */` and line 35 is a plain `console.info(...)`; it stays legal via an explicit exclusion at `.semgrep.yml:1014`. The `globalThis`-bracket idiom belongs to `src/platform/telemetry/report-failure.ts:39-41` and `src/platform/telemetry/console.ts`. The investigator conflated the two techniques across findings; both are worth documenting but a recommendation to 'use the globalThis idiom in config-diagnostics' would contradict what APP2 actually built. |
| 68 | APP1's CodeQL baseline seeds `requiredRules` for the template's four shipped queries | `/home/user/hume-demo/.github/codeql/codeql-baseline.json` lists exactly two `requiredRules`: `start-ui-web/unprotected-server-function` and `start-ui-web/untrusted-data-to-sensitive-sink`. The template ships four queries (verified by find: missing-route-search-deps.ql, unprotected-server-function.ql, react-dangerous-html-api.ql, untrusted-data-to-sensitive-sink.ql). The recommendation to seed all four in the template is still sound, but it is an improvement on APP1, not a description of it. |
| 69 | `components.json` advertises a shadcn workflow the codebase cannot actually accept — the kit is @base-ui/react, not Radix | The load-bearing premise is wrong and the supporting numbers are wrong. The investigator argued 'the default shadcn registry emits Radix-based source', but components.json:3 declares `"style": "base-vega"` — a Base UI style identifier, not the Radix default. The config is therefore self-consistent with a Base UI component kit rather than contradicting it, which is the opposite of the claim. The investigator's own confidence was 'medium' and they admit 'I could not execute `npx shadcn add` … the concrete failure mode is reasoned from the import evidence rather than observed' — so there is no observed defect at all. Their counts are also off: `grep -rho "from '@base-ui/react[^']*'" src/platform/components/ui/ \| sort -u \| wc -l` → 18 distinct subpaths, not 22. And the mitigating claim 'both apps' component sets are byte-identical in membership … zero added, zero removed in either app' is false for iris: `diff <(ls template/src/platform/components/ui) <(ls iris/src/platform/components/ui)` shows iris removed 43 `.fixture.tsx` files. The one true residue — README.md documents no component-authoring recipe at all — is real but is a doc gap, not a defect in components.json, and is better carried by the README finding. Zero components were added by either app in ~170 combined commits, so no forker hit this. |
| 70 | AGENTS.md describes the repo as a 'minimal boilerplate fork' with only the kernel module | Not present in the template. `grep -n "boilerplate\\|minimal" /home/user/start-ui-tanstack-drizzle-shadcn-hexagonal/AGENTS.md` returns nothing, and `ls src/modules/` returns account, auth, book, email, genre, kernel, user — seven modules, not one. AGENTS.md's opening section reads 'A TanStack Start application in TypeScript, organized as a strict modular monolith with hexagonal boundaries per capability.' The investigator lifted this claim from hume-demo's CLAUDE.md, which is describing hume-demo's own AGENTS.md — and even there it is wrong: `grep -n "boilerplate\\|minimal" /home/user/hume-demo/AGENTS.md` also returns nothing. This is a chain-of-hearsay citation: an app's self-description quoted as if it were a verified fact about the template. It must not reach the final report. |
| 71 | Neither the template nor either app has a .claude/skills directory | False for APP1. `ls -R /home/user/hume-demo/.claude/skills` returns three skills: `domain-modeling` (SKILL.md, ADR-FORMAT.md, CONTEXT-FORMAT.md, agents/openai.yaml), `grill-with-docs` (SKILL.md, agents/openai.yaml) and `grilling` (SKILL.md, agents/openai.yaml). This inverts the finding's conclusion in a useful direction: APP1 did invent per-repo skills, so 'ship skills in the template' is supported by app evidence rather than only by upstream trend, and domain-modeling in particular is a concrete candidate to upstream. |
| 72 | Both apps added a CLAUDE.md with an 'Authoritative references' table pointing back at AGENTS.md / TESTING.md / CONTEXT.md / .claude/rules | Half wrong. Both apps do have a CLAUDE.md (hume-demo 8043 bytes, iris-insights-crm 11240 bytes) and both do point at AGENTS.md and TESTING.md — that much holds. But iris-insights-crm has NO `.claude/` directory at all (`ls /home/user/iris-insights-crm/.claude/` → No such file or directory; a full `ls -a` of the repo root confirms it), so it cannot reference `.claude/rules`, and `grep -n '\.claude' CLAUDE.md` in iris returns nothing. The template's `.claude/rules` was committed 2026-05-29 to 2026-06-01 (commits 9819ad3, 13c01ab, 0d0c224), well before iris forked on 2026-07-31 — so APP2 actively DELETED the template's `.claude/rules` and `.claude/settings.json` and replaced them with a single CLAUDE.md. That is a 'template dead weight an app removed' signal pointing the opposite way from the finding as written, and it should be investigated rather than assumed to be neglect. |
| 73 | @tanstack/zod-adapter 1.167.0 is not deprecated and remains correct for router search-param validation | Checking only the `deprecated` field was insufficient and led to the wrong conclusion. The published manifest for @tanstack/zod-adapter@1.167.0 declares `peerDependencies: {"zod": "^3.23.8", "@tanstack/react-router": ">=1.43.2"}` — it does NOT support Zod 4, and the template forces it through anyway via pnpm-workspace.yaml `peerDependencyRules: allowedVersions: '@tanstack/zod-adapter>zod': '4'`. It was last published 2026-05-15 (only a 1.167.1-pre.0 since, 2026-07-24) while @tanstack/react-router is at 1.170.23. And hume-demo removed the package entirely. See the corresponding entry under `missed` for full evidence — this is a live modernization item, not a settled one. |
| 74 | Recommendation: 'ship a Renovate or Dependabot config with grouped PRs and an explicit hold-list' | The template already has one. `.github/dependabot.yml` exists and was added 2026-05-29 (commit 4459a84, 'Merge pull request #53 from hbmartin/tanstack-security'). It configures weekly npm updates with `versioning-strategy: increase`, `open-pull-requests-limit: 10`, and six named groups (tanstack, sentry, react, drizzle, dev-tooling, production-minor-patch), plus github-actions and docker ecosystems. Its own header comment states 'Major bumps are kept separate so framework upgrades get explicit review.' The recommendation as written would have the maintainer build something that exists. The real, narrower finding — that this config is demonstrably inert and lacks an ignore-list — is reported under `missed`. |
| 75 | 'APP2 forked and froze' / 'the evidence from APP2 is that forks otherwise never upgrade at all' | Contradicted by iris-insights-crm's pnpm-workspace.yaml. APP2 actively maintained a substantially expanded security-override block dated 2026-08-04 — six days before this audit — with brace-expansion, esbuild, fast-uri, js-yaml, launch-editor, postcss, protobufjs, shell-quote and socket.io-parser entries the template lacks, each carrying an advisory ID and rationale. It also converted the whole block from exact pins to caret ranges with a documented reason. APP2 did freeze its *tool* versions (oxlint, oxfmt, vite, tsconfig all byte-identical to the template), but it did not stop upgrading — it prioritized supply-chain remediation over toolchain currency. Characterizing it as inert misreads the signal and would lead the report to the wrong conclusion about what forks need from the template. |
| 76 | Missing verbatimModuleSyntax lets type-only Drizzle/Better Auth imports leak into the client bundle and 'quietly defeats the dependency-cruiser rules' | Overstated to the point of being wrong. dependency-cruiser runs on source (`depcruise --config .dependency-cruiser.cjs src`, package.json:52) and its rules match import statements regardless of what is emitted, so no emit-time behaviour can defeat them. Both tsc and oxc elide type-only imports whose bindings are unused. The genuine argument for verbatimModuleSyntax under a per-module isolated transform is determinism and explicitness — worth adopting, but not a security or guardrail-integrity issue. The surrounding finding survives; this specific rationale should not be repeated. |
| 77 | No `.mcp.json` — the stack maps onto the 2026 default MCP set (playwright / dbhub / Sentry) | Absence verified (`ls -a \| grep mcp` → nothing in any of the three repos), but the finding does not survive as a template recommendation. It is entirely speculative: NEITHER app added a `.mcp.json`, so there is zero evidence from this corpus that the missing file impeded anything. The specific server picks (`@playwright/mcp`, `@bytebase/dbhub`, Sentry HTTP) come from marketing round-ups (builder.io, techsy.io) I could not fetch and cannot sanity-check; the '30,000 GitHub stars as of May 2026' figure is unsourced and unverifiable. The investigator's own two caveats — that project `.mcp.json` servers sit pending human workspace-trust approval, and that tool listings cost context at session start — mean a template CANNOT enable them for a fork anyway, so shipping the file yields a pending-approval prompt rather than a capability. Committing a DSN-bearing `db` entry into a starter template is also actively risky given the repo's own `.secrets.baseline`/detect-secrets posture. Recommend dropping this from the report, or reducing it to a single sentence in a documentation section. |
| 78 | Nothing packages the agent harness for reuse, so every fork silently diverges | Factually wrong on its premise. The template DOES have a packaging and distribution mechanism: README.md:26 documents `pnpm create start-ui -t web myApp` as the supported way to start a project from it. And that mechanism demonstrably preserves the agent config — hume-demo's `.claude/settings.json`, `.claude/rules/architecture.md` and `.claude/rules/modules.md` are all byte-identical to the template's (verified by `diff`, no output). What actually happened to iris is not 'divergence from a missing packaging layer': `git ls-tree -r --name-only af6ae45 \| grep -i claude` shows `.claude/` was never in its first commit at all, while `.github/`, `.vscode/`, `.zed/` and `.oxlintrc.json` were — i.e. one directory was dropped during a copy, a copy/discoverability problem, not a packaging-format problem. The plugin recommendation is also premature by the investigator's own admission ('it is the step after them'), rests entirely on docs pages that could not be re-fetched, and would add a marketplace repo and version-bump workflow to solve a problem that a line in the README's 'Setup your IDE' section solves. The real, evidenced remedy is captured in the `.claude/settings.json` and orphaned-`.claude/rules/` findings; this one adds only unsupported machinery. |
| 79 | 'Both apps independently adopted vendored skills' is the strongest signal in this corpus that a skills layer belongs upstream | The interpretation is refuted even though the artifacts are real. The two `skills-lock.json` files share exactly three entries with byte-identical `computedHash` values (domain-modeling 363cb0f5…, grill-with-docs 9c460cbd…, grilling 368d3dd7…), all from a single third-party personal toolkit, `mattpocock/skills`. Eleven of iris's fourteen entries are `heygen-com/hyperframes` video and motion-graphics skills with no relationship to a TypeScript web app. And iris's lock file was already present in its squashed FIRST commit, af6ae45 (`git log --oneline -- skills-lock.json` → f308617, af6ae45), so it was not adopted in response to any difficulty building on the template. This is one developer carrying the same personal skill set into every repository, not two teams independently discovering a template gap. It cannot bear the weight of 'the strongest signal available'. The underlying recommendation — ship first-party golden-path skills — is retained in the verified list at medium severity, but the 'both apps invented it' justification is withdrawn. |
| 80 | "Dependabot with no grouping" — the template supposedly produces one PR per package across 126 dependencies, and should switch to Renovate | Factually wrong. I read /home/user/start-ui-tanstack-drizzle-shadcn-hexagonal/.github/dependabot.yml in full (and re-fetched it from origin/main at 199954ffb2fe via the GitHub API to rule out a local-only edit). It defines SEVEN groups: `tanstack` ('@tanstack/*'), `sentry` ('@sentry/*'), `react` (react, react-dom, @types/react, @types/react-dom), `drizzle` ('drizzle-*'), `dev-tooling` (dependency-type: development), `production-minor-patch` (dependency-type: production with exclude-patterns for the four groups above), and `actions` ('*') for github-actions — plus a docker ecosystem block. Its header comment states the intent explicitly: 'Security-only updates land immediately; routine patch/minor bumps are grouped weekly to reduce PR noise. Major bumps are kept separate so framework upgrades get explicit review.' The stated problem does not exist, and the Renovate migration recommendation was premised entirely on it. The investigator appears to have inferred 'no grouping' from the mere presence of the file without opening it. |
| 81 | "No production deployment story exists anywhere in the template or either app" | Materially false for all three repos. The TEMPLATE has a 157-line deployment guide at README.md:194-350: a '## Production' section (194-201) with the pnpm install/build/start sequence, and a '## Deploy' section (203-350) that states Nitro is already enabled in vite.config.ts, lists four pre-deploy requirements (Node 24, production .env.example values, VITE_BASE_URL over HTTPS with AUTH_ALLOWED_HOSTS for preview domains, and an explicit warning not to use `pnpm db:push` in production because it bypasses migration history), then gives per-platform instructions in collapsible sections for Cloudflare Workers (wrangler login/deploy, NODE_VERSION/PNPM_VERSION build vars, nodejs_compat flag, and a caveat that Workers is not a normal Node server), Vercel (7-step Git import, `pnpm dlx vercel --prod`, AUTH_ALLOWED_HOSTS="*.vercel.app"), Railway (Railpack detection, RAILPACK_NODE_VERSION=24, `railway init`/`railway up`, PORT handling), and Render (NITRO_PRESET=render-com, HOST=0.0.0.0, and a full copy-pasteable render.yaml) — each with upstream documentation links. Both APPS also have one: hume-demo/README.md:166 '## Vercel deployment' and iris-insights-crm/README.md:495 '## Production runbook' (plus :541 Routine operations, :560 Backup and recovery, :574 Troubleshooting). The investigator's evidence — no Dockerfile, no fly.toml/vercel.json/render.yaml committed, no deploy workflow — is all individually true (I reproduced every one of those find/ls commands), but it does not support the stated conclusion. The narrower true finding is filed under `missed` instead. |
| 82 | "There is currently no supported way to start a project from this template other than 'clone it and delete things'" (the premise of the critical rating on the README finding) | WebFetch of https://github.com/hbmartin/start-ui-tanstack-drizzle-shadcn-hexagonal reports the repository is a 'Public template' — GitHub's 'Use this template' generation path is available today. This is also the most likely explanation for the exact evidence the investigator gathered elsewhere: 'Use this template' creates a repo with a single parentless root commit and no shared history, which is precisely what `git rev-list --max-parents=0` returns in both apps. The README defect is real and is retained as a high-severity doc-drift finding, but the 'no scaffolding path at all' framing (and the resulting critical rating) is wrong. |
| 83 | Evidence that the template has no tags and that its history shares no SHA with the apps, as drawn from the local clone | The evidence, not the conclusion, is invalid — and the investigator presented it as verified. `git rev-parse --is-shallow-repository` returns true in /home/user/start-ui-tanstack-drizzle-shadcn-hexagonal and .git/shallow exists (82 bytes); the local clone holds 113 commits while GitHub reports 1,411 on main. Therefore commit 4459a840b27f (2026-05-29), cited as 'template oldest', is a shallow truncation point rather than a root commit, and `git tag` returning empty is the expected behaviour of a shallow clone, not proof the repo is untagged. I re-established both claims from valid sources — GitHub API list_tags → [] and list_releases → [], and `git rev-list --max-parents=0 HEAD` in each app's non-shallow clone — and the conclusions do survive, so the finding is retained with rebuilt evidence rather than dropped. Flagging it because the same shallow-clone trap will corrupt any other history-based claim in the final report. |
| 84 | "`npx @tanstack/cli add clerk drizzle` adds add-ons to EXISTING projects" — the basis for treating TanStack add-ons as a fork-update mechanism | Unconfirmed and probably overstated. I fetched https://raw.githubusercontent.com/TanStack/cli/main/docs/templates.md and verified the parts I could: `tanstack template init`, `tanstack template compile`, and consumption via `tanstack create new-app --template ./template.json` or a URL are all real and quoted correctly. But that document 'does not explicitly address whether add-ons can be added to existing projects' — it only demonstrates add-ons at creation time (`tanstack create my-template --add-ons clerk,drizzle,sentry`). The investigator sourced the 'existing project' claim to the CLI README, which I did not re-fetch. Since the entire 'package a template improvement as an add-on and push it to diverged forks' recommendation rests on that one unverified capability, and since the investigator themselves conceded add-on hooks are 'designed for injecting into freshly generated apps', I am refuting the recommendation and retaining only the verified half: a compiled template.json is a legitimate, versioned publish path for the create flow. Confidence on the add-on-as-update-channel idea should be low, not high. |
| 85 | "Makerkit's CLI + MCP server" three-way diff tooling, filed at high confidence | Not refuted on substance, but the confidence rating is unsupported and I am downgrading it to low. I could not re-verify the makerkit/cli README (the specific command and MCP tool names — makerkit_check_update, makerkit_apply_update, makerkit_project_pull, makerkit_project_resolve_conflicts — are quoted from a single source the investigator read once, and Makerkit is a commercial product whose repo I could not independently confirm). The useful, source-independent content is the base/local/remote framing, which is just standard three-way merge and needs no citation. Recommend the final report keep the three-way-merge recommendation and drop or heavily hedge the Makerkit product claims, since an unverifiable vendor citation in a report the user may act on is a liability. Note also that this finding is entirely contingent on the provenance stamp existing first. |
