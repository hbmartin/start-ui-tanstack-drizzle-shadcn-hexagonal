# Module Rules

Authoritative reference: `AGENTS.md`.

Use the module shape, public gates, layer dependencies, Result policy, auth
ports, and composition rules there. This file is a thin Claude path mapping and
must not duplicate or extend the provider-neutral repository policy.
